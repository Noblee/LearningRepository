# 面试题目集（候选人五）— 基于简历定制

> **候选人核心画像：** 8 年经验，天津大学本科，蚂蚁金服 → 小红书 → BingX → 个人创业（AI Agent）。核心经历：小红书搜索广告检索引擎（工程 leader）、BingX 活动营销规则引擎（Aviator 决策树）、蚂蚁变更管控平台（状态机 + BPM 引擎）、个人 AI Agent 项目（LangGraph）。Java 为主，兼具 Python/AI 工程化能力。

---

## 一、项目深挖类

### 题目 1：小红书搜索广告检索引擎——召回架构

**问题：**
你担任搜索广告工程侧 leader，支撑物料量级两年 10+ 倍增长：
1. 请描述搜索广告检索引擎的整体架构，从用户搜索到广告展示的完整链路。"两段式召回"和"一段式召回"分别是什么？为什么需要两种模式？
2. "DAG 编排引擎"的设计细节是怎样的？节点是什么？边的依赖关系如何定义？运行时如何调度？
3. 物料量级增长 10 倍，系统架构层面你做了哪些关键的扩展性设计来承接这个增长？

**参考答案：**

1. **检索引擎全链路：**
   ```
   用户搜索 Query
        ↓
   Query 理解（分词、意图识别、Query 改写）
        ↓
   召回层（从亿级物料中快速筛选候选集，万级）
     ├── 一段式召回：Query → 倒排索引直接检索 → 候选集
     └── 两段式召回：Query → 粗召回（宽松匹配，十万级）→ 精召回（精确匹配+过滤，万级）
        ↓
   粗排（轻量模型打分，筛选到千级）
        ↓
   精排（复杂模型打分，排序到百级）
        ↓
   重排/机制层（去重、频控、多样性、竞价）
        ↓
   广告展示
   ```

   - **一段式召回：** 直接从倒排索引中根据 Query 检索匹配的广告物料，一步到位。适合精确匹配场景（如关键词定向广告）。
   - **两段式召回：** 先做宽泛的粗召回（如语义匹配、向量召回），得到较大候选集，再做二次精选（如相关性过滤、定向条件过滤）。适合需要更大召回覆盖的场景（如兴趣扩展）。
   - 两种模式并存的原因：不同广告主/策略对召回精度和覆盖面的需求不同。精确投放用一段式（快且准），扩量探索用两段式（广且全）。

2. **DAG 编排引擎：**
   - 节点（Node）：每个节点是一个可独立执行的召回策略单元（如关键词召回、向量召回、人群定向召回、相关性过滤等）。
   - 边（Edge）：定义节点间的数据依赖和执行顺序。无依赖的节点可以并发执行。
   - DAG 定义：通过配置文件或代码定义节点依赖关系，框架解析后生成 DAG 拓扑。
   - 运行时调度：拓扑排序 → 按层级并发执行（无依赖的同层节点并行）→ 上游节点完成后触发下游 → 全部完成后汇总结果。
   - 核心能力：(a) 算法同学可以通过配置快速增删召回策略节点，无需修改引擎代码；(b) 支持条件分支（如 A/B 实验走不同的召回路径）；(c) 超时控制和降级（某节点超时则跳过，不影响整体）。

3. **10 倍增长的扩展性设计：**
   - 索引分片：物料索引按维度（如行业/类目）分片，不同分片部署在不同节点，请求路由到对应分片。
   - 索引瘦身：增量索引接入离线消费中心，级联删除无效物料（简历中提到索引量级精简 50%），用更少资源承载更多有效物料。
   - 倒排质量截断：大词（高频 Query）的倒排链可能有几十万条，全部遍历太慢。按物料质量分（如 eCPM 预估值）截断，只取 TopK 进入后续流程。
   - 离线预计算：将部分在线计算的逻辑下沉到离线（如定向条件预匹配），减少在线计算量。
   - 水平扩容：召回服务无状态化，按 QPS 水平扩容。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 全链路理解 | 3 分 | 从 Query 到展示的完整链路清晰，召回/粗排/精排/重排各阶段职责明确 |
| DAG 引擎设计 | 4 分 | 节点/边/调度/超时降级等设计细节完整，能说清如何支撑算法快速迭代 |
| 扩展性设计 | 3 分 | 应对 10 倍增长的措施具体且多层次 |

---

### 题目 2：倒排索引优化与收入提升

**问题：**
你提到"大词倒排质量截断与 bitmap 定向过滤优化，召回 P99 下降 10%，广告收入提升 2%"：
1. 什么是"大词"？大词在广告检索中会导致什么问题？
2. "倒排质量截断"的具体策略是什么？按什么维度截断？截断后如何保证不会漏掉高价值广告？
3. "bitmap 定向过滤"是什么？bitmap 在这里是怎么用的？为什么比传统过滤方式更快？
4. 为什么性能优化（P99 下降）会带来收入提升？二者的因果关系是什么？

**参考答案：**

1. **大词问题：**
   - 大词：搜索量极高且含义宽泛的 Query（如"连衣裙""手机壳""零食"），匹配到的广告物料非常多（倒排链长度可达数十万甚至百万）。
   - 问题：遍历整条倒排链耗时过长，导致召回超时 → 召回不完整或直接失败 → 用户看不到广告 → 广告收入损失。

2. **倒排质量截断：**
   - 策略：对倒排链中的物料按质量分排序（如 eCPM = 预估点击率 × 出价），只保留 TopK 条。
   - 截断维度：eCPM 预估值（综合出价、相关性、历史 CTR 等因素）。
   - 保证不漏高价值广告：(a) 质量分排序本身就把高价值广告排在前面；(b) 截断阈值根据统计数据设定（如保留 Top 5000 覆盖 99% 的竞价收入）；(c) 离线定期 Diff 验证截断后的收入损失在可接受范围内。
   - 本质是**精度和性能的 trade-off**：牺牲极少量的长尾低质量广告覆盖，换取整体召回性能大幅提升。

3. **bitmap 定向过滤：**
   - 场景：广告有定向条件（如地域、年龄、性别、兴趣标签），召回后需要过滤不符合定向条件的物料。
   - 传统方式：逐条遍历候选物料，对每条检查定向条件是否匹配（涉及多次内存访问和条件判断），O(N × M) 复杂度。
   - bitmap 方式：预先为每个定向条件构建 bitmap（如"北京用户"对应一个 bitmap，第 i 位为 1 表示第 i 个物料定向了北京）。过滤时对多个 bitmap 做**位与（AND）运算**，一次 CPU 指令可以处理 64 个物料（64 位系统），极大加速过滤。
   - 为什么更快：(a) bitmap 操作对 CPU 缓存友好（连续内存访问）；(b) 位运算是 CPU 的原生操作，极快；(c) 时间复杂度从 O(N×M) 降到 O(N/64 × M)。

4. **性能优化 → 收入提升的因果链：**
   ```
   P99 下降 → 超时率降低 → 更多请求成功返回广告
                          → 原本超时的大词请求现在能返回广告
                          → 广告填充率提升
                          → 更多广告展示机会
                          → 更多点击和转化
                          → 收入提升
   ```
   另外，质量截断保留的是高 eCPM 物料，本身就提高了广告的整体质量和竞价水平。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 大词理解 | 2 分 | 能说清大词的定义和对检索系统的影响 |
| 截断策略 | 3 分 | 按 eCPM 截断，有保障高价值物料的机制 |
| bitmap 原理 | 3 分 | 位运算加速过滤的原理清晰，与传统方式的性能差异能量化 |
| 性能→收入逻辑 | 2 分 | 因果链完整，理解广告系统的商业逻辑 |

---

### 题目 3：Dumper 离线框架设计

**问题：**
你从 0 到 1 设计了 dumper 框架，承载商业化 95%+ 的策略任务：
1. 这个 dumper 框架解决了什么问题？没有它之前策略任务是怎么跑的？
2. 框架的核心模块有哪些？"数据构建 / 分发 / 调度 / 管理"各自负责什么？
3. 承载 95% 的策略任务，框架的稳定性如何保障？如果某个策略任务挂了，会影响其他任务吗？
4. 这个框架和公司内部已有的大数据调度平台（如 Airflow）有什么区别？为什么不直接用？

**参考答案：**

1. **解决的问题：**
   - 之前：每个策略任务各自开发离线处理逻辑，代码分散在多个项目中，无统一管理。导致：重复开发（数据读取、写入、分发逻辑反复写）、运维困难（无统一监控和告警）、故障排查慢（每个任务的日志格式/存储位置不统一）。
   - 之后：统一的框架封装了通用能力，策略开发者只需实现核心的数据处理逻辑（"做什么"），框架负责"怎么执行"。

2. **核心模块：**
   - **数据构建（Builder）：** 从数据源（Hive/MySQL/API）拉取数据 → 按策略逻辑处理/转换 → 输出标准化数据（如索引文件、KV 数据）。提供标准的 Builder 接口，策略开发者实现自己的处理逻辑。
   - **分发（Distributor）：** 将构建完成的数据分发到线上服务节点（推送到检索引擎的索引服务器、写入 Redis/ES 等）。支持全量分发和增量分发。
   - **调度（Scheduler）：** 定时/事件触发策略任务的执行。支持 cron 表达式、依赖触发（上游任务完成后触发下游）、手动触发。
   - **管理（Manager）：** 任务的注册/配置/版本管理、执行历史查看、日志查询、监控告警、手动重试。提供管理后台。

3. **稳定性保障：**
   - 任务隔离：每个策略任务独立运行（独立进程/容器），一个任务挂了不影响其他任务。
   - 重试机制：任务失败自动重试（配置重试次数和间隔），超过重试次数进入失败队列并告警。
   - 超时控制：每个任务有执行时间上限，超时强制终止。
   - 数据校验：分发前校验数据量和格式（如新数据量不能比上一次骤降 50%，防止错误数据上线）。
   - 灰度分发：支持先分发到部分节点验证，确认无误后全量分发。
   - 回滚：保留历史版本数据，发现问题可一键回滚到上一版本。

4. **与 Airflow 等的区别：**
   - Airflow 是通用的 DAG 调度框架，擅长任务编排和调度，但不提供"数据构建→分发→上线"的端到端能力。
   - Dumper 框架是垂直于广告场景的全流程框架，内置了索引构建、在线分发、灰度验证等广告特有的能力。
   - 调度只是框架的一个模块，核心价值在于"构建+分发+管理"的一体化。
   - 类比：Airflow 是通用调度引擎，dumper 是广告领域的 DataOps 平台。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 问题定义 | 2 分 | 能说清框架之前的痛点 |
| 模块设计 | 3 分 | 四个模块职责清晰，接口抽象合理 |
| 稳定性 | 3 分 | 隔离、重试、校验、灰度、回滚等措施完整 |
| 定位区分 | 2 分 | 与通用调度平台的区别准确 |

---

### 题目 4：BingX 规则引擎——Aviator 决策树

**问题：**
你主导了基于 Aviator 的决策树规则引擎：
1. 为什么选择 Aviator 而不是 Drools 或自研表达式引擎？Aviator 的核心优势是什么？
2. "决策树"的数据结构是什么样的？一个完整的活动规则在决策树中是怎么表达的？请举例。
3. "秒级热更新"的完整链路是什么？如何保证多实例间的一致性？30 秒兜底对账任务具体做什么？
4. 单次决策 RT 从 50ms 降到 5ms，10 倍的优化来自哪里？

**参考答案：**

1. **Aviator 选型：**
   - Aviator：轻量级 Java 表达式引擎，将表达式编译为 JVM 字节码执行，性能接近原生 Java。
   - vs Drools：Drools 是重量级规则引擎（Rete 算法），适合复杂规则推理，但学习曲线陡、部署重、规则修改需重新编译。活动营销的规则逻辑相对简单（条件判断+动作执行），Drools 杀鸡用牛刀。
   - vs 自研：Aviator 已经成熟稳定，支持自定义函数扩展，无需重新造轮子。
   - 核心优势：(a) 编译为字节码，性能极高；(b) 支持预编译缓存，重复执行免编译开销；(c) API 简单，上手快；(d) 沙箱安全，不能执行危险操作。

2. **决策树数据结构：**
   ```
   根节点（活动入口）
     ├── 条件节点：user.level >= 'VIP'
     │     ├── 条件节点：task.trade_count >= 5
     │     │     └── 动作节点：发放 10 USDT 奖励
     │     └── 条件节点：task.trade_count >= 1
     │           └── 动作节点：发放 2 USDT 奖励
     └── 条件节点：user.is_new == true
           └── 动作节点：发放新手礼包
   ```
   - 每个非叶子节点是一个 Aviator 条件表达式（返回 true/false）。
   - 叶子节点是动作节点（发奖/通知/更新状态）。
   - 存储：JSON 格式持久化到 MySQL，每个节点包含 `{id, parent_id, type, expression, action_config}`。
   - 执行：从根节点开始递归，每个条件节点执行 Aviator 表达式求值，为 true 则进入子节点，命中叶子节点则执行动作。

3. **秒级热更新链路：**
   ```
   运营修改规则（前端低代码画布）
      ↓
   后端 Parser 解析 JSON → 校验 → 写入 MySQL（版本号 +1）
      ↓
   同步更新 Redis（版本号 +1 + 规则数据）
      ↓
   发送 MQ 广播消息（通知所有实例）
      ↓
   各实例收到 MQ → 拉取 Redis 最新规则 → Aviator 预编译新表达式 → 替换本地缓存
   ```
   - 多实例一致性：(a) MQ 广播确保所有实例收到通知；(b) 版本号机制——实例定期检查 Redis 版本号与本地版本号是否一致，不一致则拉取更新。
   - 30 秒兜底对账：每 30 秒定时任务检查本地规则版本号 vs Redis 版本号，如果不一致（MQ 消息丢失的兜底场景）则重新拉取并预编译。确保即使 MQ 故障，规则也会在 30 秒内最终一致。

4. **50ms → 5ms 的优化来源：**
   - **Aviator 预编译（最大收益）：** 原来每次决策都从字符串表达式解析+编译+执行，现在启动时/热更新时预编译为字节码缓存，执行时直接调用编译后的函数。编译过程约 10-20ms，预编译后执行 < 0.1ms。
   - **规则缓存在 Redis → 本地内存：** 减少每次决策的网络 IO（Redis 1-2ms → 本地内存 < 0.01ms）。
   - **决策树剪枝：** 根据用户属性快速跳过不可能命中的分支。
   - **批量处理优化：** Kafka 消费端批量拉取事件，减少单条处理的固定开销。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| Aviator 选型 | 2 分 | 与 Drools 的对比合理，理解"杀鸡用牛刀"的判断 |
| 决策树设计 | 3 分 | 数据结构清晰，能举出具体的业务规则示例 |
| 热更新链路 | 3 分 | MySQL→Redis→MQ→实例的完整链路，版本号+兜底对账保证一致性 |
| 性能优化 | 2 分 | 预编译是核心收益，能量化各优化点的贡献 |

---

### 题目 5：蚂蚁九州——状态机与 BPM 引擎

**问题：**
你负责了九州平台的状态机和 BPM 引擎：
1. 变更管控场景下的状态机有哪些状态？状态转移规则是什么？如何防止非法状态转移？
2. BPM 引擎中"用户定制化的审批流和执行流"是如何实现的？流程的定义、存储和执行分别是怎样的？
3. "错误码机制"和"自动执行预案"具体怎么工作？能否举一个具体 case？
4. "基于本地消息表的任务宕机迁移机制"——任务在执行到一半时机器宕机了，怎么在另一台机器上恢复？

**参考答案：**

1. **状态机设计：**
   - 变更工单状态示例：`待审批 → 审批中 → 审批通过 → 执行中 → 灰度中 → 观察中 → 完成 / 回滚中 → 已回滚 / 已关闭`
   - 状态转移规则：定义合法的状态转移矩阵（如"待审批"只能转移到"审批中"或"已关闭"，不能直接跳到"执行中"）。
   - 防止非法转移：
     - 代码层：状态机框架在执行转移前校验当前状态+目标状态是否在合法矩阵中。
     - 数据库层：乐观锁 `UPDATE SET status = new WHERE id = x AND status = old`，并发只有一个成功。
     - 日志审计：所有状态变更记录到审计日志，异常转移可追溯。

2. **BPM 引擎：**
   - 流程定义：用 JSON/XML 描述流程图（节点 + 连线），支持审批节点、执行节点、条件分支、并行网关、子流程等。
   - 存储：流程定义存储在 DB 中，支持版本管理。每个实例运行时创建运行记录，记录当前执行到哪个节点。
   - 执行：
     - 引擎加载流程定义 → 创建流程实例 → 从起始节点开始执行。
     - 遇到审批节点 → 挂起等待审批 → 审批通过后继续。
     - 遇到执行节点 → 调用 SPI 接口执行实际操作（如部署、配置变更等）。
     - 遇到条件分支 → 根据上下文数据判断走哪条路径。
   - 用户定制化：不同变更场景（SaaS/PaaS/IaaS）配置不同的流程模板，通过 API 总线对接不同平台的执行动作。

3. **错误码 + 自动预案：**
   - 机制：变更过程中每种可能的异常（如灰度阶段监控告警、执行超时、下游接口报错）定义一个错误码。每个错误码关联一个预案（如自动回滚、暂停等待人工介入、重试 N 次）。
   - 具体 case：灰度发布时，监控系统检测到错误率上升 → 报告错误码 `E_GRAY_ERROR_RATE_HIGH` → 系统自动查找该错误码关联的预案"自动回滚" → 触发回滚流程 → 通知相关人员。
   - 价值：实现**变更无人值守**——常见异常由系统自动处理，只有未知异常才需要人工介入。

4. **任务宕机迁移：**
   - 本地消息表记录任务的执行状态（包括当前步骤、上下文数据）。
   - 心跳机制：执行中的任务定期更新心跳时间戳。
   - 宕机检测：监控任务发现某条记录超过 N 秒未更新心跳 → 判定为宕机。
   - 迁移：另一台机器（或调度器）认领该任务 → 从本地消息表恢复执行上下文 → 从断点继续执行（如果是幂等操作则从当前步骤重新执行；如果不幂等则从上一个已确认成功的步骤开始）。
   - 防重复执行：认领时通过乐观锁 `UPDATE SET executor = 'B' WHERE id = x AND executor = 'A'` 保证只有一台机器认领成功。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 状态机设计 | 2 分 | 状态和转移规则合理，有防非法转移的机制 |
| BPM 引擎 | 3 分 | 流程定义/存储/执行三要素清晰 |
| 错误码机制 | 3 分 | 能举出具体 case，理解"无人值守"的价值 |
| 宕机迁移 | 2 分 | 心跳+认领+断点续做的方案完整 |

---

## 二、系统设计与架构能力

### 题目 6：广告检索系统的全链路漏斗优化

**问题：**
你参与了广告全链路漏斗效率优化，累计收入提升 10%：
1. 广告检索的"漏斗"每一层的淘汰率大概是多少？哪一层的优化对收入影响最大？
2. "相关性过滤前置"具体是什么？为什么前置能提升收入？
3. "顺畅度优化"是什么概念？如何度量和优化？
4. 作为工程 leader，你如何与算法同学协作做漏斗优化？工程侧和算法侧各自的边界在哪里？

**参考答案：**

1. **漏斗各层淘汰率（典型值）：**
   ```
   物料全量：亿级
      ↓ 召回：万级（淘汰 99.99%）← 覆盖面最大，对收入影响最大
      ↓ 粗排：千级（淘汰 ~90%）
      ↓ 精排：百级（淘汰 ~90%）
      ↓ 机制/重排：十级（淘汰 ~90%）
      ↓ 展示：个位数
   ```
   - 召回层对收入影响最大，因为它决定了候选集的"天花板"——后续链路只能在召回结果中挑选，召回不到的物料再好也没用。

2. **相关性过滤前置：**
   - 原来：相关性过滤在粗排或精排阶段执行（靠后），意味着召回了大量低相关性物料占用了后续链路的计算资源。
   - 前置后：在召回阶段就用轻量级的相关性模型过滤掉明显不相关的物料，让后续链路处理的都是高相关性物料。
   - 为什么提升收入：(a) 后续链路的计算资源集中在高质量物料上，排序更精准；(b) 节省的算力可以用来处理更多候选（提高召回量），整体广告质量和数量都提升。

3. **顺畅度优化：**
   - 概念：广告系统的全链路处理时间需要在严格的时间窗口内完成（如 200ms 内返回结果）。如果某个环节超时，后续环节被压缩或跳过，影响最终效果。
   - 度量：各环节耗时分布（P50/P99）、超时率、各环节的截断率（因超时被迫截断候选集）。
   - 优化：(a) 各环节设置合理的超时预算分配；(b) 减少串行调用，增加并行度；(c) 耗时不稳定的环节增加缓存或预计算；(d) 动态预算调整——如果召回快了，把省下的时间给精排。

4. **工程与算法协作：**
   - 算法侧：负责模型迭代（相关性模型、排序模型）、策略设计（截断阈值、过滤条件）、效果评估（AB 实验分析）。
   - 工程侧：负责系统架构支撑（DAG 引擎、索引管理、性能优化）、实验平台建设、数据管线（dumper 框架）、稳定性保障。
   - 边界：算法关注"效果"（CTR、CVR、收入），工程关注"效率"（RT、QPS、资源成本）。好的协作是在效果和效率之间找到最优平衡。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 漏斗理解 | 2 分 | 各层量级和淘汰率合理，知道召回层最关键 |
| 相关性前置 | 3 分 | 前置的理由和收入影响的逻辑链清晰 |
| 顺畅度 | 3 分 | 概念准确，度量和优化手段具体 |
| 协作模式 | 2 分 | 工程/算法边界清晰，有实际协作经验 |

---

### 题目 7：设计一个通用的 DAG 任务编排引擎

**问题：**
基于你在 dumper 框架和召回 DAG 引擎的经验，请设计一个通用的 DAG 任务编排引擎，要求：
- 支持任务间的依赖定义（串行 + 并行）
- 支持条件分支和动态跳过
- 支持超时、重试、降级
- 支持运行时监控和日志
- 用 Java 实现核心接口设计

**参考答案要点：**

```java
// 核心接口
interface Node {
    String getId();
    NodeResult execute(DAGContext context) throws Exception;
}

interface Condition {
    boolean evaluate(DAGContext context);
}

class DAGDefinition {
    List<Node> nodes;
    Map<String, List<String>> dependencies;  // nodeId -> 依赖的 nodeId 列表
    Map<String, Condition> branchConditions; // nodeId -> 执行条件
    Map<String, NodeConfig> configs;         // 超时/重试配置
}

class DAGEngine {
    void execute(DAGDefinition dag, DAGContext context) {
        // 1. 拓扑排序
        // 2. 按层级并发执行（无依赖的节点并行）
        // 3. 节点执行前检查条件（不满足则跳过）
        // 4. 节点执行失败 → 根据配置重试或降级
        // 5. 超时控制（Future.get(timeout)）
        // 6. 记录执行日志和状态
    }
}
```

关键设计点：
- 并发执行：`CompletableFuture` + 自定义线程池。
- 超时控制：每个节点包装为 `Future`，用 `get(timeout)` 控制。
- 降级：节点配置 `fallback` 函数，超时/异常时执行降级逻辑。
- 监控：每个节点执行前后埋点（开始时间、结束时间、状态、异常信息）。
- 上下文传递：`DAGContext` 作为共享数据载体，节点间通过 context 传递数据。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 接口抽象 | 3 分 | Node/Condition/DAGDefinition 抽象合理 |
| 并发调度 | 3 分 | 拓扑排序 + 层级并发的实现思路正确 |
| 容错设计 | 2 分 | 超时/重试/降级/条件跳过 |
| 可观测性 | 2 分 | 执行日志和监控埋点 |

---

## 三、AI 工程化能力

### 题目 8：LangGraph 智能体与 RAG

**问题：**
你的砚雀项目用 LangGraph 构建 multi-agent 写作系统：
1. LangGraph 与 LangChain 的区别是什么？为什么选 LangGraph 而不是纯 LangChain？
2. 你的 multi-agent 架构中，各个 agent 分别负责什么？它们之间的状态是怎么流转的？
3. RAG 检索增强中，embedding 模型和向量库你是怎么选型的？检索的精度和召回率如何优化？
4. "流式 SSE 生成与 DB 存储的一致性问题"——这个问题的本质是什么？你的"任务主记录 + 分片 chunk 幂等落库 + 结束态收口"方案具体是怎样的？

**参考答案：**

1. **LangGraph vs LangChain：**
   - LangChain：提供 LLM 调用、Prompt 管理、Chain 组合等基础能力，适合简单的线性流程（如 QA 链、摘要链）。
   - LangGraph：在 LangChain 基础上增加了**状态机 + 图编排**能力，支持循环、条件分支、多 agent 协作。适合复杂的多步骤、需要人机交互、需要回退重试的流程。
   - 选择 LangGraph 的原因：写作流程不是简单的线性链，而是包含"审核不通过 → 回退修改 → 重新审核"的循环，以及"人工二审 → 可能编辑后继续"的人机交互，纯 LangChain 的 Chain 难以表达。

2. **Multi-agent 架构：**
   - **风格提取 Agent：** 分析用户历史文章，提炼写作风格特征（用词偏好、句式结构、语气风格）→ 输出风格 profile。
   - **素材检索 Agent：** 基于写作主题调用 RAG，从用户的素材库（笔记、收藏、参考资料）中检索相关内容 → 输出素材列表。
   - **写作 Agent：** 接收风格 profile + 素材 + 用户指令，调用 LLM 生成文章 → 输出初稿。
   - **审核 Agent：** 对初稿做"去 AI 味"审核（检查 AI 典型的套话、重复、不自然表达）→ 输出审核意见。不通过则流转回写作 Agent 修改。
   - **人工审核节点：** 用户在 UI 上编辑修改 → 确认后继续。
   - **封面生成 Agent：** 根据文章主题调用图像生成模型 → 生成多比例封面。
   - 状态流转：LangGraph 的 StateGraph 管理全局状态（含当前阶段、文章内容、审核结果等），条件边决定流转方向。

3. **RAG 选型与优化：**
   - Embedding 模型：开源的 text2vec-large-chinese 或 OpenAI text-embedding-3-small（根据成本和效果选择）。
   - 向量库：Chroma（轻量、开发友好、适合个人项目）。大规模场景可换 Milvus/Qdrant。
   - 精度优化：(a) 文档分块策略（按段落/语义分块，而非固定长度）；(b) 查询改写（用 LLM 对用户 query 做扩展）；(c) Reranker 对初步检索结果做重排序。
   - 召回率优化：(a) 混合检索（向量检索 + 关键词检索融合）；(b) 多路召回（不同粒度的 chunk 分别检索后合并）。

4. **流式生成与 DB 一致性：**
   - 问题本质：LLM 流式返回 token（SSE），前端实时展示。但如果流式过程中断（网络中断/服务重启），已生成的内容可能部分丢失（未持久化）或重复（重试后重新生成）。同时，ORM session 在长流式连接中容易出问题（长事务/连接超时）。
   - 解决方案：
     - **任务主记录：** 生成任务开始时创建一条主记录（task_id, status=generating, total_chunks=0），作为任务的"总控"。
     - **分片 chunk 幂等落库：** 流式过程中，每收到一定数量的 token（如每 100 个 token 或每个段落），打包为一个 chunk，带上 `(task_id, chunk_seq)` 幂等写入 DB。重试时相同 chunk_seq 不会重复写入。
     - **结束态收口：** 流式完成后更新主记录 status=completed 并拼接所有 chunk 为完整内容。如果中断，status 保持 generating，恢复时从最后一个 chunk_seq 继续。
     - **ORM session 改为批次短事务：** 不用一个长 session 覆盖整个流式过程，而是每个 chunk 落库用独立的短事务，避免长事务和连接超时。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| LangGraph 理解 | 2 分 | 与 LangChain 的区别准确，选型理由合理 |
| Multi-agent 设计 | 3 分 | 各 agent 职责清晰，状态流转（含循环和人工介入）完整 |
| RAG 优化 | 2 分 | 分块/改写/Reranker/混合检索等优化手段丰富 |
| 流式一致性 | 3 分 | 问题定义准确，chunk 幂等+短事务方案可行 |

---

## 四、技术深度

### 题目 9：高并发广告系统的性能优化方法论

**问题：**
你在小红书和 BingX 都做了大量性能优化工作。请系统性地总结：
1. 在线链路（检索引擎）的性能优化，你通常从哪些维度入手？有没有一套方法论？
2. 线上 RT 抖动（P99 偶尔飙高但 P50 正常），你的排查套路是什么？
3. 从工程 leader 的角度，如何建立团队的性能意识？怎么避免"优化了又被新代码搞慢"？

**参考答案：**

1. **优化方法论（自上而下）：**
   - **链路层面：** 串行改并行、合并 RPC 调用、去除无效调用、异步化非关键路径。
   - **算法层面：** 截断（倒排截断）、剪枝（决策树剪枝）、预计算（离线算好线上直接用）。
   - **数据层面：** 缓存（Redis/本地缓存）、索引优化（bitmap/倒排）、数据瘦身（精简字段、压缩）。
   - **系统层面：** JVM 调优（GC/堆大小）、线程池调优、连接池调优、内核参数调优。
   - **架构层面：** 服务拆分（重逻辑独立部署）、读写分离、分片扩容。
   - **度量先行：** 任何优化前先建立 baseline（P50/P99/QPS/资源利用率），优化后量化对比。

2. **P99 抖动排查套路：**
   - 看时间规律：是否周期性（GC？定时任务？日志轮转？）。
   - 看机器维度：是所有机器还是个别机器（个别 → 可能是宿主机问题 / noisy neighbor）。
   - 看链路追踪：慢请求卡在哪个环节（下游 RPC？DB 查询？本地计算？）。
   - 看 GC 日志：是否与 Young GC/Full GC 停顿时间相关。
   - 看内核/容器层面：CPU Throttle（CFS 限制被触发）、NUMA 跨节点访问、网络丢包重传。
   - 常见凶手：(a) GC 停顿；(b) 某个下游偶尔慢导致的长尾；(c) 锁竞争；(d) 线程池排队。

3. **团队性能意识建设：**
   - Code Review 增加性能检查项（如禁止在循环中做 RPC 调用、禁止不带超时的外部调用）。
   - 性能门禁：新代码上线前必须通过性能基准测试，RT 劣化超过 N% 的不予上线。
   - 性能大盘：核心链路的 RT/QPS 实时可见，异常自动告警。
   - 定期 Review：每月/每季度做一次性能回顾，识别劣化趋势。
   - 文档沉淀：将典型的性能优化 case 沉淀为团队知识库，新人入职必读。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 优化方法论 | 4 分 | 多维度系统性，有"度量先行"的意识 |
| P99 排查 | 3 分 | 排查思路分层且具体 |
| 团队建设 | 3 分 | 有性能门禁、大盘、Review 等制度化手段 |

---

### 题目 10：系统稳定性——大促保障

**问题：**
你负责大促节点的系统稳定性保障（可用性 99.9%+）：
1. 大促前的准备工作有哪些？时间线是怎样的？
2. 压测是怎么做的？如何构造真实的广告检索流量？压测发现的最严重的问题是什么？
3. 降级预案有哪些？什么条件下触发？如何保证降级后广告收入损失最小？
4. 大促期间值班时，你最关注哪几个核心指标？

**参考答案：**

1. **准备工作时间线（示例）：**
   - T-4 周：梳理风险项（上下游依赖、容量瓶颈、历史故障）。
   - T-3 周：完成容量评估，提交扩容需求。
   - T-2 周：第一轮全链路压测，暴露问题。
   - T-1 周：修复压测问题，第二轮验证。降级预案联调。
   - T-3 天：代码冻结（Code Freeze），禁止非紧急变更。
   - T-1 天：扩容到位确认，预案演练，值班排班确认。
   - T 日：值班 On-Call，实时监控，预案待命。

2. **压测方案：**
   - 流量构造：回放线上真实流量（录制+倍速回放），或根据历史大促流量模型生成模拟流量。
   - 倍率：按预估峰值的 1.5-2 倍压测（留安全余量）。
   - 关注点：RT（P50/P99）、错误率、CPU/内存/网络、下游依赖的承载能力。
   - 发现的严重问题（示例）：某个召回策略在高并发下触发了大量 Redis 热 key 请求，单个 Redis 节点 CPU 打满导致超时。解决方案：本地缓存 + Key 分片。

3. **降级预案：**
   - **召回降级：** 关闭部分低优先级召回策略（如探索性召回），只保留核心策略，减少计算量。
   - **排序降级：** 精排模型降级为轻量模型（如用粗排模型替代精排模型），降低模型推理延迟。
   - **定向降级：** 关闭部分复杂定向条件（如实时人群包），减少过滤计算。
   - **频控放松：** 适当放松广告频控限制，增加广告展示机会。
   - 触发条件：RT 超过阈值 / 错误率超过阈值 / 下游依赖不可用。
   - 收入损失最小化：按"对收入影响从小到大"的顺序配置降级等级，先降影响小的策略。

4. **核心监控指标：**
   - **系统层：** 各服务 QPS、RT（P99）、错误率、CPU/内存利用率。
   - **业务层：** 广告填充率（有广告返回的请求占比）、CPM（千次展示收入）、召回量。
   - **依赖层：** Redis/ES/下游 RPC 的 RT 和可用性。
   - **资损层：** 计费异常检测（曝光/点击数据是否异常波动）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 准备流程 | 3 分 | 时间线清晰，关键节点（压测/冻结/扩容）完整 |
| 压测方案 | 2 分 | 流量构造方式合理，有实际发现问题的案例 |
| 降级预案 | 3 分 | 分级降级，按收入影响排序，触发条件明确 |
| 监控指标 | 2 分 | 系统/业务/依赖/资损多维度覆盖 |

---

## 五、Leader 能力与软技能

### 题目 11：技术 Leader 能力考察

**问题：**
你在小红书和 BingX 都担任了 team leader：
1. 你带了多少人？团队是怎么分工的？
2. 作为搜索广告工程 leader，你如何与算法 leader、产品 PM 协调优先级和资源？有没有出现过冲突？怎么解决的？
3. 你在团队中推动技术改进（如 dumper 框架）时，如何说服团队投入时间做基础设施而非业务需求？
4. 你评价一个团队成员的工作好坏的标准是什么？

**参考答案：** 开放题，主要考察：
- 团队管理的实际经验（而非理论）
- 跨团队协作和冲突解决能力
- 技术 ROI 判断能力（何时投入基础设施 vs 业务交付）
- 人才培养意识

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 团队管理 | 3 分 | 有实际的分工和管理经验 |
| 跨团队协作 | 3 分 | 有具体的冲突解决案例 |
| 技术判断 | 2 分 | 能平衡基础设施投入和业务交付 |
| 人才培养 | 2 分 | 有评价标准和培养意识 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | 广告检索全链路 + DAG 引擎 + 两段召回 | 10 | ⭐⭐⭐⭐ |
| 2 | 项目深挖 | 倒排截断 + bitmap 过滤 + 性能→收入逻辑 | 10 | ⭐⭐⭐⭐⭐ |
| 3 | 项目深挖 | Dumper 离线框架设计 | 10 | ⭐⭐⭐ |
| 4 | 项目深挖 | Aviator 决策树 + 热更新 + 性能优化 | 10 | ⭐⭐⭐⭐ |
| 5 | 项目深挖 | 蚂蚁九州：状态机 + BPM + 错误码 + 宕机迁移 | 10 | ⭐⭐⭐⭐ |
| 6 | 架构 | 广告漏斗优化 + 工程算法协作 | 10 | ⭐⭐⭐⭐ |
| 7 | 系统设计 | 通用 DAG 编排引擎 | 10 | ⭐⭐⭐⭐ |
| 8 | AI 工程 | LangGraph + RAG + 流式一致性 | 10 | ⭐⭐⭐ |
| 9 | 方法论 | 性能优化方法论 + P99 排查 + 团队建设 | 10 | ⭐⭐⭐⭐ |
| 10 | 稳定性 | 大促保障全流程 | 10 | ⭐⭐⭐ |
| 11 | Leader | 团队管理 + 跨团队协作 | 10 | ⭐⭐⭐ |

---

## 建议面试流程（60 分钟）

| 阶段 | 时间 | 内容 |
|------|------|------|
| 小红书项目深挖 | 20 分钟 | 题目 1 + 2（**必问**，最核心的技术项目） |
| 架构/设计 | 15 分钟 | 题目 4（规则引擎）或 题目 7（DAG 设计） |
| AI 工程化 | 10 分钟 | 题目 8（如果岗位需要 AI 能力则必问，否则可跳过） |
| 方法论/Leader | 10 分钟 | 题目 9 或 11 选一 |
| 候选人提问 | 5 分钟 | |

---

## 综合评价维度

| 维度 | 权重 | 说明 |
|------|------|------|
| **系统架构能力** | 30% | 大规模分布式系统的设计与演进能力（检索引擎、规则引擎、平台型系统） |
| **工程深度** | 25% | 性能优化、索引优化、离线框架等硬核工程能力 |
| **技术广度** | 15% | Java + Python + AI Agent + 广告 + 营销 + DevOps 的跨领域经验 |
| **Leader 能力** | 15% | 团队管理、跨团队协作、技术决策和推动力 |
| **业务理解** | 10% | 对广告系统商业逻辑（收入、ROI、漏斗）的理解深度 |
| **学习与成长** | 5% | 从蚂蚁→小红书→BingX→创业的成长轨迹，以及 AI 领域的探索 |

---

## 面试官特别关注点

1. **小红书检索引擎是含金量最高的项目** —— 搜索广告检索引擎是广告系统的核心，物料量级 10 倍增长+收入 3 倍增长是硬指标。重点考察候选人对倒排索引、bitmap、DAG 引擎等核心技术的理解深度，以及"性能优化如何驱动收入增长"的商业逻辑。

2. **跨公司经验的一致性** —— 蚂蚁（平台型系统/状态机）→ 小红书（广告检索/高性能）→ BingX（营销规则引擎）→ 创业（AI Agent），技术栈跨度大。需要验证每段经历的深度是否足够，还是"都浅尝辄止"。

3. **8 年经验的深度期望** —— 对于 8 年经验的候选人，不应只停留在"做了什么"，更应考察"为什么这么做""还有什么更好的方案""如果重来会怎么改"等深层思考。第三层（Why）和第五层（Redo）的追问是区分资深和普通的关键。

4. **AI Agent 项目的定位** —— 个人创业项目（几十个 star），技术含量属于 AI 工程实践而非前沿研究。如果岗位需要 AI 能力可以深挖，否则作为加分项即可，不应成为考察重点。
