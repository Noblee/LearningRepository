# 面试题目集（候选人四）— 基于简历定制

> **候选人核心画像：** Java 后端研发，2.5 年经验，阿里 1688 交易域-买家保障方向。核心经历：分库分表改造（15 张表，含亿级大表）、从 0 到 1 智能赔付系统、系统稳定性/性能升级（RT 降低 40%+）。技术栈以 Spring + MyBatis + MySQL + RocketMQ + TDDL 为主。

---

## 一、项目深挖类

### 题目 1：分库分表——方案设计

**问题：**
你独立设计并完成了保障域 15 张表的分库分表改造，这是简历中非常核心的项目：
1. 你说"结合业务属性，为每一张表定制最合适的改造迁移方案"——能否举 2-3 张有代表性的表，说说它们的分片键（sharding key）是怎么选的？为什么不同表的分片键不一样？
2. "水平切分 + 分库又分表"——分了多少个库？每个库多少张表？这个数字是怎么算出来的？
3. 你提到使用 TDDL Sequence 生成分布式唯一 ID，并用"抬升 ID"区分新旧 ID——请详细解释这个设计。为什么不用雪花算法（Snowflake）？

**参考答案：**

1. **分片键选择：**
   - **订单保障表：** 分片键选 `buyer_id`（买家 ID），因为买家保障的核心查询场景是"查某个买家的所有保障记录"，按 buyer_id 分片可以保证单用户查询落在单库单表，避免跨库。
   - **赔付流水表：** 分片键选 `order_id`（订单 ID），因为赔付流水的核心查询是"查某笔订单的赔付记录"，且赔付流水跟订单一一关联。
   - **服务配置表（商家维度）：** 分片键选 `seller_id`（卖家 ID），因为服务配置是按商家维度管理的。
   - 不同表分片键不同的原因：**分片键必须是该表最高频查询条件中的字段**，不同表的业务查询模式不同，自然选择不同的分片键。如果所有表都用同一个分片键，会导致部分表的高频查询无法命中分片键，产生全表扫描（广播查询）。

2. **库表数量计算：**
   - 评估维度：当前数据量 + 年增长率 → 预估 5 年后的总数据量。
   - 目标：每张表不超过 500 万行。
   - 计算示例：某表当前 1 亿行，年增长 2000 万 → 5 年后约 2 亿 → 分 64 张表（2 亿 / 500 万 = 40，取 2 的幂次 = 64）。
   - 分库数：取决于数据库连接数和写入 QPS 的承载能力。通常 4-16 个库。
   - 分表数：64 或 128（2 的幂次，方便哈希取模）。
   - 实际方案可能是 4 库 × 16 表 = 64 张表，或 8 库 × 8 表 = 64 张表。

3. **TDDL Sequence 与抬升 ID：**
   - TDDL Sequence：阿里中间件提供的分布式 ID 生成器，基于数据库号段（Segment）模式——每次从 DB 申请一段连续 ID（如 1-1000），本地分配用完再申请下一段。
   - **抬升 ID 设计：** 新生成的 ID 从一个较大的起始值开始（如 10 亿），而旧数据的 ID 都小于这个值。这样通过 ID 的大小就能快速区分新旧数据，便于：
     - 数据迁移时区分：旧数据需要迁移到新库，新数据直接写新库。
     - 路由判断：ID < 阈值走旧库，ID ≥ 阈值走新库（双写期间）。
     - 回滚安全：如果需要回滚，新 ID 的数据可以直接清理，不影响旧数据。
   - 为什么不用 Snowflake：(a) TDDL Sequence 是阿里内部标准方案，与 TDDL 分库分表中间件深度集成，使用更方便；(b) Snowflake 依赖时钟，有时钟回拨风险；(c) Snowflake 生成的 ID 是 64 位且非连续递增，抬升 ID 的区分新旧能力不如号段模式直观。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 分片键选择 | 4 分 | 能结合具体表的查询场景说明选择理由，理解"不同表不同分片键"的必要性 |
| 库表数量 | 3 分 | 有量化计算过程，考虑了未来增长 |
| ID 方案 | 3 分 | 抬升 ID 的设计意图和工程价值解释清晰 |

---

### 题目 2：分库分表——不带分片键的查询与数据迁移

**问题：**
1. 你提到通过"拼接分表键 / 外部查询 / 冗余表"来解决不带分片键的查询，请分别解释这三种方式，各自适用什么场景？
2. "不停机迁移"的具体方案是什么？如何保证迁移过程中数据不丢不重？如何验证迁移后数据的正确性？
3. 迁移完成后，旧库的数据如何处理？如何做切流？回滚方案是什么？

**参考答案：**

1. **三种方式解决无分片键查询：**

   | 方式 | 说明 | 适用场景 |
   |------|------|---------|
   | **拼接分表键** | 在业务标识（如订单号）中嵌入分片键信息。例如订单号的后 4 位编码 buyer_id 的哈希值，查询时从订单号中提取分片键，直接路由到目标库表。 | 订单号等业务 ID 查询，可以在 ID 生成时嵌入分片信息 |
   | **外部索引查询** | 用一张索引映射表（或 ES / Redis）存储非分片键到分片键的映射关系。查询时先查映射得到分片键，再路由查询。 | 低频的运营后台查询（如按手机号查用户保障记录） |
   | **冗余表** | 将同一份数据按不同分片键冗余存储一份。例如主表按 buyer_id 分片，冗余表按 seller_id 分片，满足卖家视角的查询。 | 两个维度的查询都是高频的核心场景（如买家查 + 卖家查） |

   **选择原则：** 高频查询用冗余表（空间换时间）；中频查询用拼接分表键（设计时预埋）；低频查询用外部索引（不影响主链路）。

2. **不停机迁移方案（双写 + 增量追平）：**

   ```
   阶段一：全量同步
   精卫（数据同步中间件）读取旧库全量数据 → 按分片规则写入新库

   阶段二：增量同步（双写）
   精卫监听旧库 binlog → 实时同步增量到新库
   （此时写操作仍走旧库，新库通过 binlog 追平）

   阶段三：数据校验
   全量对账：对比新旧库的数据条数和内容（抽样 or 全量 Diff）

   阶段四：切流
   写切新库（TDDL 路由规则切换），旧库变为只读
   观察一段时间确认无问题

   阶段五：下线旧库
   ```

   - 不丢不重的保证：
     - 精卫基于 binlog 做增量同步，binlog 本身保证了变更的完整性和顺序性。
     - 写入新库时使用 `INSERT ON DUPLICATE KEY UPDATE` 或幂等写入，防止重复。
     - 全量 + 增量校验：全量同步完成后启动增量同步，最终通过对账确认数据一致。

3. **旧库处理与回滚：**
   - 切流后旧库保留一段时间（如 1-2 周），作为回滚的兜底。
   - 回滚方案：切流通过 TDDL 路由配置实现，如果新库出现问题，秒级切回旧库。
   - 切回条件：新库出现数据异常/性能问题/慢 SQL 等。
   - 旧库数据清理：确认新库稳定运行 N 天后，逐步清理旧库数据或下线旧库实例。
   - 旧库的增量数据（切流后旧库不再有新写入，但如果切回则需要追平切流期间新库产生的增量到旧库——需要反向同步能力或业务上可以接受丢失这段增量）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 三种方式 | 3 分 | 解释清晰，适用场景判断正确 |
| 迁移方案 | 4 分 | 各阶段流程完整，精卫/binlog 同步机制理解正确 |
| 切流与回滚 | 3 分 | 有回滚预案，理解切流的配置化和可逆性 |

---

### 题目 3：智能赔付系统设计

**问题：**
你从 0 到 1 搭建了智能赔付系统，效果显著（复购率提升 20-40pt）：
1. 请画出整个智能赔付系统的架构图，从事件触发到赔付完成的完整链路。
2. 你提到"使用配置化能力 + 责任链模式封装业务逻辑校验功能"——责任链中有哪些校验节点？为什么选择责任链模式？
3. "延迟消息投递补偿校验消息"——这里的延迟消息具体解决什么问题？延迟多久？为什么需要延迟？
4. 赔付金额是算法计算的，你怎么保证不会赔多？有哪些风控措施？

**参考答案：**

1. **系统架构：**

   ```
   各场域行为消息（物流停滞/差评/辱骂/品质问题...）
          ↓ RocketMQ
   ┌──────────────────────────────┐
   │      智能赔付服务              │
   │  ┌─────────────────────────┐ │
   │  │ 消息消费 & 事件路由       │ │
   │  └──────────┬──────────────┘ │
   │             ↓                 │
   │  ┌─────────────────────────┐ │
   │  │ 责任链校验引擎（配置化）   │ │
   │  │ 用户资格→场景判定→频次    │ │
   │  │ →风控→金额计算→...       │ │
   │  └──────────┬──────────────┘ │
   │             ↓                 │
   │  ┌─────────────────────────┐ │
   │  │ 金融算法服务（赔付金额）   │ │
   │  └──────────┬──────────────┘ │
   │             ↓                 │
   │  ┌─────────────────────────┐ │
   │  │ 赔付执行 & 状态管理       │ │
   │  └──────────┬──────────────┘ │
   │             ↓                 │
   │  延迟消息 → 补偿校验           │
   └──────────────────────────────┘
          ↓ MQ 通知
   金融风控（累计赔付金额）+ 买家触达（App 推送/消息）
   ```

2. **责任链校验：**
   - 校验节点（示例）：
     - 用户资格校验：是否是正常用户（非黑名单、非恶意套利用户）
     - 场景判定：事件是否属于已配置的赔付场景（物流停滞、差评等）
     - 时效校验：事件是否在有效时间窗口内（如差评发生在收货后 N 天内）
     - 频次校验：同一用户近 N 天内赔付次数是否超限
     - 金额上限校验：单笔/累计赔付金额是否超过阈值
     - 风控校验：调用金融风控服务判断是否存在薅羊毛风险
   - 选择责任链模式的原因：
     - **解耦：** 每个校验节点独立，增删改某个校验不影响其他。
     - **配置化：** 不同赔付场景可以配置不同的校验链组合（如物流停滞场景不需要品质校验）。
     - **短路：** 前置节点不通过则后续节点不执行，节省性能。
     - **可扩展：** 新增场景只需新增校验节点并配置到链中。

3. **延迟消息的作用：**
   - 解决的问题：行为事件发生时，部分判定条件可能尚未满足。例如"物流停滞"需要停滞超过 48 小时才确认为赔付条件，收到物流异常事件时只是停滞了几小时，不能立即赔付。
   - 方案：收到事件后发送延迟消息（如延迟 24-48 小时），延迟消息到达后重新校验条件（如物流是否仍然停滞、用户是否已自行解决等）。
   - 为什么需要延迟：(a) 给问题一个自然解决的窗口（物流可能恢复、商家可能主动补偿）；(b) 避免误赔（瞬时异常不代表真正的体验问题）；(c) 降低赔付成本。
   - 延迟时长：根据场景配置化，物流停滞可能 24-48 小时，差评可能 2-6 小时。

4. **风控措施：**
   - **算法侧：** 算法模型输出赔付金额时有上下限约束（如不超过订单金额的 N%）。
   - **系统侧：**
     - 单笔上限：每笔赔付不超过 X 元（硬编码兜底）。
     - 用户级累计上限：同一用户 30 天内累计赔付不超过 Y 元。
     - 全局预算池：每日/每月赔付总额有预算上限，触达后暂停赔付。
     - 实时风控：调用金融风控能力识别异常用户（如短期大量触发赔付事件的用户）。
   - **事后：** 赔付成功后通知金融风控累计金额，定期对账，异常赔付人工复核。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构完整 | 3 分 | 从事件消费到赔付执行再到后续通知的全链路清晰 |
| 责任链设计 | 3 分 | 节点设计合理，能解释为什么用责任链（配置化+短路） |
| 延迟消息 | 2 分 | 延迟的业务意义解释清楚（等待自然解决窗口） |
| 风控措施 | 2 分 | 多层防护（算法+系统+事后），有预算池概念 |

---

### 题目 4：稳定性升级——链路优化与打标补偿

**问题：**
你把商品详情 RT 从 80ms 优化到 44ms，下单 RT 从 150ms 优化到 90ms：
1. "串行调用链路进行并发改造"——具体是哪些调用改为并发？在 Java 中你用什么方式实现并发调用？有哪些需要注意的点？
2. "合并两次 RPC 请求为一次"——这个改造的前提条件是什么？是否所有场景都适合合并？
3. "打标补偿逻辑"——请详细描述这个设计。强依赖改弱依赖的具体含义是什么？补偿的时效性如何保证？
4. 定时任务从 2 天优化到 2 小时，你具体做了哪些优化？

**参考答案：**

1. **并发改造：**
   - 原链路（串行）：查商品信息 → 查物流收货地址 → 查预计送达时效 → 查风控，总耗时 = 四者之和。
   - 改造后（并发）：四个查询无数据依赖，可以并发执行，总耗时 = max(四者)。
   - Java 实现方式：
     - `CompletableFuture.supplyAsync()` + `CompletableFuture.allOf()` 等待全部完成。
     - 或使用 `ExecutorService` + `Future` / `CountDownLatch`。
     ```java
     CompletableFuture<Product> productFuture = CompletableFuture.supplyAsync(() -> getProduct(id), executor);
     CompletableFuture<Address> addressFuture = CompletableFuture.supplyAsync(() -> getAddress(id), executor);
     CompletableFuture<Delivery> deliveryFuture = CompletableFuture.supplyAsync(() -> getDelivery(id), executor);
     CompletableFuture.allOf(productFuture, addressFuture, deliveryFuture).join();
     ```
   - 注意点：
     - **线程池管理：** 必须使用自定义线程池，不能用 ForkJoinPool.commonPool()（会与其他框架共享，导致线程饥饿）。
     - **超时控制：** `get(timeout, TimeUnit)` 设置超时，避免某个慢调用拖垮整体。
     - **异常处理：** 某个并发调用失败时，是否需要降级返回（如风控查询失败是否继续下单）。
     - **上下文传递：** RPC 调用需要传递 traceId、userId 等上下文，异步线程需要手动传递（MDC/ThreadLocal 拷贝）。

2. **RPC 合并的前提：**
   - 被合并的两次 RPC 调用是**同一下游服务**的两个接口。
   - 下游服务需要提供合并后的新接口（一次请求返回两种数据）。
   - 需要下游配合改造（提供聚合接口）。
   - **不适合合并的场景：** (a) 两次调用是不同下游服务；(b) 第二次调用依赖第一次的结果（有数据依赖）；(c) 合并后接口过于臃肿，违反单一职责。

3. **打标补偿设计：**
   - **原链路（强依赖）：** 下单时同步调用保障系统打标（标记该订单享受什么保障服务）。如果保障系统异常，打标失败 → 下单失败 → 影响下单成功率。
   - **改造后（弱依赖 + 补偿）：**
     - 下单时仍然调用保障系统打标，但**超时或失败时不阻塞下单**（降级处理，返回默认值或空标）。
     - 下单成功后，通过监听**订单消息**（如订单创建消息），异步触发补标逻辑。
     - 补标逻辑：消费订单消息 → 检查该订单是否已正确打标 → 未打标则调用保障系统补标。
   - 时效性保证：订单消息通常在秒级内可达（MQ 延迟），补标在下单后几秒内完成。对用户无感知（用户在下单瞬间看不到保障标也无妨，进入订单详情时标已补上）。
   - 兜底：如果消息消费也失败，通过定时任务扫描近 N 分钟内未打标的订单做批量补标。

4. **定时任务优化（2 天 → 2 小时）：**
   - **添加索引：** 扫描条件涉及的字段未加索引，导致全表扫描 → 加索引后走索引扫描。
   - **深度分页优化：** `LIMIT 100000, 1000` 这种深度分页会扫描大量行。改为基于 ID 的游标分页：`WHERE id > last_max_id LIMIT 1000`。
   - **大表历史数据迁移：** 亿级表中大量历史数据已无需定时任务处理，迁移到归档表后扫描范围大幅缩小。
   - **单个查改批量查：** 循环中逐条查询下游数据 → 改为批量查询（IN 查询或批量 RPC 接口）。
   - **单机改多机：** 将任务按分片键拆分为多个子任务，分配到多台机器并行执行。例如按 buyer_id 取模分 16 片，16 台机器各处理一片。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 并发改造 | 3 分 | 实现方式正确，注意到线程池/超时/上下文传递等问题 |
| RPC 合并 | 2 分 | 理解前提条件和局限性 |
| 打标补偿 | 3 分 | 强→弱依赖的转变清晰，有消息补标+定时兜底的双重保障 |
| 定时任务 | 2 分 | 优化手段具体，每项都能说出原理 |

---

## 二、Java 基础与并发

### 题目 5：Java 并发编程

**问题：**
1. `CompletableFuture` 和 `Future` 的区别是什么？你在项目中用 `CompletableFuture` 做并发调用时，异常处理是怎么做的？
2. 线程池的核心参数有哪些？如何合理配置？你在项目中的线程池是怎么配置的？
3. `synchronized` 和 `ReentrantLock` 的区别？什么场景下必须用 `ReentrantLock`？
4. `ThreadLocal` 在你的项目中是怎么使用的？有什么需要注意的坑？

**参考答案：**

1. **CompletableFuture vs Future：**
   - `Future`：只能阻塞式地 `get()` 获取结果，无法组合、无法链式处理，异常处理不灵活。
   - `CompletableFuture`：支持链式编程（`thenApply`/`thenAccept`/`thenCompose`），支持组合多个异步任务（`allOf`/`anyOf`），支持异常处理（`exceptionally`/`handle`），可以手动完成。
   - 异常处理实践：
     ```java
     CompletableFuture<Product> future = CompletableFuture
         .supplyAsync(() -> queryProduct(id), executor)
         .exceptionally(ex -> {
             log.error("查询商品异常, id={}", id, ex);
             return defaultProduct; // 降级返回默认值
         });
     ```
   - 注意：`allOf().join()` 时如果某个 future 异常，需要逐个检查每个 future 的结果。

2. **线程池核心参数：**
   - `corePoolSize`（核心线程数）、`maximumPoolSize`（最大线程数）、`keepAliveTime`（非核心线程空闲存活时间）、`workQueue`（任务队列）、`threadFactory`（线程工厂）、`handler`（拒绝策略）。
   - 配置原则：
     - IO 密集型任务（如 RPC 调用）：核心线程数 = CPU 核数 × 2 ~ CPU 核数 × 4。
     - CPU 密集型任务：核心线程数 ≈ CPU 核数 + 1。
     - 队列选择：有界队列（`LinkedBlockingQueue` + 合理容量），避免 OOM。
     - 拒绝策略：`CallerRunsPolicy`（让调用者线程执行，起到限流效果）或自定义策略（记录日志+告警）。
   - 项目中：为 RPC 并发调用单独创建线程池（如 core=16, max=32, queue=256），与框架默认线程池隔离，避免互相影响。

3. **synchronized vs ReentrantLock：**
   - `synchronized`：JVM 内置锁，自动加锁释放，简单但功能有限。
   - `ReentrantLock`：显式加锁释放（需 try-finally），支持：(a) 可中断获取锁（`lockInterruptibly`）；(b) 超时获取锁（`tryLock(timeout)`）；(c) 公平锁；(d) 多个 Condition。
   - 必须用 ReentrantLock 的场景：需要超时获取锁（避免死锁），或需要公平锁，或需要多条件等待。

4. **ThreadLocal 使用与坑：**
   - 使用场景：存储请求级上下文（如 userId、traceId），避免在方法参数中层层传递。
   - 坑：**线程池复用场景下的内存泄漏和数据串用**——线程池中的线程不会销毁，ThreadLocal 不会自动清理，下一个请求可能读到上一个请求的数据。
   - 解决：在请求结束时（如 Filter/Interceptor 的 finally 块）显式调用 `ThreadLocal.remove()`。
   - 异步场景：`CompletableFuture` 在其他线程执行时无法自动继承 ThreadLocal，需要手动拷贝（如阿里的 `TransmittableThreadLocal`）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| CompletableFuture | 3 分 | 与 Future 区别清晰，异常处理有实际代码经验 |
| 线程池配置 | 3 分 | 参数含义正确，配置有原则和业务依据 |
| 锁机制 | 2 分 | 区别准确，能说出必须用 ReentrantLock 的场景 |
| ThreadLocal | 2 分 | 了解线程池下的泄漏问题和 TTL 方案 |

---

### 题目 6：JVM 与性能调优

**问题：**
1. 你的保障域服务 JVM 堆内存设置了多大？GC 策略用的什么？如何选择的？
2. 如果线上服务出现 Full GC 频繁（如每分钟一次），你的排查思路是什么？
3. 你在做链路优化时，是如何定位性能瓶颈的？用了哪些工具或方法？

**参考答案：**

1. **JVM 配置：**
   - 堆内存：根据服务的 QPS、对象创建速率和存活对象大小评估。通常 4G-8G（阿里内部服务常见配置）。
   - GC 策略：阿里内部常用 G1 或 CMS。
     - G1：适合大堆（> 4G），停顿时间可控（`-XX:MaxGCPauseMillis`），是 JDK 9+ 默认 GC。
     - CMS：适合对延迟敏感的场景，但有内存碎片和 Concurrent Mode Failure 问题。
   - 选择依据：服务对 RT 敏感（保障下单链路）→ 选择 G1（停顿可控）或 ZGC（更低停顿，JDK 11+）。

2. **Full GC 频繁排查：**
   - 看 GC 日志：确认 Full GC 的原因（Old 区满？Metaspace 满？System.gc() 调用？）。
   - 老年代增长分析：如果是 Old 区不断增长 → 可能有内存泄漏或大对象频繁进入老年代。
   - Heap Dump 分析：`jmap -dump:format=b,file=heap.hprof <pid>` → MAT（Memory Analyzer Tool）分析大对象和 GC Root 引用链。
   - 常见原因：(a) 大 List/Map 未及时释放；(b) 缓存无限增长（没有淘汰策略）；(c) 数据库查询返回大量数据全加载到内存；(d) ThreadLocal 泄漏。
   - 临时处理：调大堆内存（治标）；根据 dump 分析修复代码（治本）。

3. **性能瓶颈定位工具：**
   - **链路追踪：** 阿里内部的鹰眼（类似 Jaeger），看每个 RPC 调用的耗时分布。
   - **火焰图：** Arthas 的 `profiler start/stop` 生成 CPU 火焰图，定位热点方法。
   - **Arthas：** `trace` 命令追踪方法内部各步骤耗时；`watch` 命令观察方法参数和返回值。
   - **慢 SQL 日志：** 数据库慢查询日志 + EXPLAIN 分析执行计划。
   - **监控大盘：** QPS、RT、错误率、CPU、内存、线程数等指标趋势。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| JVM 配置 | 3 分 | 参数合理，选型有依据 |
| Full GC 排查 | 4 分 | 思路清晰，知道 Heap Dump + MAT 分析 |
| 性能定位 | 3 分 | 工具链丰富（链路追踪 + Arthas + 火焰图） |

---

## 三、MySQL 深入

### 题目 7：MySQL 索引与慢 SQL 优化

**问题：**
你的项目成果是"日均慢 SQL 从 200+ 降到 0"，请深入回答：
1. 慢 SQL 的主要类型有哪些？在你的项目中，导致慢 SQL 的前三大原因分别是什么？
2. 一个 `WHERE a = 1 AND b > 10 ORDER BY c` 的查询，索引应该怎么建？为什么？
3. 分库分表后，`COUNT`、`ORDER BY`、`JOIN` 这些操作会遇到什么问题？你是怎么解决的？
4. TDDL 中间件的路由原理你了解吗？一条 SQL 是如何被路由到正确的库和表的？

**参考答案：**

1. **慢 SQL 类型与原因：**
   - 类型：全表扫描、索引失效、深度分页、大表 JOIN、锁等待。
   - 项目中前三大原因：
     - **缺少索引 / 索引失效：** 单表数据量过亿，查询条件未建索引，导致全表扫描。或索引存在但因隐式类型转换（如 varchar 字段用 int 查）、函数包裹（如 `WHERE DATE(create_time) = '2024-01-01'`）而失效。
     - **深度分页：** 定时任务使用 `LIMIT offset, size` 遍历大表，offset 越大越慢（MySQL 需要扫描 offset + size 行）。
     - **大表无归档：** 亿级表中 90% 是历史数据，但查询和扫描仍然覆盖全量数据。

2. **索引设计：**
   - `WHERE a = 1 AND b > 10 ORDER BY c`
   - 最优索引：`(a, b, c)`
     - `a = 1`：等值查询，索引第一列完全命中。
     - `b > 10`：范围查询，索引第二列命中。但范围查询之后的列（c）**无法用于索引排序**。
   - 所以实际效果：索引用于 `a = 1 AND b > 10` 的过滤，但 `ORDER BY c` 仍需 filesort。
   - 替代方案：如果排序性能更关键，可以建 `(a, c, b)` —— `a` 等值匹配后，`c` 可用于索引排序（避免 filesort），`b > 10` 的过滤在 Using Index Condition（ICP）中完成。
   - **最终选择取决于数据分布和业务优先级**（过滤优先还是排序优先）。

3. **分库分表后的聚合问题：**
   - **COUNT：** 需要在所有分片上执行 COUNT 再汇总。TDDL 会自动将 `SELECT COUNT(*)` 路由到所有分片并汇总结果。如果频繁需要全局 COUNT，可以维护一个异步更新的计数表。
   - **ORDER BY + LIMIT：** 需要每个分片都返回 TopN，中间件再做归并排序取全局 TopN。数据量大时内存开销高。优化：尽量带上分片键减少涉及的分片数。
   - **JOIN：** 跨分片 JOIN 非常昂贵（理论上需要笛卡尔积）。解决方案：(a) 尽量避免跨分片 JOIN，在应用层做数据组装；(b) 如果必须关联，将关联表用相同的分片键分片，保证关联数据在同一分片；(c) 小表广播（每个分片都存一份小表数据）。

4. **TDDL 路由原理：**
   - SQL 发送到 TDDL → SQL 解析提取分片键的值 → 执行分片规则（如 `buyer_id % 64`）计算目标库和表编号 → 改写 SQL（替换逻辑表名为物理表名）→ 路由到目标数据源执行。
   - 如果 SQL 不含分片键：TDDL 会将 SQL 广播到所有分片执行，再汇总结果（性能差，应尽量避免）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 慢 SQL 原因 | 3 分 | 有实际项目经验，不仅是理论 |
| 索引设计 | 3 分 | 能分析范围查询对后续列排序的影响，给出替代方案 |
| 分库分表聚合 | 2 分 | 了解 COUNT/ORDER BY/JOIN 的问题和解决方案 |
| TDDL 路由 | 2 分 | 理解 SQL 解析 → 分片计算 → 改写 → 路由的流程 |

---

## 四、消息队列与设计模式

### 题目 8：RocketMQ 在智能赔付中的应用

**问题：**
1. 智能赔付系统中 RocketMQ 的消费模式是集群消费还是广播消费？为什么？
2. 多个场域（物流、评价、对话、品质）的行为消息，你是用一个 Topic 还是多个 Topic？Consumer Group 如何设计？
3. 延迟消息到达后，发现场景已经解决了（如物流恢复了），如何处理？会不会造成重复赔付？
4. 如果消息消费失败了，重试策略是怎样的？重试多少次？最终还是失败怎么办？

**参考答案：**

1. **消费模式：**
   - **集群消费（Clustering）：** 一条消息只被同一 Consumer Group 中的一个消费者消费。
   - 选择集群消费，因为赔付操作需要且仅需要执行一次。如果用广播消费，每个消费者实例都会执行一次，导致重复赔付。

2. **Topic 与 Consumer Group：**
   - **多个 Topic**（推荐）：每个场域一个 Topic（如 `topic_logistics_event`、`topic_review_event`），清晰解耦。
   - 或 **一个 Topic + Tag 区分**：所有场域事件发到同一 Topic，用 Tag 区分场景类型。适合场域较少且处理逻辑相似的情况。
   - Consumer Group：一个赔付服务对应一个 Consumer Group，订阅所有相关 Topic。如果不同场域的处理逻辑差异大，可以拆分为多个 Consumer Group。

3. **延迟消息到达后的处理：**
   - 延迟消息到达后会**重新执行完整的责任链校验**。如果场景已解决（如物流恢复），在场景判定校验节点就会不通过，直接终止，不会赔付。
   - 防重复赔付：(a) 幂等 key（如 `order_id + scene_type + event_id`），同一事件只赔付一次；(b) 赔付前查询赔付记录表，已赔付则跳过。

4. **消费失败重试：**
   - RocketMQ 默认重试 16 次（延迟递增：10s, 30s, 1m, 2m, 3m, 4m, ...）。
   - 可自定义重试次数（如 3-5 次）。
   - 最终失败：消息进入**死信队列（DLQ）**，由监控系统告警，人工排查处理。
   - 死信消息处理：定期消费死信队列，分析失败原因（下游服务不可用？数据异常？），修复后重新投递。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 消费模式 | 2 分 | 选集群消费并能说清原因 |
| Topic 设计 | 3 分 | 方案合理，能对比 Topic vs Tag 的取舍 |
| 幂等与防重 | 3 分 | 延迟消息重新校验 + 幂等 key 双重保障 |
| 重试与死信 | 2 分 | 了解重试机制和死信队列的处理 |

---

## 五、系统设计

### 题目 9：设计一个可配置的服务保障平台

**问题：**
你提到"为多个业务方提供可配置的服务保障个性化能力"。现在请你设计一个通用的服务保障平台，要求：
- 支持多种保障类型（晚发赔、晚揽赔、晚到赔、品质保障等）
- 保障规则可配置（触发条件、赔付标准、适用范围）
- 支持新业务方快速接入
- 高并发下稳定运行（商品详情和下单链路均需调用）

请从数据模型、核心流程、配置化设计、性能保障等方面展开。

**参考答案要点：**

1. **数据模型：**
   - 保障类型表（guarantee_type）：类型 ID、名称、描述、状态。
   - 保障规则表（guarantee_rule）：规则 ID、类型 ID、触发条件（JSON DSL / 规则表达式）、赔付标准（固定金额 / 比例 / 阶梯）、适用范围（类目/商家等级/区域）、优先级、生效时间。
   - 商家服务开通表（seller_service）：商家 ID、保障类型 ID、开通状态、配置覆盖（可覆盖默认规则）。
   - 订单保障记录表（order_guarantee）：订单 ID、保障类型 ID、状态（生效/触发/赔付/失效）。

2. **核心流程：**
   - **商品详情/下单打标：** 查询商家开通了哪些保障服务 → 根据商品属性匹配适用的保障规则 → 打标（标记该商品/订单享受哪些保障）。
   - **保障触发：** 监听物流/交易事件 → 匹配触发条件（如发货超时 N 小时）→ 执行赔付流程。

3. **配置化设计：**
   - 规则引擎：将触发条件抽象为可配置的规则表达式（如 `ship_delay_hours > 48 AND category IN ('服装', '鞋靴')`）。
   - 新业务方接入：只需配置新的保障类型 + 规则，无需开发代码。
   - 灰度能力：规则支持按比例/白名单灰度生效。

4. **性能保障：**
   - 商家服务开通信息和规则配置**缓存到 Redis**（候选人简历中提到了缓存优化）。
   - 缓存更新：配置变更时通过 MQ 通知各节点刷新本地缓存。
   - 打标接口 RT 要求 < 50ms，读缓存路径要做到 < 10ms。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 数据模型 | 3 分 | 实体关系清晰，规则表设计灵活 |
| 流程完整 | 3 分 | 打标 + 触发 + 赔付全流程 |
| 配置化 | 2 分 | 有规则引擎思路，新接入无需开发 |
| 性能设计 | 2 分 | 缓存方案合理，RT 可控 |

---

## 六、稳定性与线上问题

### 题目 10：线上故障排查

**问题：**
场景一：周一早上发现保障域的某个定时任务在周末执行了两次（本应只执行一次），导致部分用户收到了重复的赔付。你如何排查和处理？

场景二：下单链路中保障打标接口突然 RT 飙升到 500ms（正常 40ms），导致下单整体变慢。紧急排查步骤是什么？

**参考答案：**

**场景一：定时任务重复执行**

1. **确认影响面：** 重复赔付了多少用户？金额多少？
2. **排查原因：**
   - 检查定时任务调度日志：是否被触发了两次？
   - 可能原因：(a) 调度平台误触发（如手动触发 + 定时触发同时执行）；(b) 任务执行超时后调度平台重试了一次；(c) 多机部署时缺少分布式锁，多台机器同时执行；(d) 代码中有重复消费逻辑。
3. **止血：** 暂停该定时任务。
4. **修复数据：** 通过对比执行日志和赔付记录，找出重复赔付的用户，发起资金回收或标记为已知异常。
5. **根因修复：** (a) 加分布式锁保证同一时间只有一个实例执行；(b) 赔付操作增加幂等校验（基于唯一业务 ID）；(c) 定时任务增加执行结果记录，重复执行时先检查上次结果。

**场景二：打标 RT 飙升**

1. **立即确认：** 是否全量飙升还是部分请求？看监控曲线。
2. **检查近期变更：** 有代码发布或配置变更则优先回滚。
3. **链路追踪：** 查看慢请求的 Trace，定位耗时集中在哪个环节：
   - 缓存层：Redis 是否异常？检查 Redis 响应时间、连接数、是否有大 Key 操作。
   - 数据库层：是否有慢 SQL？是否有锁等待？`SHOW PROCESSLIST` 检查。
   - 下游 RPC：并发调用的某个下游是否变慢？
   - 本地计算：GC 是否异常？线程池是否打满？
4. **临时措施：**
   - 如果是下游慢：触发熔断/降级，打标接口降级返回默认值（宁可漏标也不阻塞下单，后续消息补标）。
   - 如果是 Redis 问题：切换到备用缓存或降级直查 DB（注意限流）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 重复执行排查 | 3 分 | 能识别多种可能原因，修复方案包含幂等和分布式锁 |
| 数据修复 | 2 分 | 有具体的资金回收方案 |
| RT 飙升排查 | 3 分 | 排查思路分层（缓存→DB→RPC→GC），使用链路追踪 |
| 应急降级 | 2 分 | 能利用之前设计的打标补偿机制做降级 |

---

### 题目 11：压力面——分库分表翻车场景

**问题：**
假设你刚完成分库分表切流上线，运行一周后突然收到大量报警：某几个分片的慢 SQL 重新出现，而其他分片正常。你怎么分析这个问题？

**参考答案：**

1. **数据热点/倾斜：**
   - 最可能的原因：分片键选择或哈希算法导致数据分布不均匀。某些分片承载了远超平均水平的数据量或请求量。
   - 排查：查看各分片的数据量分布（`SELECT COUNT(*) FROM table_xxx` 逐个分片统计），确认是否存在严重倾斜。
   - 如果是数据倾斜：分析倾斜原因（如某些大卖家的数据集中在同一分片），考虑调整分片策略或对大卖家做特殊处理。

2. **迁移残留数据：**
   - 旧数据迁移时某些分片的数据未完全清理，导致数据量偏大。
   - 排查：检查这几个分片中是否有大量旧数据（通过 ID 范围或时间范围判断）。

3. **查询未改造完全：**
   - 某些查询仍然没带分片键，触发广播查询，但只有数据量大的分片表现为慢 SQL。
   - 排查：查看慢 SQL 的具体内容，是否缺少分片键条件。

4. **处理方案：**
   - 短期：对热点分片临时扩容（增加读副本或增大分片的 DB 资源配置）。
   - 中期：数据再平衡（re-sharding）或对热点 key 做二级分片。
   - 长期：完善分片键选择的评估机制，上线前做充分的数据分布预演。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 热点分析 | 4 分 | 第一时间想到数据倾斜，知道如何验证 |
| 其他原因 | 3 分 | 能想到残留数据、广播查询等非典型原因 |
| 处理方案 | 3 分 | 短中长期方案层次分明 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | 分库分表方案设计（分片键、库表数、ID 方案） | 10 | ⭐⭐⭐⭐ |
| 2 | 项目深挖 | 分库分表迁移与无分片键查询 | 10 | ⭐⭐⭐⭐ |
| 3 | 项目深挖 | 智能赔付系统架构（责任链+延迟消息+风控） | 10 | ⭐⭐⭐⭐ |
| 4 | 项目深挖 | 稳定性升级（并发改造+打标补偿+定时任务优化） | 10 | ⭐⭐⭐ |
| 5 | Java 基础 | CompletableFuture、线程池、锁、ThreadLocal | 10 | ⭐⭐⭐ |
| 6 | JVM | GC 策略、Full GC 排查、性能工具 | 10 | ⭐⭐⭐ |
| 7 | MySQL | 慢 SQL 优化、索引设计、分库分表聚合 | 10 | ⭐⭐⭐⭐ |
| 8 | MQ | RocketMQ 消费模式、Topic 设计、幂等、死信 | 10 | ⭐⭐⭐ |
| 9 | 系统设计 | 可配置的服务保障平台 | 10 | ⭐⭐⭐⭐ |
| 10 | 故障排查 | 定时任务重复执行 + 打标 RT 飙升 | 10 | ⭐⭐⭐ |
| 11 | 综合压力 | 分库分表上线后热点问题 | 10 | ⭐⭐⭐⭐ |

---

## 建议面试流程（60 分钟）

| 阶段 | 时间 | 内容 |
|------|------|------|
| 分库分表深挖 | 20 分钟 | 题目 1 + 2（**必问**，这是简历最硬的项目） |
| 项目深挖二选一 | 10 分钟 | 题目 3（智能赔付）或 题目 4（稳定性升级） |
| 技术基础 | 15 分钟 | 题目 5（Java 并发，必问）+ 题目 7（MySQL，快速过） |
| 系统设计/故障 | 15 分钟 | 题目 9 或 10 或 11 选一 |

---

## 综合评价维度

| 维度 | 权重 | 说明 |
|------|------|------|
| **分库分表深度** | 25% | 这是候选人最核心的项目，必须深入考察方案设计、迁移细节和踩坑经验 |
| **系统稳定性意识** | 20% | 打标补偿、慢 SQL 优化、监控完善等体现的工程素养 |
| **Java 基础** | 20% | 并发编程、JVM 调优等基本功 |
| **MySQL 功底** | 15% | 索引设计、SQL 优化、分库分表聚合查询等 |
| **业务理解与架构** | 10% | 对买家保障业务的理解，配置化/可扩展性的思考 |
| **沟通表达** | 10% | 技术方案阐述的清晰度和条理性 |

---

## 面试官特别关注点

1. **分库分表是最核心的考察项** —— 这个项目涉及方案设计、ID 策略、无键查询、不停机迁移、切流回滚，每个环节都值得深挖。能否把每个环节说清楚、说到位，是区分"参与者"和"Owner"的关键。

2. **稳定性升级体现了工程成熟度** —— 串行改并发、强依赖改弱依赖+补偿、定时任务优化这些是后端工程师的基本功，但做到什么程度、有什么量化结果（RT 80→44ms）非常重要。

3. **智能赔付体现了从 0 到 1 的能力** —— 系统不复杂但涉及多团队协作（金融/算法/场域），考察候选人在技术之外的协作和推动能力。

4. **候选人工作 2.5 年但项目密度高** —— 分库分表 + 智能赔付 + 稳定性升级三个大项目，说明执行力强。面试时可以适当追问时间管理和优先级决策。
