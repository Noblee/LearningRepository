
![](区间树查询.png)
```
Interval_search(T, i)
    x = T.root
    while x!=T.nil and i 不和 x.int重叠
        if x.left != T.nil x.left.max<i.low
            x = x.right
        else x = x.left
    return x;
```
