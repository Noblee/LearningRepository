# 纯技术面试题库

> 不绑定任何候选人简历，按技术领域分类。每道题都有追问链，从基础到深入层层递进。适用于后端开发岗位（Go/Java）。

---

## 一、MySQL

### 1.1 一条 SQL 的执行全过程

**问题：** 从客户端发出 `SELECT * FROM users WHERE id = 1` 到拿到结果，MySQL 内部经历了哪些步骤？

**参考答案：**

```
客户端 → 连接器（认证、权限校验）
      → 查询缓存（8.0 已移除）
      → 解析器（词法分析、语法分析，生成 AST）
      → 预处理器（表/列是否存在、权限再校验）
      → 优化器（选择索引、确定 JOIN 顺序、生成执行计划）
      → 执行器（调用存储引擎接口读取数据）
      → 存储引擎（InnoDB）：
          → Buffer Pool 查找（命中则直接返回）
          → 未命中则从磁盘加载数据页到 Buffer Pool
          → 返回结果
```

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "优化器是怎么决定用哪个索引的？" | 基于统计信息（cardinality）做成本估算，选成本最低的执行计划 |
| L2 | "优化器选错索引的情况你遇到过吗？怎么处理？" | `FORCE INDEX` 强制指定 / `ANALYZE TABLE` 更新统计信息 / 改写 SQL |
| L3 | "Buffer Pool 的淘汰策略是什么？为什么不用普通 LRU？" | 改良 LRU：young/old 分区，防止全表扫描冲刷热数据 |

---

### 1.2 事务隔离级别与 MVCC

**问题：** MySQL InnoDB 的四种隔离级别分别是什么？默认用哪个？MVCC 是怎么实现可重复读的？

**参考答案：**

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|---------|------|----------|------|
| READ UNCOMMITTED | 可能 | 可能 | 可能 |
| READ COMMITTED | 不可能 | 可能 | 可能 |
| **REPEATABLE READ**（默认） | 不可能 | 不可能 | InnoDB 通过间隙锁解决 |
| SERIALIZABLE | 不可能 | 不可能 | 不可能 |

**MVCC 实现（可重复读）：**
- 每行数据有两个隐藏字段：`trx_id`（最后修改的事务 ID）和 `roll_pointer`（指向 undo log 中的旧版本）。
- 事务开始时生成一个 **Read View**，记录当前活跃事务 ID 列表。
- 读取数据时，沿 undo log 版本链查找**第一个对当前事务可见的版本**：
  - `trx_id < min_trx_id`（已提交）→ 可见
  - `trx_id > max_trx_id`（事务开始后才创建）→ 不可见
  - `trx_id` 在活跃列表中 → 不可见（未提交）
- **可重复读 vs 读已提交的区别：** RR 在事务开始时创建一次 Read View（后续读用同一个）；RC 在每次 SELECT 时都创建新的 Read View。

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "MVCC 能完全解决幻读吗？" | 快照读可以，当前读（`SELECT ... FOR UPDATE`）靠间隙锁（Gap Lock）解决 |
| L2 | "间隙锁会导致什么问题？" | 死锁。两个事务分别锁定相邻间隙，互相等待对方释放 |
| L3 | "你在项目中遇到过死锁吗？怎么排查的？" | `SHOW ENGINE INNODB STATUS` 查看死锁日志、`information_schema.INNODB_LOCKS` |

---

### 1.3 索引底层与实战

**问题：** 为什么 MySQL 索引用 B+ 树而不是 B 树、红黑树或跳表？

**参考答案：**

| 数据结构 | 不选的原因 |
|---------|----------|
| **B 树** | 非叶子节点也存储数据，导致每个节点容纳的 key 数量少 → 树高更高 → 磁盘 IO 次数更多 |
| **红黑树** | 二叉树，树高 = O(log₂N)，千万级数据树高约 23-24 层 → 23 次磁盘 IO。B+ 树 3-4 层即可 |
| **跳表** | 范围查询需要回到底层链表遍历，且层数不稳定，不如 B+ 树的叶子节点双向链表高效 |
| **Hash** | 只能等值查询，不支持范围查询和排序 |

**B+ 树的优势：**
- 非叶子节点只存 key（不存数据），每个节点能容纳更多 key → 树更矮（3-4 层） → 磁盘 IO 少。
- 叶子节点通过双向链表相连 → 范围查询只需找到起点然后沿链表遍历，非常高效。
- 每个节点大小 = 一个磁盘页（16KB），与磁盘 IO 的最小单位对齐。

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "一棵 3 层的 B+ 树能存多少条记录？" | 假设每个页 16KB，主键 bigint 8 字节 + 指针 6 字节，每个非叶子节点约 1170 个 key。3 层 ≈ 1170 × 1170 × 16 ≈ 约 2000 万条 |
| L2 | "什么是索引下推（Index Condition Pushdown）？" | MySQL 5.6+，将 WHERE 条件中涉及索引列的过滤下推到存储引擎层，减少回表次数 |
| L3 | "联合索引 `(a, b)` 对 `ORDER BY a, b DESC` 有用吗？" | MySQL 8.0+ 支持降序索引，8.0 前索引只有升序，`b DESC` 会导致 filesort |

---

### 1.4 锁机制

**问题：** InnoDB 有哪些类型的锁？`SELECT ... FOR UPDATE` 和 `SELECT ... LOCK IN SHARE MODE` 的区别？什么情况下行锁会退化为表锁？

**参考答案：**

**锁类型：**
- **共享锁（S Lock）：** 允许多个事务同时读，阻塞写。`LOCK IN SHARE MODE`。
- **排他锁（X Lock）：** 阻塞其他所有读写。`FOR UPDATE`。
- **意向锁（IS/IX）：** 表级锁，快速判断表中是否有行被锁定，避免逐行检查。
- **记录锁（Record Lock）：** 锁定索引记录。
- **间隙锁（Gap Lock）：** 锁定索引记录之间的间隙，防止幻读。
- **临键锁（Next-Key Lock）：** 记录锁 + 间隙锁，InnoDB 在 RR 级别下的默认加锁方式。

**行锁退化为表锁的场景：**
- **查询未走索引：** `UPDATE users SET name='x' WHERE age = 25`，如果 age 字段没有索引，InnoDB 无法确定锁哪些行，只能锁全表。
- 这不是真的"表锁"，而是对所有记录加了临键锁，效果等同于表锁。

**追问：**
> "一个 UPDATE 语句的加锁过程是怎样的？" → 先在索引上查找匹配记录 → 对匹配的索引记录加 X 锁 → 如果有二级索引也要加锁 → 修改数据 → 事务提交释放锁。

---

## 二、Redis

### 2.1 数据结构底层实现

**问题：** Redis 的 String、List、Hash、Set、ZSet 分别用了什么底层数据结构？Redis 为什么这么快？

**参考答案：**

| 类型 | 底层编码 | 切换条件 |
|------|---------|---------|
| String | int（整数）/ embstr（≤44 字节短字符串）/ raw（长字符串） | 按内容和长度自动切换 |
| List | listpack（小数据量）→ quicklist（大数据量） | 元素数 > 128 或单元素 > 64 字节 |
| Hash | listpack → hashtable | 同上 |
| Set | intset（全整数）→ hashtable | 元素数 > 512 或含非整数 |
| ZSet | listpack → skiplist + hashtable | 元素数 > 128 或单元素 > 64 字节 |

**Redis 快的原因：**
1. **纯内存操作**：数据在内存中，读写都是纳秒级。
2. **单线程模型**：避免了锁竞争和上下文切换（6.0+ 网络 IO 多线程，命令执行仍单线程）。
3. **IO 多路复用**：epoll 高效处理大量连接。
4. **高效数据结构**：SDS、跳表、listpack 等针对性优化。
5. **通信协议简单**：RESP 协议解析开销小。

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "单线程为什么还能扛住十万 QPS？" | 因为瓶颈不在 CPU（命令执行极快），而在网络 IO，epoll 解决了 IO 瓶颈 |
| L2 | "Redis 6.0 的多线程改了什么？" | 网络 IO（读请求/写响应）多线程化，命令执行仍单线程，无需加锁 |
| L3 | "跳表为什么不用红黑树？" | 跳表实现简单、范围查询更高效、并发友好（局部修改 vs 红黑树的旋转），Antirez（Redis 作者）本人也说过实现简单是主要原因 |

---

### 2.2 持久化

**问题：** RDB 和 AOF 的区别？各自的优缺点？你们线上用的什么策略？

**参考答案：**

| 维度 | RDB | AOF |
|------|-----|-----|
| 原理 | 全量快照（fork 子进程，COW） | 追加写命令日志 |
| 触发 | 手动 BGSAVE / 配置自动触发 | 每条命令 / 每秒 fsync / 不主动 fsync |
| 恢复速度 | 快（直接加载二进制） | 慢（重放所有命令） |
| 数据安全 | 可能丢失最近一次快照后的数据 | 最多丢 1 秒数据（everysec 策略） |
| 文件大小 | 小（压缩的二进制） | 大（文本命令，需定期 rewrite） |
| 对性能影响 | fork 时有短暂卡顿（内存大时明显） | 持续的磁盘写入 |

**线上推荐：** AOF（everysec）+ RDB 定期备份。Redis 4.0+ 支持混合持久化（RDB 头 + AOF 尾），兼顾恢复速度和数据安全。

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "fork 的时候如果内存 10GB，子进程也占 10GB 吗？" | 不会，COW（Copy-On-Write），fork 只复制页表，父进程修改的页才会复制。但如果写入频繁，可能接近 2 倍内存 |
| L2 | "AOF rewrite 的过程中有新写入怎么办？" | 新写入同时追加到旧 AOF 和 rewrite 缓冲区，rewrite 完成后将缓冲区内容追加到新 AOF，原子替换文件 |
| L3 | "Redis 宕机重启后数据恢复的完整流程？" | 优先加载 AOF（数据更完整），AOF 不存在则加载 RDB。混合模式下加载混合文件（先 RDB 部分再 AOF 部分） |

---

### 2.3 过期策略与内存淘汰

**问题：** Redis 的 key 过期了是怎么删除的？内存满了怎么办？

**参考答案：**

**过期删除策略（三种配合）：**
1. **惰性删除：** 访问 key 时检查是否过期，过期则删除。优点：CPU 友好。缺点：大量过期 key 没被访问则一直占内存。
2. **定期删除：** 每 100ms 随机抽取一批带过期时间的 key 检查，过期则删除。如果过期比例 > 25%，再抽一批。
3. **定时删除（Redis 不用）：** 每个 key 设一个定时器，到期自动删。CPU 代价太高。

**内存淘汰策略（`maxmemory-policy`）：**

| 策略 | 说明 |
|------|------|
| noeviction | 内存满后拒绝写入（默认） |
| allkeys-lru | 所有 key 中淘汰最近最少使用的 |
| volatile-lru | 有过期时间的 key 中淘汰 LRU |
| allkeys-lfu | 所有 key 中淘汰最不常用的（4.0+） |
| volatile-lfu | 有过期时间的 key 中淘汰 LFU |
| allkeys-random | 随机淘汰 |
| volatile-random | 有过期时间的 key 中随机淘汰 |
| volatile-ttl | 淘汰剩余 TTL 最短的 key |

**追问：** "LRU 和 LFU 怎么选？" → LRU 适合热点有时间局部性的场景（最近访问的还会再访问）；LFU 适合热点稳定的场景（长期高频的 key 不应被偶尔的冷 key 挤掉）。Redis 的 LRU 是近似 LRU（采样），不是精确的。

---

### 2.4 Redis Cluster

**问题：** Redis Cluster 的数据分片原理？一个 key 是怎么找到它所在的节点的？节点宕机了怎么办？

**参考答案：**

- **哈希槽（Hash Slot）：** 共 16384 个槽，均匀分配到各主节点。
- **路由：** `CRC16(key) % 16384` → 得到槽编号 → 查找该槽归属的节点。
- **客户端如何知道槽分布？** 连接任意节点获取槽分布表（`CLUSTER SLOTS`），客户端缓存在本地。
- **MOVED 重定向：** 如果客户端请求了错误节点，节点返回 `MOVED <slot> <ip>:<port>`，客户端重定向并更新本地缓存。
- **节点宕机：** 主节点故障 → Cluster 内部的故障检测（Gossip 协议，多数节点认为 FAIL）→ 对应的从节点自动晋升为新主节点 → 槽重新映射。

**追问：**
> "为什么是 16384 个槽而不是更多？" → 每个节点通过 Gossip 消息交换槽分布信息，16384 个槽的 bitmap 只需要 2KB（16384/8），网络开销小。如果用 65536 个槽则需要 8KB，Gossip 消息过大。

---

## 三、计算机网络

### 3.1 TCP 三次握手与四次挥手

**问题：** 为什么握手是三次而不是两次？为什么挥手是四次而不是三次？TIME_WAIT 状态的作用是什么？

**参考答案：**

**三次握手（不是两次）：**
- 两次握手无法防止**历史连接的建立**。假设客户端发了一个旧的 SYN（网络延迟），服务端收到后回 SYN-ACK，两次握手此时连接就建立了——但客户端并没有要建连接。三次握手时，客户端收到 SYN-ACK 后发现不是自己发起的，不会回 ACK，服务端超时后释放资源。
- 核心目的：**双方确认彼此的收发能力**。SYN → 服务端确认客户端能发；SYN-ACK → 客户端确认服务端能收能发；ACK → 服务端确认客户端能收。

**四次挥手（不是三次）：**
- TCP 是全双工的，关闭时需要双方各自关闭自己的发送通道。
- 客户端发 FIN（我不发了）→ 服务端回 ACK（知道了，但我可能还有数据要发）→ 服务端发完数据后发 FIN（我也不发了）→ 客户端回 ACK。
- 中间服务端的 ACK 和 FIN 不能合并，因为服务端可能还有数据要发。（实际上如果服务端没有待发数据，可以合并为三次，即 FIN+ACK 同时发送。）

**TIME_WAIT：**
- 持续 2MSL（Maximum Segment Lifetime，通常 60 秒）。
- 作用一：确保最后的 ACK 能到达服务端（如果丢了，服务端会重发 FIN，客户端在 TIME_WAIT 期间可以重发 ACK）。
- 作用二：确保本连接的所有报文在网络中消亡，防止新连接收到旧报文。

**追问链：**

| 层级 | 追问 | 考察点 |
|------|------|--------|
| L1 | "服务端出现大量 TIME_WAIT 会怎样？怎么解决？" | 端口耗尽。`SO_REUSEADDR` / `tcp_tw_reuse` / 缩短 TIME_WAIT 时长 |
| L2 | "服务端出现大量 CLOSE_WAIT 说明什么？" | 服务端没有正确关闭连接（代码 bug：未 close socket）。需要排查代码中的资源泄漏 |
| L3 | "TCP 的 keepalive 和 HTTP 的 keep-alive 是一回事吗？" | 不是。TCP keepalive 是传输层探测连接是否存活（默认 2 小时发一次探测包）；HTTP keep-alive 是应用层复用连接（一个 TCP 连接上发多个 HTTP 请求） |

---

### 3.2 HTTP/HTTPS

**问题：** HTTPS 的握手过程？客户端如何验证服务器的证书？对称加密和非对称加密在 HTTPS 中分别用在哪里？

**参考答案：**

```
客户端                                   服务端
  │── ClientHello（支持的加密套件列表）──→│
  │←── ServerHello（选定的加密套件）  ────│
  │←── 服务端证书（含公钥）           ────│
  │←── ServerHelloDone               ────│
  │                                       │
  │  [客户端验证证书]                      │
  │  [生成随机的 Pre-Master Secret]        │
  │  [用服务端公钥加密 Pre-Master Secret]  │
  │                                       │
  │── ClientKeyExchange（加密的 PMS）──→│
  │── ChangeCipherSpec               ──→│
  │── Finished（加密的验证数据）      ──→│
  │                                       │
  │←── ChangeCipherSpec              ────│
  │←── Finished（加密的验证数据）     ────│
  │                                       │
  │←─── 后续通信使用对称加密 ─────────→│
```

- **非对称加密：** 仅用于握手阶段传输 Pre-Master Secret（RSA 或 ECDHE）。
- **对称加密：** 双方用 PMS 推导出会话密钥，后续所有数据用对称加密传输（AES 等）。
- **为什么不全用非对称：** 非对称加密速度慢（比对称慢 100-1000 倍），不适合大量数据传输。

**证书验证：** 客户端内置了受信任的根 CA 证书 → 验证服务端证书的签名链（服务端证书 → 中间 CA → 根 CA）→ 检查证书是否过期、域名是否匹配、是否被吊销（CRL/OCSP）。

---

## 四、操作系统

### 4.1 进程、线程、协程

**问题：** 进程和线程的区别？线程和协程的区别？Go 的 goroutine / Java 的虚拟线程属于哪种？

**参考答案：**

| 维度 | 进程 | 线程 | 协程 |
|------|------|------|------|
| 调度 | OS 内核调度 | OS 内核调度 | 用户态调度（runtime） |
| 切换开销 | 大（页表切换、TLB 刷新） | 中（寄存器、栈切换） | 小（只切换少量寄存器） |
| 内存 | 独立地址空间 | 共享进程地址空间 | 共享线程栈，自己的栈很小（KB 级） |
| 通信 | IPC（管道/共享内存/消息队列） | 共享内存 + 锁 | channel / yield |
| 创建成本 | 高（MB 级内存） | 中（MB 级栈） | 低（KB 级栈） |

- **Go goroutine：** 协程。由 Go runtime 的 GMP 调度器管理，初始栈 2KB，可动态扩缩。一个进程可创建数百万个 goroutine。
- **Java 虚拟线程（JDK 21 Project Loom）：** 类似协程。由 JVM 管理，挂载在少量 OS 线程上，遇到阻塞自动让出。

**追问：**
> "goroutine 遇到系统调用阻塞时会怎样？" → M 被阻塞，P 与 M 解绑，找一个空闲 M（或创建新 M）继续运行 P 上的其他 G。

---

### 4.2 虚拟内存与页表

**问题：** 什么是虚拟内存？为什么需要虚拟内存？页缺失（Page Fault）是怎么处理的？

**参考答案：**

- **虚拟内存：** 每个进程有独立的虚拟地址空间（如 0 ~ 2⁶⁴），由操作系统通过页表（Page Table）将虚拟地址映射到物理内存。
- **为什么需要：**
  1. **隔离性：** 进程间不能直接访问对方的内存。
  2. **简化编程：** 每个进程认为自己独占内存，无需关心物理地址分配。
  3. **超额分配：** 虚拟地址空间可以远大于物理内存（部分页可以交换到磁盘）。
- **Page Fault 处理：** CPU 访问虚拟地址 → 查页表发现该页不在物理内存（未映射或被换出） → 触发 Page Fault 中断 → OS 处理：
  - 如果是合法地址但页被换出：从磁盘（swap）加载到物理内存 → 更新页表 → 重新执行指令。
  - 如果是非法地址：发送 SIGSEGV（段错误），进程被杀。

**追问：**
> "malloc 调用后内存是立即分配的吗？" → 不是。malloc 只分配虚拟地址空间，物理页在首次访问时通过 Page Fault 才真正分配（延迟分配 / Lazy Allocation）。

---

## 五、分布式系统

### 5.1 一致性哈希

**问题：** 一致性哈希解决了什么问题？虚拟节点是什么？为什么需要虚拟节点？

**参考答案：**

- **解决的问题：** 普通取模哈希（`hash(key) % N`），当节点数 N 变化时，几乎所有 key 的映射都会改变，导致大面积缓存失效。一致性哈希将节点和 key 都映射到环上，增删节点只影响相邻节点的 key 映射，迁移量最小。
- **虚拟节点：** 每个物理节点在环上创建多个虚拟节点（如 150 个），key 先映射到虚拟节点再路由到物理节点。
- **为什么需要：** 物理节点少时，环上分布不均匀，导致数据倾斜（某些节点承担大部分 key）。虚拟节点使分布更均匀。

**追问：**
> "一致性哈希在 Redis Cluster 中有用到吗？" → 没有。Redis Cluster 用的是哈希槽（16384 slots），不是一致性哈希。哈希槽的好处是槽的分配可以手动调整（指定哪些槽在哪个节点），而一致性哈希的分配是算法决定的，不灵活。

---

### 5.2 Raft 共识算法

**问题：** Raft 算法的核心思想是什么？Leader 选举过程？日志复制过程？

**参考答案：**

**核心思想：** 在多个节点中选出一个 Leader，所有写请求通过 Leader 处理，Leader 将日志复制到多数节点后提交。

**三种角色：** Leader（唯一写入入口）、Follower（被动接收日志）、Candidate（选举中间状态）。

**Leader 选举：**
1. Follower 在 election timeout 内未收到 Leader 心跳 → 转为 Candidate → 任期号 +1 → 给自己投票 → 向其他节点请求投票。
2. 收到多数票（> N/2）→ 成为 Leader → 开始发送心跳。
3. 每个节点每个任期只投一票（先到先得），保证每个任期最多一个 Leader。

**日志复制：**
1. 客户端写请求 → Leader 追加到本地日志。
2. Leader 将日志条目发送给所有 Follower。
3. 多数 Follower 确认（写入本地日志）→ Leader 提交该条目 → 返回客户端成功。
4. 下次心跳通知 Follower 提交。

**追问：**
> "脑裂（Split Brain）场景 Raft 怎么处理？" → 网络分区后可能出现两个 Leader（不同分区各选一个），但只有拥有多数节点的分区的 Leader 能成功提交（另一个 Leader 收不到多数确认）。分区恢复后，任期号低的 Leader 自动退位。

---

### 5.3 幂等设计

**问题：** 什么是幂等？为什么分布式系统中幂等这么重要？有哪些实现幂等的方法？

**参考答案：**

- **定义：** 同一操作执行一次和执行多次的效果一样。`f(f(x)) = f(x)`。
- **为什么重要：** 分布式环境中，网络超时后客户端会重试，服务端可能收到重复请求。如果接口不幂等，会导致重复扣款、重复下单等严重问题。

**实现方法：**

| 方法 | 原理 | 适用场景 |
|------|------|---------|
| **唯一 ID + 去重表** | 每次请求携带唯一 ID，服务端用唯一索引/Redis 去重 | 通用，最常用 |
| **数据库唯一约束** | `INSERT ... ON DUPLICATE KEY` 或 `UPSERT` | 写操作 |
| **状态机** | 只允许合法的状态转换（待支付→已支付，但不允许已支付→已支付） | 状态变更类操作 |
| **乐观锁（版本号）** | `UPDATE SET ... WHERE version = old_version` | 更新操作 |
| **Token 机制** | 先获取 token，提交时校验并删除 token，重复提交时 token 已不存在 | 前端防重复提交 |
| **天然幂等** | `SET x = 5`（无论执行几次结果都一样） | 赋值类操作 |

**追问：**
> "去重表的记录什么时候清理？不清理会一直增长。" → 根据业务设置 TTL（如 24 小时内的去重足够），定时任务清理过期记录。或者用 Redis 的 `SETNX + TTL` 替代数据库去重表。

---

## 六、场景分析题

### 6.1 数据库连接池打满

**问题：** 线上服务突然报大量 "cannot acquire connection" 错误，数据库连接池被打满了。可能的原因有哪些？怎么排查和处理？

**参考答案：**

**可能原因：**
1. **慢 SQL：** 某条 SQL 执行时间很长，连接被长时间占用 → 其他请求拿不到连接。
2. **事务未正确关闭：** 代码中开启事务但异常路径没有 commit/rollback → 连接泄漏。
3. **流量突增：** QPS 超过连接池最大容量。
4. **下游数据库故障：** DB 响应变慢或不可用，连接全阻塞在等待 DB 响应。
5. **死锁：** 多个事务互相等待，连接全被占用。
6. **连接池配置不合理：** maxPoolSize 太小。

**排查步骤：**
1. 查看连接池监控：活跃连接数、等待线程数、连接获取超时次数。
2. `SHOW PROCESSLIST`：查看 MySQL 当前连接状态，是否有大量 Sleep 或 Lock Wait。
3. 查慢 SQL 日志：是否有新出现的慢 SQL。
4. 查最近代码变更：是否有事务使用不当。
5. 查应用日志：是否有异常堆栈显示事务未关闭。

**处理：**
- 短期：扩大连接池 / kill 掉阻塞的 SQL / 重启服务。
- 长期：修复慢 SQL / 修复事务泄漏 / 增加连接获取超时告警。

---

### 6.2 缓存热 Key

**问题：** 什么是热 Key 问题？如何发现和解决？

**参考答案：**

- **定义：** 某个 Redis key 被极高频访问（如秒杀商品、明星微博），导致该 key 所在的 Redis 节点 CPU/网络打满，而其他节点空闲。
- **发现：** Redis 的 `--hotkeys` 参数 / `MONITOR` 命令（生产慎用）/ 业务侧统计访问频率 / 阿里云等云服务提供的热 key 分析功能。
- **解决方案：**
  1. **本地缓存：** 在应用进程内缓存热 key（如 Go 的 `sync.Map`、Java 的 Caffeine），减少 Redis 访问。
  2. **读副本：** 热 key 读请求分散到 Redis 从节点。
  3. **Key 分片：** 将一个热 key 拆分为多个（如 `hot_key_0` ~ `hot_key_9`），读时随机选一个，写时全部更新。
  4. **提前预热 + 永不过期：** 热 key 不设过期时间，后台异步更新。

---

### 6.3 如何保证接口的幂等性

**问题：** 你的一个转账接口，用户点了两次提交按钮，怎么保证只扣一次钱？

**参考答案：**

**方案：全局唯一请求 ID + 幂等表**

```
1. 前端生成或后端下发唯一的 request_id
2. 后端收到请求：
   BEGIN TRANSACTION;
   -- 尝试插入幂等记录
   INSERT INTO idempotent_log(request_id, status) VALUES('xxx', 'processing');
   -- 如果插入失败（唯一键冲突）→ 说明已处理过 → 查询之前的处理结果直接返回
   -- 如果插入成功 → 执行转账逻辑
   UPDATE account SET balance = balance - 100 WHERE user_id = 1 AND balance >= 100;
   -- 更新幂等记录状态
   UPDATE idempotent_log SET status = 'done', result = '...' WHERE request_id = 'xxx';
   COMMIT;
3. 返回结果
```

**追问：** "如果第一次请求执行到一半服务挂了（幂等记录状态为 processing），第二次请求进来怎么办？" → 检查 processing 状态的创建时间，如果超过阈值（如 30s），认为上次请求失败，删除旧记录重新执行。

---

## 七、编码题

### 7.1 实现一个简单的 HashMap

**问题：** 请用你熟悉的语言实现一个简化版的 HashMap，支持 Put 和 Get 操作。说清楚哈希冲突的解决方式。

**考察：** 数组 + 链表（拉链法），哈希函数，扩容策略。

**进阶追问：**
- "什么时候需要扩容？负载因子怎么设？"（通常 0.75，元素数/桶数 > 0.75 时扩容为 2 倍）
- "Java HashMap 链表长度超过 8 会怎样？"（转红黑树，缩到 6 转回链表）

### 7.2 实现生产者消费者模型

**问题：** 用 Go/Java 实现一个线程安全的生产者消费者模型，要求有界队列。

**Go 版本：** 用 channel 天然实现。
**Java 版本：** `BlockingQueue` 或 `ReentrantLock + Condition`。

**看点：** 是否考虑了优雅退出、队列满/空时的处理、多生产者多消费者。

### 7.3 实现一个定时任务调度器

**问题：** 设计一个支持"在指定延迟后执行任务"的调度器。要求支持高并发添加任务。

**看点：** 最小堆（优先队列） 或 时间轮（Timing Wheel）。
- 最小堆：每次取堆顶检查是否到期，适合任务量不大的场景。
- 时间轮：将时间划分为槽位，任务放入对应槽位，指针转动时执行当前槽的任务。适合大量定时任务（如 Kafka、Netty 使用时间轮）。

---

## 使用指南

### 按考察时长选题

| 时长 | 推荐组合 |
|------|---------|
| 10 分钟快速摸底 | 1.2（MVCC）+ 2.1（Redis 快的原因）+ 3.1（TCP 三次握手） |
| 20 分钟中等考察 | 1.1 + 1.3（MySQL 两道）+ 2.2（Redis 持久化）+ 5.3（幂等） |
| 30 分钟深入考察 | 1.2 + 1.4 + 2.1 + 3.1 + 5.2（Raft）+ 6.1（连接池打满） |

### 按候选人水平调整

| 水平 | 策略 |
|------|------|
| 初级（1-2 年） | 重点考 MySQL 索引、Redis 基础、TCP/HTTP、编码题 |
| 中级（3-5 年） | 加入 MVCC、锁机制、分布式一致性、场景分析 |
| 高级（5 年+） | 重点考场景分析 + 追问链深挖到 L3，减少基础概念题 |
