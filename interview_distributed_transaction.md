# 面试题：分布式事务

> 由浅入深，共 8 道题，覆盖理论基础 → 主流方案 → 实战落地 → 踩坑与权衡。建议用时 15-25 分钟。

---

## 题目 1：什么是分布式事务？为什么需要它？

**问题：**
本地事务大家都熟悉，请问：
1. 什么情况下本地事务不够用了，必须引入分布式事务？请举一个你实际遇到的例子。
2. 分布式事务要解决的核心问题是什么？

**参考答案：**

1. **何时需要分布式事务：**
   - 单体应用拆分为微服务后，一个业务操作涉及多个独立数据库或服务。例如：
     - 电商下单：订单服务（写订单 DB）+ 库存服务（扣库存 DB）+ 积分服务（加积分 DB），三个独立的数据库无法用一个本地事务包裹。
     - 转账：A 银行扣款 + B 银行加款，跨机构无法共享事务。
   - 本质：**当一个业务操作横跨多个「事务参与者」（不同数据库、不同服务、不同中间件）时**，本地事务的 ACID 只能保证单个参与者内部，无法保证全局的原子性和一致性。

2. **核心问题：**
   - **原子性（Atomicity）：** 多个参与者要么全部成功，要么全部回滚，不能出现部分成功部分失败的中间状态。
   - 受限于网络分区、节点故障等分布式环境固有的不确定性，实现全局原子性远比本地事务复杂。

**评分标准（满分 5 分）：**
| 表现 | 分数 |
|------|------|
| 只是泛泛地说"多个服务需要保持一致" | 2 分 |
| 能举出具体的业务场景，说清楚"跨数据库/跨服务"的本质 | 4 分 |
| 还能提到 CAP/BASE 等理论背景 | 5 分 |

---

## 题目 2：CAP 和 BASE

**问题：**
1. CAP 定理的三个要素分别是什么？为什么分布式系统只能满足其中两个？
2. BASE 理论是什么？它和 ACID 是什么关系？
3. 在你的实际项目中，你们选择的是 CP 还是 AP？为什么？

**参考答案：**

1. **CAP 定理：**
   - **C（Consistency）一致性：** 所有节点在同一时刻看到相同的数据。
   - **A（Availability）可用性：** 每个请求都能在合理时间内收到非错误的响应（不保证数据最新）。
   - **P（Partition Tolerance）分区容忍性：** 系统在网络分区（节点间通信中断）时仍能继续工作。
   - **为什么只能满足两个：** 网络分区在分布式环境中不可避免（P 必须满足），因此实际选择只在 C 和 A 之间。当发生网络分区时：选 C 则拒绝部分请求以保证一致性（如返回错误）；选 A 则允许响应但数据可能不一致。
   - 注意：CAP 讨论的是**网络分区发生时**的行为，正常情况下三者可以同时满足。

2. **BASE 理论：**
   - **BA（Basically Available）基本可用：** 出现故障时允许损失部分可用性（如响应时间变长、降级返回）。
   - **S（Soft State）软状态：** 允许系统存在中间状态（如"支付中""处理中"），数据不需要时刻一致。
   - **E（Eventually Consistent）最终一致性：** 经过一段时间后，所有数据副本最终会达到一致。
   - **与 ACID 的关系：** ACID 追求强一致性（每次操作后立即一致），BASE 是对 ACID 在分布式场景下的妥协，用"最终一致性"换取更好的可用性和性能。二者不是对立关系，而是不同场景下的不同选择。

3. **实际选择（举例）：**
   - 金融/支付场景：偏 CP（宁可不可用也不能数据不一致）。
   - 电商商品展示/社交动态：偏 AP（允许短暂看到旧数据，但保证一直可访问）。
   - 实际中很少是纯粹的 CP 或 AP，而是在不同的业务环节做不同的取舍。

**评分标准（满分 5 分）：**
| 表现 | 分数 |
|------|------|
| 只能背出 C/A/P 的定义 | 2 分 |
| 能解释为什么"三选二"，且理解 P 的不可避免性 | 3 分 |
| 能结合 BASE 理论和实际项目说清楚取舍 | 5 分 |

---

## 题目 3：2PC（两阶段提交）

**问题：**
1. 请描述 2PC 的完整流程（Prepare 阶段和 Commit 阶段）。
2. 2PC 有哪些经典问题？分别在什么场景下发生？
3. 3PC 是如何改进 2PC 的？解决了什么问题？又引入了什么新问题？

**参考答案：**

1. **2PC 完整流程：**

   ```
   阶段一：Prepare（投票阶段）
   ┌────────────┐
   │  协调者 TM   │ ── Prepare ──→ 参与者A：锁定资源，写 redo/undo log，返回 YES/NO
   │             │ ── Prepare ──→ 参与者B：锁定资源，写 redo/undo log，返回 YES/NO
   └────────────┘

   阶段二：Commit / Rollback（提交阶段）
   若全部 YES → 协调者发送 Commit → 参与者提交事务，释放资源
   若任一 NO  → 协调者发送 Rollback → 参与者回滚事务，释放资源
   ```

   - Prepare 阶段：协调者（Transaction Manager）向所有参与者发送 Prepare 请求。参与者执行事务操作但**不提交**，将结果（YES 可以提交 / NO 无法提交）反馈给协调者。此时参与者已经**锁定了相关资源**。
   - Commit 阶段：协调者根据所有参与者的反馈做全局决策。全部 YES 则发送 Commit；任一 NO 或超时则发送 Rollback。参与者收到指令后执行提交或回滚，释放资源。

2. **经典问题：**

   | 问题 | 描述 | 发生场景 |
   |------|------|---------|
   | **同步阻塞** | Prepare 阶段参与者锁定资源后必须等待协调者指令，期间资源被独占，其他事务无法访问 | 所有 2PC 事务都存在 |
   | **单点故障** | 协调者宕机后参与者不知道该 Commit 还是 Rollback，陷入阻塞 | 协调者在 Commit 阶段宕机 |
   | **数据不一致** | 协调者发送 Commit 后，只有部分参与者收到（网络故障），导致一些提交了一些没提交 | Commit 指令发送过程中网络分区 |
   | **资源长时间锁定** | 如果协调者迟迟不发送二阶段指令，参与者持有的锁无法释放 | 协调者慢或网络延迟 |

3. **3PC 的改进：**
   - 在 Prepare 和 Commit 之间增加了 **PreCommit** 阶段（CanCommit → PreCommit → DoCommit）。
   - 参与者引入**超时机制**：如果在 PreCommit 阶段后长时间没收到 DoCommit，参与者会自动提交（因为已经进入 PreCommit 说明大概率全局一致）。
   - **解决了：** 参与者的阻塞问题（超时自动决策，不会无限等待协调者）。
   - **新问题：** 超时自动提交可能在网络分区时导致数据不一致（协调者实际发送了 Rollback 但参与者没收到，自动 Commit 了）。
   - **实际应用：** 3PC 在工业界很少使用，因为增加了一轮网络交互（延迟更高），且没有根本解决数据不一致问题。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 流程描述 | 3 分 | 两个阶段的细节清晰（锁定资源、不提交、等待指令） |
| 问题分析 | 4 分 | 至少说出同步阻塞、单点故障、数据不一致三个问题 |
| 3PC 理解 | 3 分 | 能说出改进点和新引入的问题，了解其实际不常用 |

---

## 题目 4：TCC（Try-Confirm-Cancel）

**问题：**
1. TCC 的三个阶段分别做什么？与 2PC 的核心区别是什么？
2. 请以"下单扣库存"为例，说明 TCC 的 Try、Confirm、Cancel 各自的具体操作。
3. TCC 有哪些难点和局限性？"空回滚""悬挂"是什么问题？如何解决？

**参考答案：**

1. **TCC 三阶段：**
   - **Try：** 尝试执行，**预留**业务资源（冻结而非扣减），做业务检查。
   - **Confirm：** 确认执行，**真正提交**业务操作，使用 Try 阶段预留的资源。此阶段不做业务检查，要求幂等。
   - **Cancel：** 取消执行，**释放** Try 阶段预留的资源。要求幂等。
   - **与 2PC 的核心区别：**
     - 2PC 是**数据库层面**的协议，由数据库的事务管理器实现，业务无感知。
     - TCC 是**业务层面**的协议，Try/Confirm/Cancel 都是业务代码自己实现，对业务有侵入性。
     - 2PC 锁定的是数据库行/表资源；TCC "锁定"的是业务资源（如冻结库存），不持有数据库锁，资源占用更灵活。

2. **下单扣库存的 TCC 示例：**

   | 阶段 | 库存服务操作 | 数据变化 |
   |------|-------------|---------|
   | **Try** | 冻结库存：`frozen_stock += 1, available_stock -= 1` | 可用 100 → 99，冻结 0 → 1 |
   | **Confirm** | 扣减冻结库存：`frozen_stock -= 1` | 可用 99，冻结 1 → 0，实际库存 -1 |
   | **Cancel** | 释放冻结库存：`frozen_stock -= 1, available_stock += 1` | 可用 99 → 100，冻结 1 → 0，恢复原状 |

   关键点：Try 阶段只是"冻结"而非直接扣减，这样即使 Cancel 也不影响其他用户的可用库存。

3. **难点与局限性：**

   - **开发成本高：** 每个参与者需要实现三个接口，业务侵入性强。
   - **幂等要求：** Confirm 和 Cancel 必须幂等（网络重试可能导致多次调用）。
   - **空回滚：** Try 阶段因网络超时未到达参与者，但 TCC 框架判定超时后触发 Cancel。此时参与者没有执行过 Try，直接收到 Cancel 请求。
     - 解决：Cancel 时检查是否有对应的 Try 记录，没有则直接返回成功（空操作）。
   - **悬挂（Suspend）：** 空回滚之后，迟到的 Try 请求又到达了参与者。如果正常执行 Try，会导致资源被冻结但永远不会有 Confirm 或 Cancel 来释放。
     - 解决：Try 时检查是否已有对应的 Cancel 记录，如果有则拒绝执行（因为事务已经结束了）。
   - **实现方式：** 通常引入事务记录表（transaction_log），记录每个事务的每个参与者的状态（tried/confirmed/cancelled），用于判断空回滚和悬挂。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 三阶段理解 | 3 分 | Try 是预留而非扣减，区别于 2PC |
| 业务举例 | 3 分 | 能用具体例子说清冻结→确认/释放的数据变化 |
| 异常处理 | 4 分 | 空回滚和悬挂问题能说清定义、场景和解决方案 |

---

## 题目 5：Saga 模式

**问题：**
1. Saga 模式的核心思想是什么？与 TCC 有什么区别？
2. Saga 有"编排"和"协同"两种实现方式，分别是什么？各有什么优缺点？
3. Saga 的补偿操作有什么要求？如果补偿也失败了怎么办？

**参考答案：**

1. **Saga 核心思想：**
   - 将一个长事务拆分为一系列**本地事务**（T1, T2, T3, ...），每个本地事务都有一个对应的**补偿事务**（C1, C2, C3, ...）。
   - 正向执行：T1 → T2 → T3 → ... 全部成功则事务完成。
   - 反向补偿：如果 Tn 失败，则逆向执行 C(n-1) → C(n-2) → ... → C1，撤销已完成的操作。

   **与 TCC 的核心区别：**

   | 维度 | TCC | Saga |
   |------|-----|------|
   | 资源预留 | Try 阶段预留资源，不直接修改 | 每步直接修改（无预留阶段） |
   | 隔离性 | Try 冻结资源提供了一定隔离性 | 无隔离性，中间状态对外可见 |
   | 实现复杂度 | 每步三个接口（Try/Confirm/Cancel） | 每步两个接口（正向 + 补偿） |
   | 适用场景 | 需要资源预留的场景（如资金冻结） | 长流程、跨多服务的业务编排 |

2. **编排（Orchestration）vs 协同（Choreography）：**

   **编排模式（中心协调者）：**
   ```
   ┌──────────────┐
   │  Saga 协调者   │
   │  (Orchestrator)│
   └──┬───┬───┬───┘
      │   │   │
      ↓   ↓   ↓
   Svc A  Svc B  Svc C
   ```
   - 有一个中心协调者控制整个 Saga 流程，依次调用各服务，根据结果决定继续还是补偿。
   - 优点：流程清晰可控、易于监控和调试、集中管理状态。
   - 缺点：协调者是单点（需要高可用）、协调者逻辑复杂、服务与协调者耦合。

   **协同模式（事件驱动）：**
   ```
   Svc A ──事件──→ Svc B ──事件──→ Svc C
     ↑                                 │
     └──────── 补偿事件 ←───────────────┘
   ```
   - 没有中心协调者，各服务通过事件（MQ）驱动。每个服务完成后发布事件，下一个服务监听事件后执行。
   - 优点：无中心点、服务间松耦合、扩展性好。
   - 缺点：流程分散难以追踪、调试困难、环形依赖风险、难以理解全局流程。

   **选型建议：** 参与者少（3-5 个）用编排；参与者多、流程复杂用协同。实际项目中编排模式更常用（可控性更强）。

3. **补偿操作的要求：**
   - **幂等性：** 补偿可能被重复调用（网络重试），必须保证多次执行结果一致。
   - **可交换性：** 即使正向操作和补偿操作并发执行（极端场景），最终结果也应正确。
   - **必须成功：** 补偿操作在设计上应该一定能成功（因为它只是回滚已做的变更）。

   **补偿也失败了怎么办：**
   - 重试：补偿操作设计为幂等后，可以无限重试直到成功。
   - 人工介入：重试超过 N 次后告警，人工处理。
   - 记录日志：完整记录事务的每一步状态和失败原因，方便人工追溯和修复。
   - 实际上如果补偿操作设计合理（只是回滚自己做的变更），几乎不会永久失败（除非底层存储故障）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 核心思想 | 3 分 | 正向+补偿的模型清晰，与 TCC 的区别（有无预留）准确 |
| 编排 vs 协同 | 4 分 | 两种模式描述正确，优缺点对比全面 |
| 补偿设计 | 3 分 | 幂等、必须成功、失败兜底方案 |

---

## 题目 6：本地消息表 + MQ

**问题：**
1. 本地消息表方案的完整流程是什么？它解决了什么问题？
2. 为什么要把消息写入本地数据库而不是直接发 MQ？
3. 这个方案有什么局限性？与 RocketMQ 的事务消息有什么区别？

**参考答案：**

1. **完整流程：**

   ```
   生产者侧：
   ┌───────────────────────────────────────┐
   │ BEGIN TRANSACTION                       │
   │   1. 执行业务操作（如扣减库存）            │
   │   2. 写入本地消息表（status = pending）    │
   │ COMMIT                                  │
   └───────────────────────────────────────┘
         ↓
   定时任务扫描 pending 消息 → 发送到 MQ → 发送成功后更新为 sent
         ↓
   消费者侧：
   消费 MQ 消息 → 执行下游业务 → ACK
   ```

   - 解决的问题：**业务操作和消息发送的原子性**。直接先操作 DB 再发 MQ，可能出现 DB 成功但 MQ 发送失败（消息丢失），或 MQ 发送成功但 DB 回滚（幽灵消息）。本地消息表将两者放在同一个本地事务中，保证原子性。

2. **为什么写本地 DB：**
   - 业务操作和消息记录在**同一个数据库**中，可以用**本地事务**保证原子性（要么都成功，要么都回滚）。
   - 如果直接发 MQ，DB 操作和 MQ 发送是两个不同系统的操作，无法用本地事务包裹，就变成了一个分布式事务问题（用分布式事务来解决分布式事务，陷入循环）。

3. **局限性与对比：**

   **局限性：**
   - 消息表与业务表耦合在同一个数据库中，增加了 DB 负担。
   - 需要定时任务轮询，有延迟（非实时）。
   - 消息表需要定期清理，否则数据量会持续增长。
   - 对业务代码有侵入性（需要在业务事务中插入消息记录）。

   **与 RocketMQ 事务消息的对比：**

   | 维度 | 本地消息表 | RocketMQ 事务消息 |
   |------|----------|------------------|
   | 原理 | 本地事务 + 定时扫描 | Half 消息 + 本地事务 + 回查 |
   | 实时性 | 有延迟（定时扫描间隔） | 近实时（事务提交后立即投递） |
   | 依赖 | 只依赖数据库 | 依赖 RocketMQ |
   | 侵入性 | 需要消息表和定时任务 | 需要实现事务监听器（回查接口） |
   | 适用性 | 通用（任何 MQ 都可配合） | 仅适用于 RocketMQ |

   RocketMQ 事务消息流程：
   - 发送 Half 消息（对消费者不可见）→ 执行本地事务 → Commit/Rollback Half 消息。
   - 如果未收到 Commit/Rollback，Broker 会主动**回查**本地事务状态。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 流程描述 | 3 分 | 生产者侧的本地事务 + 定时扫描 + 消费者侧完整 |
| 原理理解 | 3 分 | 能说清为什么用本地 DB 而非直接发 MQ |
| 对比分析 | 4 分 | 局限性客观，与事务消息的区别准确 |

---

## 题目 7：方案选型与对比

**问题：**
现在总结一下——2PC、TCC、Saga、本地消息表这四种方案：
1. 请从一致性强度、性能、开发复杂度、适用场景四个维度做一个对比。
2. 给你以下三个真实场景，你会分别选择哪种方案？说明理由。
   - 场景 A：银行跨行转账（A 行扣款 + B 行加款）
   - 场景 B：电商下单（创建订单 + 扣库存 + 扣优惠券 + 加积分）
   - 场景 C：用户注册后发欢迎邮件和赠送新人券

**参考答案：**

1. **四种方案对比：**

   | 维度 | 2PC | TCC | Saga | 本地消息表 |
   |------|-----|-----|------|-----------|
   | **一致性** | 强一致 | 最终一致（有预留隔离） | 最终一致（无隔离） | 最终一致 |
   | **性能** | 差（同步阻塞、锁资源） | 中（多一轮网络调用） | 好（异步、无锁） | 好（异步） |
   | **开发复杂度** | 低（DB 层面，业务无感） | 高（3 个接口/参与者） | 中（2 个接口/参与者） | 低-中（消息表+定时任务） |
   | **业务侵入** | 无 | 强 | 中 | 中 |
   | **适用场景** | 同一 DB 集群内/强一致 | 资金/资源预留场景 | 长流程跨服务编排 | 异步通知/最终一致 |
   | **可用性影响** | 大（阻塞等待） | 中 | 小 | 小 |

2. **场景选型：**

   **场景 A — 银行跨行转账 → TCC**
   - 理由：资金操作需要"冻结→确认/释放"的语义，不能出现中间状态（如 A 扣了 B 没加）。
   - Try：A 冻结金额，B 预增（或预留额度）。
   - Confirm：A 扣减冻结金额，B 正式入账。
   - Cancel：A 解冻，B 取消预增。
   - 不用 2PC：跨银行无法共享数据库事务。
   - 不用 Saga：Saga 没有隔离性，A 扣款后在 B 加款前如果失败，中间状态（A 钱少了 B 钱没多）对用户可见。

   **场景 B — 电商下单 → Saga（编排模式）**
   - 理由：涉及 4 个服务，流程长，TCC 开发成本太高（4×3=12 个接口）。
   - Saga 流程：创建订单 → 扣库存 → 扣优惠券 → 加积分。任一步失败则逆序补偿。
   - 补偿：取消订单、回补库存、返还优惠券、扣回积分。
   - 中间状态可接受：用户看到"订单处理中"是正常的。
   - 库存部分如果对一致性要求高（不能超卖），可对库存单独采用 TCC（混合使用）。

   **场景 C — 注册后发邮件和赠券 → 本地消息表**
   - 理由：发邮件和赠券是注册成功后的**异步通知**，不需要与注册操作强一致，最终一致即可。
   - 注册事务中写入消息表 → 定时任务/MQ 消费 → 调用邮件服务和券服务。
   - 即使延迟几秒甚至几分钟也完全可接受。
   - 最简单的方案，不需要引入重量级的分布式事务框架。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 对比全面性 | 4 分 | 四维度对比准确，能抓住每种方案的核心特点 |
| 场景 A 选型 | 2 分 | 选 TCC 并能说清冻结语义的必要性 |
| 场景 B 选型 | 2 分 | 选 Saga 并能解释为什么不用 TCC（成本）和不用 2PC（跨服务） |
| 场景 C 选型 | 2 分 | 选最简方案（本地消息表/MQ），体现"不过度设计"的判断 |

---

## 题目 8：实际落地的踩坑与经验

**问题：**
1. 你在实际项目中用过哪种分布式事务方案？遇到过什么坑？
2. 有人说"能不用分布式事务就不用"，你怎么看？有哪些方式可以规避分布式事务？
3. 如果面试者是开发框架/中间件的角色：你了解 Seata 或 DTM 等分布式事务框架吗？它们的实现原理是什么？

**参考答案：**

1. **实际踩坑（候选人结合自身回答，以下为常见 case）：**
   - **补偿操作未幂等：** 补偿被重复调用导致数据异常（如库存被多次回补）。
   - **超时设置不合理：** TCC 的 Try 超时太短，正常执行也被判定超时触发 Cancel。
   - **日志不完整：** 事务中间步骤未记录日志，出问题时无法追溯到底执行到了哪一步。
   - **测试覆盖不足：** 正常流程测得很好，但异常路径（网络超时、部分失败、重复消息）覆盖不足。
   - **性能未预估：** 引入分布式事务后，核心链路延迟从 50ms 增加到 200ms，未提前做性能评估。

2. **"能不用就不用"的理解：**
   - **完全认同。** 分布式事务增加了复杂度、降低了性能、引入了更多的故障点。
   - **规避方式：**
     - **合理划分服务边界：** 将强关联的数据放在同一个服务/数据库中，用本地事务解决。例如：订单和订单明细放在同一个库，不需要分布式事务。
     - **业务层面容忍最终一致性：** 很多场景其实不需要强一致。用户看到"处理中"然后刷新后看到结果，是完全可接受的。
     - **异步化 + 重试 + 对账：** 把同步变异步，用消息队列解耦，下游保证幂等消费，定时对账兜底。这是工业界用得最多的"穷人版分布式事务"。
     - **查询模式拆分：** CQRS（命令查询职责分离），写入走主链路保证一致，查询走异步同步的读模型，允许延迟。

3. **Seata / DTM 框架（加分项）：**
   - **Seata：** 阿里开源，支持 AT（自动补偿，基于 undo log）、TCC、Saga、XA 四种模式。AT 模式最常用——通过代理数据源拦截 SQL，自动生成 undo log，失败时自动回滚。
   - **DTM：** Go 语言实现，支持 TCC、Saga、XA、二阶段消息等。轻量，适合 Go 技术栈。
   - 核心思路都是：**提供一个事务协调者（TC）来管理全局事务的状态和分支事务的提交/回滚。**

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 实战经验 | 3 分 | 能说出具体的踩坑 case（不是背理论） |
| 规避思路 | 4 分 | 认同"能不用就不用"，且能给出实际的规避方法 |
| 框架了解 | 3 分 | 了解 Seata/DTM 等框架，知道 AT 模式的 undo log 原理 |

---

## 综合评分与面试引导

### 题目难度梯度

```
基础理论                                      实战深度
  ├── 题目1: 概念    ──────────────────────────────┤
  ├── 题目2: CAP/BASE ─────────────────────────────┤
  ├── 题目3: 2PC ──────────────────────────────────┤
  ├── 题目4: TCC ──────────────────────────────────┤
  ├── 题目5: Saga ─────────────────────────────────┤
  ├── 题目6: 本地消息表 ────────────────────────────┤
  ├── 题目7: 对比选型  ─────────────────────────────┤
  └── 题目8: 踩坑经验  ─────────────────────────────┤
  ⭐        ⭐⭐       ⭐⭐⭐      ⭐⭐⭐⭐
```

### 建议用法

| 面试时长 | 推荐组合 |
|---------|---------|
| **10 分钟快速考察** | 题目 3（2PC）+ 题目 7（选型对比） |
| **15 分钟标准考察** | 题目 3 + 题目 4（TCC，重点考空回滚/悬挂）+ 题目 7 |
| **25 分钟深入考察** | 题目 1→3→4→5→7→8，全链路递进 |

### 评级标准

| 等级 | 表现 |
|------|------|
| **A（优秀）** | 2PC/TCC/Saga/消息表原理全部清晰，能对比选型，有实战踩坑经验，理解"能不用就不用"的工程哲学 |
| **B（良好）** | 掌握 2-3 种方案的原理，能做基本选型，了解幂等和补偿的重要性 |
| **C（合格）** | 知道 2PC 和 TCC 的基本流程，但对异常场景（空回滚、悬挂）理解模糊 |
| **D（不足）** | 只能背出名词定义，无法描述具体流程或做场景选型 |

### 区分"背八股"vs"真理解"的追问技巧

| 追问 | 背八股者的回答 | 真理解者的回答 |
|------|-------------|-------------|
| "TCC 的 Try 阶段具体冻结的是什么？" | "冻结资源"（泛泛而谈） | "比如库存场景，available_stock 减 1，frozen_stock 加 1，这样其他事务看到的可用库存是准确的" |
| "Saga 的中间状态对业务有什么影响？" | "可能数据不一致"（模糊） | "比如订单已创建但库存还没扣，此时用户查到了订单但商品详情页显示库存没变，需要业务上能接受这个窗口" |
| "你为什么不用 2PC？" | "因为 2PC 有单点问题"（背的） | "我们是跨服务调用，不共享数据库，2PC 用不了；而且在线交易场景阻塞等待不可接受" |
| "补偿失败了怎么办？" | "重试"（一个词） | "先重试 3 次，仍失败则写入异常表告警，同时记录完整的事务上下文方便人工恢复" |
