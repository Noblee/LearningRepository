# 面试题目集 — 基于候选人简历定制

> 候选人核心画像：C++ 高性能在线系统开发，具备超大规模存储架构设计与优化经验，涉及序列化、缓存、冷热分离、GPU 检索等方向。

---

## 一、项目深挖类（考察对自身项目的理解深度）

### 题目 1：Protobuf → FlatBuffers 迁移的技术决策与实施细节

**问题：**
你主导了核心数据结构从 Protobuf 迁移到 FlatBuffers 的工作，请详细回答以下几点：
1. Protobuf 和 FlatBuffers 在内存模型上的核心区别是什么？为什么 FlatBuffers 能降低 21% 的内存？
2. 迁移过程中遇到的最大技术挑战是什么？如何保证"平响无劣化"？
3. FlatBuffers 有哪些局限性？在什么场景下你不会推荐使用它？

**参考答案：**

1. **内存模型区别：**
   - Protobuf 是反序列化模型：读取时需要将二进制数据解码（decode）到内存中的结构体对象，每个字段都需要独立分配内存（如 string 需要 `std::string` 对象），存在大量小对象分配和内存碎片。
   - FlatBuffers 是零拷贝（zero-copy）模型：数据以扁平化二进制格式存储，读取时直接通过偏移量（offset）访问原始 buffer，无需反序列化，不产生额外的内存分配。
   - 内存降低 21% 的来源：(a) 消除了反序列化产生的临时对象和 STL 容器开销；(b) FlatBuffers 的内存布局更紧凑，减少了内存对齐浪费和指针开销；(c) 无需维护 Protobuf message 对象本身的元数据。

2. **技术挑战与平响保障：**
   - Schema 兼容性：需要设计新旧格式并存的过渡方案，可能采用灰度切换或双写双读策略。
   - 访问模式适配：FlatBuffers 的随机字段访问有额外的间接寻址开销（通过 vtable），对于频繁访问的热路径需要做缓存或预读优化。
   - 平响保障手段：(a) 消除了反序列化的 CPU 开销，读路径理论上更快；(b) 针对热点字段做访问优化；(c) 充分的线上灰度与 A/B 测试验证。

3. **FlatBuffers 局限性：**
   - 数据修改不友好：FlatBuffers 的 buffer 是不可变的（immutable），如需修改需要重建整个 buffer。
   - 写入性能：构建 FlatBuffer 的 builder 模式比 Protobuf 的直接赋值更复杂。
   - 调试困难：二进制格式不如 Protobuf 的 text format 直观。
   - 不适合频繁修改、schema 变动频繁的场景。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 原理理解 | 3 分 | 能清晰阐述零拷贝 vs 反序列化的内存模型差异，说清内存节省的具体来源 |
| 工程实践 | 3 分 | 能描述迁移方案（灰度、兼容性、回滚策略），提及具体的性能验证方法 |
| 局限性认知 | 2 分 | 能客观分析 FlatBuffers 的缺点和不适用场景 |
| 数据量化 | 2 分 | 能给出具体的 benchmark 数据或合理的推算逻辑 |

---

### 题目 2：分级缓存架构设计

**问题：**
你设计的"压缩全量存储 + LRU 热数据缓存"二级模型：
1. 请描述该架构的整体数据流和读写路径。
2. 为什么选择 LRU 而非 LFU 或其他淘汰策略？
3. 压缩算法如何选型？压缩/解压对 CPU 和延迟的影响如何评估和控制？
4. 如何确定 LRU 缓存的容量？如何监控缓存命中率并做动态调整？

**参考答案：**

1. **数据流：**
   - 全量数据以压缩格式（如 LZ4/Zstd）持久化存储在内存中，保证数据完整性。
   - 热数据以解压后的原始格式缓存在 LRU Cache 中，提供低延迟访问。
   - 读路径：先查 LRU Cache → 命中则直接返回 → 未命中则从压缩全量中读取并解压 → 写入 LRU Cache → 返回。
   - 写路径/更新：更新压缩全量存储，同时失效或更新 LRU Cache 中对应条目。

2. **LRU vs LFU：**
   - LRU 实现简单（O(1) 的 HashMap + 双向链表），内存开销小。
   - 商品搜索场景中，热点商品具有时间局部性（temporal locality），最近被访问的商品大概率短期内再次被访问，LRU 与该特征匹配。
   - LFU 需要维护频率计数，对于突发热点的响应不如 LRU 灵敏，且实现复杂度更高。
   - 当然，如果存在长期稳定的热点（如常年爆款商品），LFU 或 W-TinyLFU 可能更优，可以视线上数据分布做二次优化。

3. **压缩算法选型：**
   - 候选：LZ4（极快解压，压缩比中等）、Zstd（压缩比高，解压速度优秀）、Snappy（折中方案）。
   - 评估维度：(a) 解压速度（直接影响 cache miss 延迟）；(b) 压缩比（决定内存节省幅度）；(c) 压缩速度（影响写入/更新延迟）。
   - 实际选型建议：LZ4 或 Zstd level 1-3，在解压速度和压缩比间取得平衡。可以对不同字段分别选择压缩策略。

4. **容量规划与监控：**
   - 初始容量：根据线上访问日志分析商品访问分布（通常服从幂律分布/Zipf），取 Top N% 商品覆盖 80-90% 请求量所需内存作为初始值。
   - 动态调整：监控 cache hit rate、P99 latency、内存水位等指标，设置告警阈值，必要时动态扩缩缓存容量。
   - 预热策略：服务启动时根据历史访问统计预加载热点商品，避免冷启动问题。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构描述 | 3 分 | 读写路径清晰，考虑了一致性问题（更新时缓存如何处理） |
| 缓存策略 | 3 分 | 对 LRU/LFU 等策略有深入理解，能结合业务特征分析选型 |
| 压缩选型 | 2 分 | 了解主流压缩算法特性，能给出量化对比和选型依据 |
| 运维监控 | 2 分 | 有完善的容量规划思路和线上监控方案 |

---

### 题目 3：冷热正排分离架构

**问题：**
1. 你如何定义"冷"和"热"数据？判定标准是什么？阈值如何设定？
2. 冷热分离后的系统架构是什么样的？冷数据的读取路径如何保证延迟可控？
3. 冷转热、热转冷的数据迁移策略是怎样的？如何保证迁移过程中服务的稳定性？
4. 你提到低峰期核数缩减约 10000 Core，CPU 利用率从 29% 提升到 37%，这是如何实现的？K8s 层面做了什么？

**参考答案：**

1. **冷热定义：**
   - 基于算法团队提供的商品活跃度评分（如最近 N 天的曝光量、点击量、购买量、搜索命中频率等多维度指标）。
   - 阈值设定：分析历史数据，找到"少量商品覆盖大部分请求"的拐点（如 20% 的商品覆盖 95% 的请求），以此为界划分冷热。
   - 需定期（如每天/每周）重新计算，避免数据陈旧。

2. **架构设计：**
   - 热正排集群：常驻内存，承担绝大多数在线请求，保证低延迟。
   - 冷正排集群：可采用更高密度的部署（更少 CPU、更多内存或 SSD），甚至可以使用之前预研的 RocksDB SSD 方案。
   - 请求路由：在上层增加路由层，根据商品 ID 判断冷热归属，分别路由到对应集群。
   - 冷数据延迟控制：(a) 冷集群也可以有小规模内存缓存；(b) 冷数据请求通常出现在长尾场景，业务上对延迟容忍度更高；(c) 可以设置冷请求的超时和降级策略。

3. **数据迁移策略：**
   - 定期批量迁移：每天/每周根据最新的冷热评分结果，通过全量/增量构建的方式更新两个集群。
   - 冷→热升温：若某冷商品突然变热（如大促），需要快速升温机制，可通过消息队列触发实时迁移。
   - 稳定性保障：(a) 双写过渡期，冷热集群同时持有该商品数据；(b) 灰度迁移，逐步增大迁移比例；(c) 回滚机制，异常时快速回退。

4. **K8s 资源优化：**
   - 冷数据独立部署后，热集群的数据量显著减少，单 Pod 内存需求下降，可以缩减 Pod 规格或数量。
   - 结合 K8s 的 HPA（Horizontal Pod Autoscaler）或定时扩缩策略，在低峰期自动缩减热集群副本数。
   - CPU 利用率提升的原因：总核数减少但处理的请求量不变（热请求占绝大多数），单核负载提高，利用率自然提升。
   - 可能还配合了 VPA（Vertical Pod Autoscaler）调整单 Pod 的 CPU request/limit。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 冷热定义 | 2 分 | 标准清晰、可量化，考虑了动态调整 |
| 架构清晰度 | 3 分 | 冷热集群的部署方式、请求路由、数据流向描述完整 |
| 迁移稳定性 | 3 分 | 有完善的冷热转换机制和灰度、回滚方案 |
| K8s 实践 | 2 分 | 能说清 HPA/VPA 等机制，理解资源效能提升的原理 |

---

### 题目 4：GPU 检索项目的架构与优化

**问题：**
1. 请介绍你们 GPU ANN 检索服务的整体架构，从索引构建到在线服务的完整链路。
2. IVF 算法的原理是什么？为什么选择 IVF 而非 HNSW 等其他 ANN 算法？
3. INT8 量化将 400GB 压缩到 4GB，压缩比约 100 倍。请分析这个压缩比的来源（IVF 聚类 + 量化 + 其他因素各贡献了多少）？量化对召回率的影响如何评估？
4. 布隆过滤器在这里是如何使用的？"聚类计算前高效筛选"具体指什么？

**参考答案：**

1. **整体架构：**
   - 离线链路：商品向量生成 → 向量聚类（IVF 训练 codebook）→ INT8 量化 → 构建 GPU 索引 → 索引分发到在线 GPU 节点。
   - 在线链路：Query 向量输入 → 布隆过滤器前置过滤 → 选取 TopK 最近聚类中心（nprobe）→ 在对应聚类内做 INT8 向量距离计算 → 返回 TopN 结果。
   - 索引更新策略：定期全量重建 + 增量更新（如有需要）。

2. **IVF 原理与选型：**
   - IVF（Inverted File Index）：先用 K-Means 将向量空间划分为 N 个聚类（Voronoi cells），查询时只在最近的 nprobe 个聚类中搜索，将搜索范围从全量缩小到 nprobe/N。
   - 选择 IVF 而非 HNSW 的原因：(a) IVF 的内存访问模式更适合 GPU 并行计算（批量连续内存访问 vs HNSW 的随机图遍历）；(b) IVF 可以与 PQ/SQ 等量化方法无缝结合；(c) HNSW 的图结构在 GPU 上难以高效实现（大量随机内存访问，不利于 GPU 的 warp 并行模式）。

3. **压缩比分析：**
   - 原始 400GB 假设为 float32 向量（4 bytes/维度），INT8 量化后 1 byte/维度，4x 压缩。
   - 如果使用 IVF-PQ（Product Quantization），进一步将向量分段量化，可实现更高压缩比。例如 128 维向量用 PQ 编码为 16 字节，则 32x 压缩。
   - 400GB → 4GB 约 100x 压缩，可能结合了 PQ + 去除冗余元数据 + 索引结构优化等多重因素。
   - 召回率评估：离线用全量精确搜索的 Ground Truth 做 Recall@K 评估，典型目标是 Recall@100 > 95%。可以通过调整 nprobe 和量化参数来平衡召回率和性能。

4. **布隆过滤器的应用：**
   - 场景：搜索场景中有大量需要过滤的商品（如下架商品、不符合筛选条件的商品）。如果在 GPU 距离计算后再过滤，会浪费大量 GPU 算力。
   - 方案：将需要过滤的商品 ID 集合构建为布隆过滤器，在聚类内遍历向量做距离计算之前，先用布隆过滤器判断该商品是否需要跳过。
   - "聚类计算前高效筛选"指的是：在 IVF 的 nprobe 个聚类确定后、对聚类内向量做逐一距离计算之前，通过布隆过滤器快速剔除无效商品，减少实际的距离计算量。
   - 布隆过滤器的特性（有假阳性、无假阴性）在此场景下可接受：误判只会导致多过滤少量有效商品，不会遗漏。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构理解 | 2 分 | 离线/在线链路完整清晰 |
| ANN 算法原理 | 3 分 | IVF 原理正确，能对比分析 IVF vs HNSW 在 GPU 上的适配性 |
| 量化理解 | 3 分 | 能分析压缩比构成，了解量化对召回率的影响和评估方法 |
| 布隆过滤器 | 2 分 | 使用场景和原理正确，理解假阳性的取舍 |

---

## 二、系统设计与架构类

### 题目 5：大规模 KV 存储系统设计

**问题：**
假设你需要设计一个支持 10 亿级商品的正排存储系统，要求：
- 单条记录平均 2KB，总数据量约 2TB
- 读 QPS 100 万，写 QPS 1 万
- 读延迟 P99 < 5ms
- 支持在线实时更新
请给出你的系统架构设计，包括存储引擎选型、数据分片策略、缓存设计、高可用方案等。

**参考答案：**

1. **存储引擎选型：**
   - 内存层：用 FlatBuffers + 压缩的二级缓存方案（如简历中所述），热数据常驻内存。
   - 持久化层：RocksDB 或类似 LSM-Tree 引擎，写入性能好，适合增量更新场景。
   - 混合方案：热数据全内存，冷数据 SSD（RocksDB），与候选人的冷热分离经验一致。

2. **数据分片：**
   - 按商品 ID 一致性哈希分片，分布到多个存储节点。
   - 每个分片副本数 >= 3，保证高可用。
   - 分片数量预留足够余量（如 1024 个虚拟分片），方便后续扩容。

3. **缓存设计：**
   - L1 Cache：客户端本地缓存（进程内），容量小但延迟极低（< 0.1ms）。
   - L2 Cache：集中式缓存（如 Redis 集群），容量大，延迟约 1ms。
   - L3 Storage：后端存储服务，内存+SSD 混合。
   - 缓存一致性：写入时更新存储后异步失效缓存（Cache-Aside），或使用发布订阅通知。

4. **高可用方案：**
   - 多副本 + Raft/Paxos 一致性协议。
   - 跨机房/可用区部署。
   - 读请求可以读从副本（最终一致性可接受的场景）。
   - 故障检测 + 自动切换。

5. **写入链路：**
   - 写入先到消息队列（如 Kafka）保证持久化。
   - 异步消费更新存储引擎和缓存。
   - 批量写入优化（batch write）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 整体架构 | 3 分 | 分层清晰，组件选型合理 |
| 分片与扩展性 | 2 分 | 分片策略合理，考虑了扩容场景 |
| 缓存设计 | 2 分 | 多级缓存，考虑了一致性问题 |
| 高可用 | 2 分 | 副本、故障转移等方案完整 |
| 量化分析 | 1 分 | 能做容量估算和性能分析 |

---

### 题目 6：Java 到 C++ 迁移的架构决策

**问题：**
你参与了将召回与粗排从 Java 迁移到 C++ 的项目：
1. Java 引擎的性能瓶颈具体体现在哪些方面？为什么选择 C++ 而非 Rust/Go？
2. 迁移过程中如何保证业务逻辑的正确性？有哪些验证手段？
3. "双倍召回量下平响保持稳定"，你做了哪些针对性优化？

**参考答案：**

1. **Java 瓶颈与 C++ 选型：**
   - GC 停顿：Java GC（即使是 ZGC/G1）在大内存场景下仍有不可忽略的停顿，影响 P99 延迟。
   - 内存开销：Java 对象头（16 bytes）、装箱、STL 等额外内存开销大。
   - JIT 编译不稳定：热点函数的 JIT 编译可能导致短暂的性能波动。
   - 选择 C++：(a) 团队已有 C++ 技术栈积累（正排系统就是 C++）；(b) C++ 的内存控制力最强，无 GC 开销；(c) 丰富的高性能库生态（SIMD、向量化等）；(d) Rust 学习曲线陡峭，Go 的 GC 问题与 Java 类似。

2. **正确性验证：**
   - 流量回放：录制线上真实请求，分别发送到 Java 和 C++ 服务，Diff 对比结果。
   - Shadow 流量：线上双发，C++ 服务只读不写，对比结果一致性。
   - 单元测试 + 集成测试：关键逻辑用相同的测试用例覆盖。
   - 灰度发布：逐步切流（1% → 10% → 50% → 100%），每阶段验证业务指标（如 CTR、GMV）无劣化。

3. **性能优化手段：**
   - 内存池/对象池：避免频繁的内存分配和释放。
   - SIMD 向量化：对向量距离计算等热点路径使用 SSE/AVX 指令加速。
   - 数据结构优化：使用 cache-friendly 的数据布局（如 SoA vs AoS）。
   - 并发优化：无锁数据结构、线程亲和性绑定、减少锁竞争。
   - 网络优化：批量请求合并、连接池复用、异步 IO。
   - 编译器优化：PGO（Profile-Guided Optimization）、LTO（Link-Time Optimization）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 瓶颈分析 | 3 分 | 能具体分析 Java 在高性能场景的瓶颈，语言选型理由充分 |
| 正确性验证 | 3 分 | 有完善的验证方案（流量回放、灰度等） |
| 性能优化 | 4 分 | 优化手段丰富且具体，能结合实际场景说明效果 |

---

## 三、C++ 基础与高性能编程

### 题目 7：内存管理与性能优化

**问题：**
1. 在高并发服务中，频繁的 `new`/`delete` 会带来什么问题？你在项目中是如何解决的？
2. 请解释 `std::shared_ptr` 的性能开销，以及在高性能场景中的替代方案。
3. 什么是 False Sharing？在你的多线程服务中是否遇到过？如何解决？

**参考答案：**

1. **频繁 new/delete 的问题与解决：**
   - 问题：(a) 系统调用开销（`brk`/`mmap`）；(b) 内存碎片导致内存利用率下降；(c) glibc malloc 的锁竞争（多线程场景）；(d) 内存分配器的元数据开销。
   - 解决方案：
     - 内存池（Memory Pool）：预分配大块内存，自行管理分配和回收。
     - 对象池（Object Pool）：针对固定大小的对象复用。
     - 使用高性能分配器：如 jemalloc、tcmalloc（线程本地缓存减少锁竞争）。
     - Arena 分配器：批量分配，统一释放，适用于请求级生命周期的对象（如 FlatBuffers 的 builder）。

2. **shared_ptr 的开销与替代：**
   - 开销：(a) 引用计数的原子操作（`atomic_increment`/`atomic_decrement`），每次拷贝/销毁都有原子操作开销；(b) 控制块的额外内存分配（`make_shared` 可减轻）；(c) 解引用时的间接寻址。
   - 替代方案：
     - `std::unique_ptr`：明确所有权，零额外开销。
     - 侵入式引用计数（Intrusive Ref Count）：将计数器嵌入对象本身，减少一次内存分配。
     - 生命周期管理：通过 Arena/Pool 管理对象生命周期，避免引用计数。
     - 在读多写少的场景中，可使用 RCU（Read-Copy-Update）模式。

3. **False Sharing：**
   - 定义：多个线程访问不同的变量，但这些变量位于同一个 CPU Cache Line（通常 64 bytes）中，导致一个线程的写入使其他线程的 Cache Line 失效，产生不必要的缓存一致性流量。
   - 解决：(a) 使用 `alignas(64)` 或 padding 确保不同线程的数据位于不同的 Cache Line；(b) C++17 的 `std::hardware_destructive_interference_size`；(c) 将频繁写入的变量分开放置。
   - 实际案例：多线程计数器、线程本地统计等场景容易出现。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 内存管理 | 4 分 | 了解 malloc 的问题和替代方案，有实际使用经验 |
| 智能指针 | 3 分 | 理解 shared_ptr 的底层实现和性能影响 |
| False Sharing | 3 分 | 概念正确，有解决方案，最好有实际案例 |

---

### 题目 8：并发编程

**问题：**
1. 请比较 `std::mutex`、`std::shared_mutex`、自旋锁（spinlock）和无锁（lock-free）数据结构的适用场景。
2. 在你的在线服务中，请求的并发模型是怎样的？使用了线程池还是协程？为什么？
3. 如何实现一个线程安全的 LRU Cache？请描述你的设计方案和锁策略。

**参考答案：**

1. **锁类型对比：**
   - `std::mutex`：通用互斥锁，适合临界区较长（> 微秒级）的场景，会触发线程切换。
   - `std::shared_mutex`：读写锁，适合读多写少场景，允许多个读者并发访问。
   - 自旋锁：适合临界区极短（< 微秒级）的场景，busy-wait 避免线程切换开销，但长时间持有会浪费 CPU。
   - 无锁数据结构：基于 CAS（Compare-And-Swap）原子操作，适合极高并发场景，但实现复杂、调试困难，且可能有 ABA 问题。

2. **并发模型：**
   - 典型的在线服务使用线程池 + 异步 IO 模型。
   - 每个请求由线程池中的工作线程处理，避免频繁创建/销毁线程。
   - 为什么不用协程：C++20 的协程生态尚不成熟，且在 CPU 密集型计算场景（如向量计算、排序）中协程优势不大；协程更适合 IO 密集型场景。
   - 如果使用了 brpc/grpc 等框架，可能内置了基于 bthread（用户态线程）的并发模型。

3. **线程安全 LRU Cache：**
   - 基础方案：HashMap + 双向链表 + 全局互斥锁。简单但锁竞争大。
   - 优化方案一：分片（Sharded LRU）—— 将 key 空间哈希到多个独立的 LRU 分片，每个分片独立加锁，减少锁竞争。
   - 优化方案二：读路径用 shared_lock（读写锁），写路径（插入/淘汰）用 exclusive lock。
   - 优化方案三：结合无锁 HashMap（如 ConcurrentHashMap）+ 近似 LRU（如采样淘汰），进一步降低锁开销。
   - 分片数量通常设为 CPU 核数的 2-4 倍。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 锁机制理解 | 3 分 | 各种锁的适用场景判断正确 |
| 并发模型 | 3 分 | 能说清线程模型选择的原因，了解 bthread 等用户态线程 |
| LRU 设计 | 4 分 | 有分片等优化思路，考虑了并发性能 |

---

## 四、数据结构与算法

### 题目 9：布隆过滤器深入

**问题：**
你在 GPU 检索项目中使用了布隆过滤器：
1. 布隆过滤器的基本原理是什么？时间和空间复杂度？
2. 给定 1 亿个商品 ID 需要过滤，要求假阳性率 < 1%，如何计算所需的位数组大小和哈希函数个数？
3. 布隆过滤器如何支持删除操作？如果需要删除，你会怎么设计？
4. 在 GPU 上使用布隆过滤器有什么特殊考虑？如何优化 GPU 上的布隆过滤器查询？

**参考答案：**

1. **基本原理：**
   - 一个长度为 m 的位数组（bit array），初始全为 0。
   - 插入元素时，用 k 个独立哈希函数计算 k 个位置，将这些位置置 1。
   - 查询时，计算 k 个位置，若全为 1 则"可能存在"，若有任一为 0 则"一定不存在"。
   - 时间复杂度：插入和查询都是 O(k)；空间复杂度：O(m) bits。

2. **参数计算：**
   - n = 1 亿，目标假阳性率 p = 0.01。
   - 最优位数组大小：m = -n × ln(p) / (ln2)² ≈ 1亿 × 9.585 ≈ 9.585 亿 bits ≈ 114 MB。
   - 最优哈希函数个数：k = (m/n) × ln2 ≈ 9.585 × 0.693 ≈ 6.64，取 7。

3. **删除支持：**
   - 标准布隆过滤器不支持删除（删除一个位可能影响其他元素）。
   - Counting Bloom Filter：将每个位扩展为一个计数器（如 4 bits），插入时 +1，删除时 -1。空间开销约为标准版的 4 倍。
   - Cuckoo Filter：支持删除，且在相同假阳性率下空间效率可能更优。
   - 实际方案选择取决于删除频率和空间预算。

4. **GPU 上的优化：**
   - 内存访问模式：GPU 的全局内存访问有高延迟，应将布隆过滤器放在 Shared Memory 或利用 L1/L2 Cache。若过大则分块加载。
   - Warp 级并行：同一 Warp 中的 32 个线程可以同时查询不同元素的布隆过滤器，利用合并内存访问（Coalesced Access）。
   - 哈希函数设计：选择计算简单的哈希函数（如 MurmurHash），避免分支和复杂运算。
   - Block Bloom Filter：将位数组按 Cache Line 大小分块，每个元素的 k 个哈希值都落在同一个块内，提高缓存命中率。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 基本原理 | 2 分 | 原理描述正确完整 |
| 参数计算 | 3 分 | 能正确推导公式并计算 |
| 删除方案 | 2 分 | 了解 Counting/Cuckoo Filter |
| GPU 优化 | 3 分 | 理解 GPU 内存层次和并行模式对布隆过滤器的影响 |

---

### 题目 10：向量检索算法

**问题：**
1. 请对比 IVF、HNSW、PQ 三种 ANN 算法的原理、优缺点和适用场景。
2. Product Quantization（PQ）的原理是什么？如何用 PQ 加速向量内积/距离计算？
3. 如果让你从零设计一个支持实时更新的 ANN 索引，你会如何设计？

**参考答案：**

1. **三种算法对比：**

   | 维度 | IVF | HNSW | PQ |
   |------|-----|------|----|
   | 原理 | K-Means 聚类后倒排 | 多层导航小世界图 | 向量子空间量化编码 |
   | 构建时间 | 中（聚类训练） | 长（图构建） | 中（码本训练） |
   | 查询时间 | 取决于 nprobe | O(log N) 图遍历 | 查表加速 |
   | 内存占用 | 存原始向量（大） | 原始向量+图结构（大） | 极小（量化后） |
   | 召回率 | 中-高 | 高 | 中 |
   | 更新友好 | 中（添加到对应聚类） | 差（图更新复杂） | 差（需重训码本） |
   | GPU 适配 | 好（批量并行） | 差（随机遍历） | 好（查表并行） |

   实际常组合使用：IVF-PQ、IVF-SQ（Scalar Quantization）等。

2. **PQ 原理：**
   - 将 D 维向量均分为 M 个子空间（每个子空间 D/M 维）。
   - 对每个子空间独立做 K-Means 聚类（通常 K=256），得到 M 个码本。
   - 编码：每个子向量用其最近聚类中心的 ID（1 byte）表示，D 维向量编码为 M bytes。
   - 距离计算加速：预计算 query 子向量到每个码本中心的距离（M × K 个距离值），存入查找表。计算 query 与某编码向量的距离时，只需 M 次查表和求和，O(M) 时间。

3. **实时更新的 ANN 索引设计：**
   - 架构：LSM-Tree 思想 —— 分层索引。
   - 实时层（L0）：新数据写入内存中的小型精确索引（如 flat index），支持实时查询。
   - 持久层（L1+）：定期将实时层数据合并到大型 ANN 索引（如 IVF-PQ）中，全量重建。
   - 查询合并：查询时同时搜索实时层和持久层，合并结果。
   - 删除处理：标记删除 + 定期压缩（compaction）。
   - 增量更新优化：IVF 索引可以直接将新向量追加到对应聚类的倒排列表中，无需全量重建。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 算法对比 | 3 分 | 三种算法原理正确，优缺点分析到位 |
| PQ 原理 | 3 分 | 能清晰解释子空间量化和查表加速 |
| 实时索引设计 | 4 分 | 架构合理，考虑了写入、查询、删除的完整生命周期 |

---

## 五、计算机基础

### 题目 11：操作系统与网络

**问题：**
1. 一个在线服务进程启动后，内存使用持续增长但从不释放（非内存泄漏，free list 显示有大量可用内存）。请分析可能的原因和解决方案。
2. 在你的存储服务中，某次升级后发现 P99 延迟偶发飙升到 100ms（正常 < 5ms），如何排查？请给出你的排查思路。
3. TCP 和 RDMA 在高性能存储系统中的区别和选型考虑？

**参考答案：**

1. **内存不释放的原因：**
   - glibc malloc 的内存归还策略：`free()` 不一定会将内存归还操作系统（通过 `brk` 分配的内存，只有堆顶的连续空闲区域才能归还）。中间释放的内存只是进入 free list，进程的 RSS 不会下降。
   - 内存碎片：大量不同大小的分配和释放导致外部碎片，即使空闲内存总量大，也无法合并归还。
   - 解决方案：(a) 使用 jemalloc/tcmalloc 替代 glibc malloc，碎片管理更好；(b) 使用 `mmap` 分配的大块内存，释放时可以直接归还 OS；(c) 定期通过 `malloc_trim()` 强制归还；(d) 使用内存池/Arena 管理生命周期明确的对象。

2. **P99 延迟排查思路：**
   - 第一层：确认是否偶发 —— 看延迟分布和发生时间规律，是否与 GC、日志轮转、cron job 等周期性事件相关。
   - 第二层：系统层面 —— `dmesg` 看是否有 OOM、网络异常；`perf` 抓热点函数；检查 CPU throttle（K8s CFS 限制）、NUMA 跨节点访问、磁盘 IO 毛刺。
   - 第三层：应用层面 —— 检查锁竞争（lock contention）、内存分配毛刺、某些特定请求触发的慢路径（如 cache miss 导致解压）。
   - 第四层：网络层面 —— 网卡中断绑核、TCP 重传、网络拥塞。
   - 结合 trace 系统（如 OpenTelemetry）定位慢请求的具体阶段。

3. **TCP vs RDMA：**
   - TCP：通用、成熟、硬件要求低，但需要内核参与（系统调用、协议栈处理、数据拷贝），延迟约 50-100μs。
   - RDMA：绕过内核直接访问远端内存，延迟 < 5μs，零拷贝，CPU 开销极低。但需要专用网卡（如 Mellanox ConnectX）和网络交换机支持。
   - 选型考虑：对延迟极度敏感的场景（如分布式内存数据库）用 RDMA；一般在线服务 TCP 够用且运维成本低。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 内存管理 | 3 分 | 理解 glibc malloc 的归还策略和碎片问题 |
| 延迟排查 | 4 分 | 排查思路系统化，覆盖系统/应用/网络多层面 |
| 网络基础 | 3 分 | TCP/RDMA 对比准确，了解各自适用场景 |

---

## 六、RocksDB 与存储引擎

### 题目 12：RocksDB 深入

**问题：**
你提到有 RocksDB 的调研与使用经验：
1. RocksDB 的 LSM-Tree 架构简述，读放大、写放大、空间放大分别是什么？
2. 在 SSD 化方案中，RocksDB 的性能瓶颈主要在哪里？你们遇到的"平响问题"具体是什么？
3. 如何调优 RocksDB 以适应在线低延迟读取场景？

**参考答案：**

1. **LSM-Tree 架构：**
   - 写入路径：Write → WAL（Write-Ahead Log）→ MemTable → Immutable MemTable → Flush 到 L0 SST → Compaction 合并到 L1, L2, ...
   - 读取路径：MemTable → L0 SST（可能多个重叠） → L1 → L2 → ... → 布隆过滤器 + 索引块辅助定位。
   - 三大放大：
     - 写放大：一次写入的数据在 Compaction 过程中被反复读写，写放大 = 实际写入磁盘量 / 用户写入量，典型值 10-30x。
     - 读放大：一次点查可能需要检查多个 Level 的 SST 文件，读放大 = 实际读取磁盘量 / 返回数据量。
     - 空间放大：Compaction 未完成时，旧数据和新数据共存，空间放大 = 磁盘占用量 / 有效数据量。

2. **SSD 方案的平响问题：**
   - 即使是 NVMe SSD，单次随机读延迟约 50-200μs，对比内存的 100ns 级差了 1000 倍。
   - 在缓存未命中时，一次商品查询可能需要多次 SSD 读取（多级索引 + 数据块），导致长尾延迟。
   - Compaction 期间的 IO 竞争会进一步恶化读延迟。
   - Block Cache 命中率不稳定：商品访问分布的变化导致缓存命中率波动。
   - 具体表现：P99 或 P999 延迟不可控，超出在线服务的 SLA 要求。

3. **低延迟调优：**
   - Block Cache 优化：增大 Block Cache 容量，使用 Clock Cache（无锁、更适合高并发）替代 LRU Cache。
   - 布隆过滤器：为每个 SST 文件配置布隆过滤器（通常 10 bits/key），减少不必要的磁盘读取。
   - 减少 Level 数：使用 Level Compaction + 适当的 level_compaction_dynamic_level_bytes，减少最大 Level 数。
   - 限制 L0 文件数：设置 `level0_file_num_compaction_trigger` 较小值，避免 L0 积累太多文件导致读放大。
   - Rate Limiter：限制 Compaction 的 IO 带宽，避免与前台读竞争。
   - Pin L0/L1 in Cache：将低 Level 的索引和过滤器常驻内存。
   - Direct IO：绕过 OS Page Cache，避免双重缓存。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| LSM-Tree 原理 | 3 分 | 架构描述正确，三大放大概念清晰 |
| 平响分析 | 3 分 | 能具体分析 SSD 延迟的来源和影响因素 |
| 调优方案 | 4 分 | 调优手段丰富且针对性强，有量化认知 |

---

## 七、综合开放题

### 题目 13：如果让你重新设计正排存储系统

**问题：**
基于你过去一年多的经验，如果让你从零设计一套商品正排存储系统，面对当前的规模和未来 3 倍增长预期，你会怎么设计？请从以下角度展开：
1. 存储格式与序列化方案
2. 冷热分层策略
3. 缓存架构
4. 成本与性能的平衡
5. 可扩展性和运维便利性

**参考答案要点：**

这是一道开放题，主要考察候选人的架构视野和总结反思能力。优秀回答应包含：

1. **存储格式：** 直接使用 FlatBuffers 或类似零拷贝格式，避免反序列化开销。配合字段级压缩（不同字段使用不同压缩策略）。

2. **冷热分层：** 三级架构 —— 热数据（全内存，FlatBuffers 原始格式）、温数据（内存压缩存储）、冷数据（SSD/RocksDB）。基于实时访问统计自动迁移。

3. **缓存架构：** 进程内 L1 Cache + 分布式 L2 Cache + 后端存储。每一层都有独立的淘汰策略和容量管理。预热机制必不可少。

4. **成本与性能平衡：** 基于业务 SLA 分级 —— 搜索主路径要求 P99 < 5ms，用全内存方案；推荐长尾场景可以容忍 P99 < 20ms，用 SSD 方案。通过差异化 SLA 实现整体成本最优。

5. **可扩展性：** 存储与计算分离架构，水平扩展无状态的查询节点，存储层独立扩容。配置化的字段剥离能力（如多语言标题可以灵活剥离或合并）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 系统性思考 | 3 分 | 方案完整覆盖了存储、缓存、分层等各方面 |
| 经验总结 | 3 分 | 能体现对过去项目的深度反思，说出"如果重来会怎么做不同" |
| 前瞻性 | 2 分 | 考虑了未来规模增长和技术演进 |
| 成本意识 | 2 分 | 能在性能和成本之间做合理取舍 |

---

### 题目 14：压力面 — 线上故障处理

**问题：**
场景：周五晚上 8 点（大促期间），正排集群的内存使用率突然从 75% 飙升到 95%，触发了 P0 告警。部分 Pod 已经开始 OOM 被 Kill。你是 On-Call 的 Owner，请描述你的应急处理流程。

**参考答案：**

1. **止血（前 5 分钟）：**
   - 立即查看监控大盘，确认影响范围（哪些集群、哪些 Pod、影响了多少 QPS）。
   - 如果是近期有变更上线，立即回滚。
   - 紧急扩容：增加 Pod 副本数或提升单 Pod 内存 limit。
   - 触发降级策略：如关闭非核心功能（多语言标题等低优先级字段的返回）。

2. **定位（5-30 分钟）：**
   - 检查是否有异常的数据更新（如某次全量更新推送了异常大的数据）。
   - 检查是否有热点请求导致缓存膨胀。
   - 分析内存 profile（如 jemalloc 的 heap profiling），确定内存增长的具体来源。
   - 与上下游沟通，确认是否有流量突增或异常请求模式。

3. **修复（30 分钟 - 数小时）：**
   - 根据根因制定针对性修复方案。
   - 如果是数据异常：修正数据后触发增量更新。
   - 如果是代码 bug：hotfix 修复并灰度发布。
   - 如果是容量不足：推动永久性扩容。

4. **复盘：**
   - 故障时间线、影响面、根因分析。
   - 预防措施：如何避免类似问题再次发生（加强监控、增加水位预警、改进变更流程等）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 应急反应 | 3 分 | 第一时间止血意识强，回滚/扩容/降级手段清晰 |
| 排查能力 | 3 分 | 排查思路有条理，能从多角度分析 |
| 沟通协调 | 2 分 | 提到了与团队、上下游的协作 |
| 复盘意识 | 2 分 | 有事后复盘和改进措施的意识 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | Protobuf → FlatBuffers 迁移 | 10 | ⭐⭐⭐ |
| 2 | 项目深挖 | 分级缓存架构设计 | 10 | ⭐⭐⭐⭐ |
| 3 | 项目深挖 | 冷热正排分离架构 | 10 | ⭐⭐⭐⭐ |
| 4 | 项目深挖 | GPU 检索架构与优化 | 10 | ⭐⭐⭐⭐⭐ |
| 5 | 系统设计 | 大规模 KV 存储设计 | 10 | ⭐⭐⭐⭐ |
| 6 | 系统设计 | Java → C++ 迁移决策 | 10 | ⭐⭐⭐ |
| 7 | C++ 基础 | 内存管理与性能优化 | 10 | ⭐⭐⭐ |
| 8 | C++ 基础 | 并发编程 | 10 | ⭐⭐⭐⭐ |
| 9 | 数据结构 | 布隆过滤器 | 10 | ⭐⭐⭐ |
| 10 | 算法 | 向量检索 ANN 算法 | 10 | ⭐⭐⭐⭐ |
| 11 | 计算机基础 | 操作系统与网络 | 10 | ⭐⭐⭐⭐ |
| 12 | 存储引擎 | RocksDB 深入 | 10 | ⭐⭐⭐⭐ |
| 13 | 综合开放 | 系统重新设计 | 10 | ⭐⭐⭐⭐⭐ |
| 14 | 综合开放 | 线上故障处理 | 10 | ⭐⭐⭐ |

**建议面试流程（60 分钟）：**
- 项目深挖（25 分钟）：从题目 1-4 中选 2 题，深入追问
- 技术基础（15 分钟）：从题目 7-8 中选 1 题，题目 9-10 中选 1 题
- 系统设计（15 分钟）：题目 5 或 13 选 1 题
- 应急能力（5 分钟）：题目 14

**综合评价维度：**
- **技术深度**（30%）：对核心技术原理的理解程度
- **工程能力**（30%）：实际项目中的设计、实施、优化能力
- **系统思维**（20%）：能否从全局角度思考问题
- **沟通表达**（10%）：技术问题的阐述是否清晰有条理
- **学习潜力**（10%）：对新技术的认知和思考深度
