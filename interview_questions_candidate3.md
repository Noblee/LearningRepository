# 面试题目集（候选人三）— 基于简历定制

> **候选人核心画像：** Go 后端开发工程师，天津大学硕士（计算机技术），字节跳动抖音电商用户增长方向，核心负责付费会员体系（省钱卡）、权益直塞系统、资损防控体系，具备高并发交易场景下的数据一致性保障经验。

---

## 一、项目深挖类

### 题目 1：省钱卡交易链路与一致性保障

**问题：**
你负责省钱卡的 C 侧交易、履约、逆向链路，并设计了多类一致性校验 FaaS：
1. 请描述省钱卡从用户下单到开卡成功的完整交易链路，涉及哪些系统和服务？
2. 你提到了"领券落表一致性"和"会籍 binlog 合法性校验"，分别是什么问题？具体的校验方案是怎样的？
3. 为什么选择 FaaS 来实现一致性校验？FaaS 在这个场景下相比常驻服务有什么优劣？
4. 高并发场景下（如大促），一致性校验本身会不会成为性能瓶颈？如何保障？

**参考答案：**

1. **交易链路：**
   - 用户侧：浏览省钱卡商品页 → 选择卡类型（月卡/季卡/续费卡）→ 确认下单 → 支付。
   - 交易侧：下单服务创建订单 → 调用支付网关 → 支付回调 → 履约服务触发开卡。
   - 履约侧：开通会籍（写入会籍表，状态变更）→ 权益发放（优惠券、免运费券等逐一发放）→ 更新用户标签。
   - 涉及系统：交易中台、支付系统、会籍服务、权益中台、券系统、用户标签系统、消息通知等。
   - 逆向链路：退卡 → 权益回收（已使用的不回收，未使用的回收）→ 退款 → 会籍状态变更。

2. **一致性问题与校验方案：**
   - **领券落表一致性：** 用户开卡后需要发放多张优惠券，可能出现"券系统发放成功但本地权益表未记录"或反之的不一致。
     - 校验方案：定时任务对比券系统的发放记录和本地权益落表记录，发现差异后触发补偿（补发或补记）。
     - 实时校验：发放链路中采用"先落本地表 → 再调券系统 → 回写状态"的事务模式，失败则进入重试队列。
   - **会籍 binlog 合法性校验：** 会籍表的变更（开通、续费、退卡）通过 binlog 同步到下游系统。需要校验 binlog 中的状态变更是否合法（如不能从"已退卡"变为"已退卡"，不能跳过中间状态）。
     - 校验方案：消费 binlog，根据预定义的状态机规则检查每条变更的合法性，违规则告警。

3. **FaaS 选型原因：**
   - 优势：(a) 按需执行，不占用常驻资源（校验是定时/事件触发的，非持续运行）；(b) 独立部署，不影响主链路性能；(c) 开发迭代快，每个校验逻辑独立为一个函数，互不影响；(d) 天然适合事件驱动（如 binlog 变更触发校验）。
   - 劣势：(a) 冷启动延迟（但对校验场景可接受，不在用户请求关键路径上）；(b) 执行时长受限（FaaS 通常有超时限制）；(c) 状态管理困难（需要外部存储）。

4. **高并发保障：**
   - 校验与主链路解耦：校验是异步执行的，不在交易关键路径上，不影响用户体验。
   - 采样校验：大促期间可调整为采样校验（如 10%），降低校验系统压力。
   - FaaS 弹性扩缩：根据触发频率自动扩缩实例。
   - 校验结果聚合：不逐条告警，而是按时间窗口聚合后批量告警，避免告警风暴。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 链路完整性 | 3 分 | 交易→履约→权益发放→逆向链路清晰，涉及系统完整 |
| 一致性方案 | 3 分 | 能描述具体的不一致场景和校验/补偿方案 |
| FaaS 选型 | 2 分 | 优劣分析到位，能说明为什么适合这个场景 |
| 高并发思考 | 2 分 | 解耦、采样、弹性等保障措施合理 |

---

### 题目 2：盲盒系统与续费卡设计

**问题：**
你将卡券体系升级为盲盒系统，并支持了续费卡功能：
1. "盲盒系统"的业务模型是什么？"每日解锁"的技术方案如何实现？如何保障用户权益不超领？
2. 续费卡的"无缝衔接体验"具体指什么？技术上如何实现？涉及哪些边界 case？
3. 你提到"通过延时 MQ + FaaS 保证跨系统数据一致性"，请详细描述这个方案。为什么用延时 MQ 而不是即时 MQ？

**参考答案：**

1. **盲盒系统：**
   - 业务模型：用户购买省钱卡后，每天可以"开盲盒"解锁一项权益（优惠券/折扣/专属价等），增加每日回访动力。
   - 每日解锁实现：
     - 权益池配置化：后台配置每天可解锁的权益池（按天数编排或随机池）。
     - 解锁状态管理：用户维度记录已解锁的天数和具体权益，存储在 Redis（快速查询）+ MySQL（持久化）。
     - 防超领方案：(a) Redis `SETNX` 原子性保证同一天只能解锁一次；(b) 数据库唯一索引 `(user_id, card_id, day_seq)` 做兜底；(c) 权益发放前校验当前已领取数量 ≤ 应领取上限。

2. **续费卡无缝衔接：**
   - 含义：用户当前会员即将到期时，续费卡的权益自动接续，不出现"断档"（如到期当天续费，新权益应立即生效，且不重复发放今天的权益）。
   - 技术实现：
     - 会籍期限拼接：续费后 `new_expire = max(current_expire, now) + duration`，而非从当前时间开始计算。
     - 权益去重：续费后新一轮权益发放需要跳过当天已领取的权益（查询解锁记录）。
     - 导购 SDK 同步：续费成功后实时刷新用户的会员状态和权益展示，避免页面显示"已过期"。
   - 边界 case：
     - 已过期 N 天后续费：需要决定是否补发过期期间的权益。
     - 支付中但未成功：需要锁定续费资格，避免重复下单。
     - 退卡后再续费：需要处理会籍状态回溯问题。

3. **延时 MQ + FaaS 方案：**
   - 场景：续费成功后，需要通知多个下游系统（会籍、权益、标签等），但各系统处理速度不一致，可能出现短暂不一致。
   - 方案：续费成功后立即发送即时 MQ 通知各下游处理。同时发送一条延时 MQ（如延时 5 分钟），触发 FaaS 进行一致性校验——检查所有下游系统是否都已正确处理（会籍是否续期、权益是否刷新、标签是否更新），未完成的触发补偿。
   - 为什么用延时 MQ：给各下游系统足够的处理时间后再校验，如果用即时 MQ，下游可能还没处理完，校验结果不准确，会产生大量误告警或无效补偿。
   - 延时时长选择：根据下游系统的 P99 处理时间设定（如最慢的系统 P99 = 3s，则延时 30s-60s 是安全的）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 盲盒系统设计 | 3 分 | 每日解锁实现清晰，防超领方案有层次（Redis + DB 双保险） |
| 续费衔接 | 3 分 | 无缝衔接逻辑正确，能识别边界 case |
| 延时 MQ 方案 | 4 分 | 方案描述完整，能说清延时而非即时的原因 |

---

### 题目 3：权益直塞系统设计

**问题：**
你从 0 搭建了权益直塞系统：
1. "直塞"与常规的"领取"模式有什么区别？为什么需要直塞？
2. 离线直塞（Hive/DA 数据）和事件直塞（页面访问/消费触发）的架构分别是怎样的？核心差异在哪里？
3. "保障权益不超发"是如何实现的？在百万级用户同时直塞的场景下，如何保证既不超发也不漏发？
4. 算法动态增发是怎么接入的？算法模型给出什么信号？你的系统如何响应？

**参考答案：**

1. **直塞 vs 领取：**
   - 领取模式：系统创建权益后等待用户主动领取（如弹窗"点击领取优惠券"）。优点是用户感知强，缺点是领取率受 UI 曝光和用户主动性影响。
   - 直塞模式：系统根据策略直接将权益放入用户账户，用户无需操作即可获得。优点是覆盖率高、转化效率高（直接进入"我的卡包"），缺点是需要更强的防超发和资损控制。
   - 需要直塞的原因：新客激励、老客回流等场景需要主动触达用户，等待用户自行领取的效率太低。

2. **两种直塞模式：**
   - **离线直塞：**
     - 数据源：Hive 离线计算出符合条件的用户列表（如近 30 天未购买的老客），由 DA（数据分析师）配置圈选规则。
     - 架构：Hive SQL → 产出用户列表 → 导入直塞任务系统 → CronJob 触发 → 批量调用权益发放接口。
     - 特点：T+1 延迟，批量大，适合活动预热、大规模首发。
   - **事件直塞：**
     - 数据源：用户实时行为事件（如访问商城首页、完成一笔消费）。
     - 架构：用户行为 → 消息队列（Kafka）→ 事件消费服务 → 判断是否满足直塞条件 → 实时调用权益发放接口。
     - 特点：实时触发，精准度高，适合行为驱动的增发场景。
   - 核心差异：触发时机（定时批量 vs 实时事件）、数据新鲜度（T+1 vs 实时）、并发模式（批量并发 vs 流式处理）。

3. **防超发方案：**
   - 全局库存控制：Redis 原子计数器记录已发放总量，`DECR` 到 0 时停止发放。
   - 用户级去重：Redis `SET user:{uid}:benefit:{id} NX EX ttl` 或 DB 唯一索引，保证同一用户同一权益只发一次。
   - 幂等发放：每次发放携带唯一业务 ID（如 `task_id + user_id`），下游权益系统基于此 ID 做幂等。
   - 防漏发：任务完成后执行对账（已发放用户数 vs 目标用户数），差异部分触发补发。
   - 百万级并发：分片 + worker pool 模式，将用户列表分片（如按 uid 哈希分 100 片），每片由独立 worker 处理，避免单点瓶颈。

4. **算法动态增发：**
   - 算法信号：算法模型基于用户画像、行为特征、ROI 预测输出"该用户是否应增发"及"增发什么权益"的决策。
   - 接入方式：(a) 直塞系统暴露 API 供算法服务调用；或 (b) 算法服务将增发决策写入消息队列，直塞系统消费后执行。
   - 系统响应：收到增发信号 → 校验库存和用户资格 → 发放权益 → 记录发放日志。
   - 安全兜底：即使算法判断需要增发，直塞系统仍需做库存、频次、金额等硬性校验，防止算法异常导致超发。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 模式理解 | 2 分 | 直塞 vs 领取的区别和适用场景清晰 |
| 架构设计 | 3 分 | 离线/事件两种模式架构完整，差异分析到位 |
| 防超发方案 | 3 分 | 多层保障（库存+去重+幂等+对账）层次分明 |
| 算法对接 | 2 分 | 接入方式合理，有安全兜底意识 |

---

### 题目 4：资损防控与对账系统

**问题：**
你从 0 到 1 搭建了资损对账与预警系统：
1. 对账系统的整体架构是什么？数据源有哪些？对账的核心逻辑是什么？
2. 你主动召回潜在资损风险 10+ 次，能否举一个具体的 case？从发现到处理的全过程？
3. "去除噪音报警"你是怎么做的？如何平衡报警的灵敏度和准确率？
4. 如果让你将这套资损防控体系推广到其他业务线，你会怎么抽象和设计？

**参考答案：**

1. **对账系统架构：**
   - 数据源：
     - 交易系统的订单数据（开卡、退卡记录）
     - 券系统的发放/核销记录
     - 会籍系统的状态变更记录
     - 资金系统的收付款记录
   - 对账逻辑：跨系统数据交叉校验。
     - 订单-会籍一致性：有开卡订单 → 必有对应会籍记录，且状态一致。
     - 会籍-权益一致性：有效会籍期间 → 权益应已全部发放。
     - 退卡-退款一致性：退卡成功 → 退款应在规定时间内到账。
     - 券发放-库存一致性：已发放券总量 ≤ 配置的库存上限。
   - 执行方式：定时任务（如每小时/每天）从各系统拉取数据，进行比对，差异记录写入异常表并触发告警。

2. **具体 case 示例：**
   - 发现：对账任务发现某批次优惠券的实际发放量超出了配置的库存上限（配置 10 万张，已发放 10.2 万张）。
   - 原因排查：并发场景下 Redis 库存扣减和 DB 记录存在时序差异，极端情况下库存校验被绕过。
   - 处理：立即暂停该券的发放 → 联系权益团队冻结多发的 2000 张券 → 修复并发校验逻辑（加入 Lua 脚本原子扣减）→ 上线后验证修复效果。
   - 事后：将该 case 沉淀为常态化对账规则。

3. **去除噪音报警：**
   - 分级报警：P0（资金损失确认）→ 即时电话/消息通知；P1（疑似异常）→ 告警群消息；P2（轻微偏差）→ 日报汇总。
   - 阈值动态调整：根据业务量动态计算阈值（如误差率 < 0.1% 不报警），而非固定绝对值。
   - 聚合与去重：相同类型的异常在时间窗口内聚合为一条告警。
   - 白名单机制：已知的、已确认无风险的差异（如时间窗口内的延迟同步）加入白名单，不再触发报警。
   - 定期复盘：每周 review 告警记录，将连续 N 天无效的告警规则标记为待优化。

4. **通用化设计：**
   - 抽象对账模型：数据源 A ↔ 数据源 B，定义比对规则（字段映射、匹配条件、容差范围）。
   - 配置化：不同业务线只需配置数据源连接、字段映射和比对规则，无需开发代码。
   - 插件化数据接入：支持 MySQL、Hive、API、MQ 等多种数据源接入方式。
   - 统一告警：对接统一的告警平台，支持多种通知渠道。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构设计 | 3 分 | 数据源和对账逻辑完整，跨系统校验思路清晰 |
| 实际 case | 3 分 | 能具体描述发现→排查→处理的全过程 |
| 噪音处理 | 2 分 | 分级、聚合、动态阈值等手段合理 |
| 通用化思维 | 2 分 | 能抽象出可复用的对账框架 |

---

## 二、高并发与数据一致性

### 题目 5：分布式数据一致性

**问题：**
你的项目中多次提到"数据一致性"，这是一个核心主题：
1. 在省钱卡开卡场景中，需要同时完成"创建会籍 + 发放 N 张券 + 更新用户标签"，如何保证这些操作的一致性？如果其中一步失败了怎么办？
2. 分布式事务有哪些实现方式？2PC、TCC、Saga 各自的原理和适用场景？你在项目中用的是哪种？
3. "最终一致性"在你们的业务中可以接受到什么程度？延迟 1 秒？1 分钟？1 小时？不同场景的容忍度分别是什么？

**参考答案：**

1. **开卡一致性方案：**
   - 不适合用分布式事务（涉及多个独立系统，2PC 的性能和可用性代价太大）。
   - **实际方案：本地事务 + 异步补偿（Saga 模式）**
     - 步骤一：本地事务创建会籍记录（状态：开通中）。
     - 步骤二：异步/同步调用券系统发放优惠券。成功则继续，失败则进入重试队列。
     - 步骤三：调用标签系统更新用户标签。
     - 步骤四：所有步骤完成后更新会籍状态为"已开通"。
     - 补偿：如果某一步在多次重试后仍失败，触发反向补偿（回收已发放的券、回退会籍状态）或人工介入。
     - 幂等保证：每一步都基于唯一业务 ID 做幂等，重试不会导致重复操作。

2. **分布式事务方式对比：**
   - **2PC（Two-Phase Commit）：**
     - 原理：协调者 → Prepare（所有参与者锁定资源）→ Commit/Rollback。
     - 优点：强一致性。
     - 缺点：同步阻塞、单点故障（协调者）、性能差、资源长时间锁定。
     - 适用：同一数据库内的跨表事务，或对一致性要求极高且参与者少的场景。
   - **TCC（Try-Confirm-Cancel）：**
     - 原理：Try（预留资源）→ Confirm（确认提交）→ Cancel（释放资源）。每个参与者实现这三个接口。
     - 优点：不锁资源（Try 阶段只预留），性能较好。
     - 缺点：开发成本高（每个操作需要实现 3 个接口），业务侵入性强。
     - 适用：资金相关的场景（如扣款、转账），需要预留和确认。
   - **Saga：**
     - 原理：将长事务拆分为一系列本地事务，每个本地事务有对应的补偿操作。正向执行全部成功则完成，某步失败则逆向执行已完成步骤的补偿。
     - 优点：无锁、无协调者、适合长流程。
     - 缺点：最终一致性（中间状态可见）、补偿逻辑可能复杂。
     - 适用：电商订单、开卡等涉及多个微服务的长流程。
   - **项目中的选择：** Saga 模式（本地事务 + 异步消息 + 补偿），因为开卡流程涉及多个独立系统，允许最终一致性。

3. **最终一致性容忍度：**
   - 开卡权益发放：容忍 **秒级~分钟级** 延迟。用户开卡后几秒到一分钟内看到权益到账是可接受的。
   - 退卡退款：容忍 **分钟级~小时级**。退款通常有"1-3 个工作日到账"的用户预期。
   - 会籍状态同步到导购页：容忍 **秒级**。用户开卡后立即刷新页面应看到会员标识。
   - 对账：容忍 **小时级~天级**。T+1 对账在大多数场景下足够。
   - 库存扣减：**零容忍**。库存必须实时准确（Redis 原子操作保证）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 开卡一致性 | 3 分 | 方案合理（Saga + 补偿 + 幂等），不过度设计 |
| 分布式事务 | 4 分 | 2PC/TCC/Saga 原理正确，选型与业务匹配 |
| 容忍度分析 | 3 分 | 能根据不同业务场景给出合理的一致性等级 |

---

### 题目 6：Redis 并发自旋锁与任务去重

**问题：**
你在 Diff SDK 中设计了"Redis TaskID + 并发自旋锁"来避免高流量下任务重复生成：
1. 请详细描述这个方案的实现。什么是"并发自旋锁"？与标准的 Redis 分布式锁有什么区别？
2. TaskID 是如何生成和使用的？如何保证全局唯一？
3. 在极端场景下（如 Redis 短暂不可用），你的方案会怎样？是否会导致任务重复或丢失？
4. 这套方案的性能如何？锁的持有时间和竞争频率是怎样的？

**参考答案：**

1. **方案实现：**
   - 场景：高并发 MQ 消息消费时，多个消费者可能同时处理相同业务实体的 Diff 任务，需要保证同一实体只生成一个 Diff 任务。
   - TaskID：基于业务实体的唯一标识（如 `diff:{entity_type}:{entity_id}:{version}`）生成。
   - 并发自旋锁：尝试获取锁失败后，不立即放弃，而是短暂等待后重试（自旋），避免频繁的 sleep/context switch。
     ```go
     func TryAcquireWithSpin(ctx context.Context, rdb *redis.Client, key string, maxRetry int) bool {
         for i := 0; i < maxRetry; i++ {
             ok, _ := rdb.SetNX(ctx, key, "1", ttl).Result()
             if ok {
                 return true
             }
             runtime.Gosched() // 让出 CPU 时间片
         }
         return false
     }
     ```
   - 与标准分布式锁的区别：(a) 自旋而非阻塞等待，适合锁持有时间极短的场景；(b) 不需要看门狗续期（任务生成是毫秒级操作）；(c) 更轻量，无需 Lua 脚本释放。

2. **TaskID 生成：**
   - 基于业务语义生成（非 UUID）：`diff:{entity_type}:{entity_id}:{timestamp_window}`。
   - timestamp_window 做时间窗口归一化（如按分钟取整），保证同一分钟内相同实体的任务 ID 相同。
   - 使用方式：`SETNX taskID → 成功则创建任务 → 失败则说明已有相同任务在处理`。

3. **Redis 不可用场景：**
   - 获取锁失败（Redis 超时）→ 降级策略：(a) 允许重复生成任务，下游做幂等去重；(b) 或直接跳过，依赖重试机制后续补偿。
   - 关键点：任务去重是优化手段而非强一致性要求，少量重复任务是可接受的（下游 Diff 对比本身具有幂等性）。

4. **性能指标：**
   - 锁持有时间：任务创建是写 DB/MQ 操作，通常 1-5ms，TTL 设为 10-30s（远大于持有时间，防止异常情况）。
   - 竞争频率：取决于相同实体的 MQ 消息并发度，通常较低（同一实体短时间内不会有大量变更）。
   - 自旋次数：通常 1-3 次即可获取锁，极少出现高竞争。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 方案实现 | 3 分 | 自旋锁实现清晰，与标准分布式锁的区别明确 |
| TaskID 设计 | 3 分 | 基于业务语义生成，有时间窗口归一化的思考 |
| 容错降级 | 2 分 | Redis 不可用时有降级策略，理解去重的非强制性 |
| 性能分析 | 2 分 | 能给出合理的性能估算 |

---

## 三、Go 语言与系统设计

### 题目 7：Go 并发与内存模型

**问题：**
1. 在你的直塞系统中，百万级用户的批量直塞如何利用 Go 的并发能力？请描述你的并发模型设计。
2. 以下代码在什么情况下会出问题？

```go
var m = make(map[string]int)

func main() {
    for i := 0; i < 100; i++ {
        go func(key string) {
            m[key] = 1
        }(fmt.Sprintf("key_%d", i))
    }
    time.Sleep(time.Second)
    fmt.Println(len(m))
}
```

3. Go 的 `context.Context` 在你的项目中是如何使用的？除了超时控制，还有哪些用法？

**参考答案：**

1. **批量直塞并发模型：**
   - 整体流程：加载用户列表 → 分片 → Worker Pool 并发处理 → 汇总结果。
   ```go
   const workerCount = 50
   const batchSize = 500

   func BatchPush(users []User, benefit Benefit) {
       taskCh := make(chan []User, workerCount)
       var wg sync.WaitGroup

       // 启动 worker pool
       for i := 0; i < workerCount; i++ {
           wg.Add(1)
           go func() {
               defer wg.Done()
               for batch := range taskCh {
                   pushBatch(batch, benefit)
               }
           }()
       }

       // 分片投递
       for i := 0; i < len(users); i += batchSize {
           end := min(i+batchSize, len(users))
           taskCh <- users[i:end]
       }
       close(taskCh)
       wg.Wait()
   }
   ```
   - 关键设计：(a) Worker 数量可配置，避免压垮下游；(b) 有界 channel 做背压控制；(c) 每个 batch 内部可以进一步并发（如调用券系统的批量接口）；(d) 失败重试 + 错误收集。

2. **代码问题：**
   - **Go map 非并发安全：** 多个 goroutine 并发写 map 会触发 `fatal error: concurrent map writes`，程序直接 panic（不是数据竞争那么简单，Go runtime 会主动检测并 crash）。
   - 修复方案：
     - 方案一：使用 `sync.Mutex` 保护 map 访问。
     - 方案二：使用 `sync.Map`（适合读多写少或 key 集合相对固定的场景）。
     - 方案三：预分配结果数组 `results := make([]int, 100)`，每个 goroutine 写自己的索引位置（无竞争）。
   - 额外问题：`time.Sleep` 等待不可靠，应使用 `sync.WaitGroup`。

3. **Context 的使用：**
   - **超时控制：** `context.WithTimeout`，控制下游 RPC/Redis/MySQL 调用的超时。
   - **取消传播：** 用户取消请求时，通过 context 取消传播给所有下游 goroutine，避免无效计算。
   - **请求级数据传递：** `context.WithValue` 传递请求 ID（traceID）、用户信息等。
   - **生命周期管理：** 在 worker pool 中通过 `ctx.Done()` 监听优雅退出信号。
   - **级联取消：** `errgroup.WithContext` 实现"一个失败全部取消"的语义。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 并发模型 | 4 分 | Worker Pool 设计合理，有背压控制和错误处理 |
| Map 安全问题 | 3 分 | 能指出 concurrent map writes 的 panic，给出修复方案 |
| Context 理解 | 3 分 | 多种用法覆盖全面，有实际项目中的案例 |

---

### 题目 8：SPI 化改造与 SDK 设计

**问题：**
你完成了 C 侧多触点的 SPI 化改造，并建设了省钱卡导购 SDK：
1. 什么是 SPI？SPI 化改造的目标和收益是什么？在你的场景中，SPI 的具体定义是什么？
2. 导购 SDK 的"原子校验能力"包括哪些？这些校验的执行顺序和短路策略是怎样的？
3. 多触点（直播间、短触区、支付成功页、商城首页）的差异如何在 SDK 中处理？如何在"通用性"和"定制化"之间取得平衡？

**参考答案：**

1. **SPI 与改造目标：**
   - SPI（Service Provider Interface）：定义一套标准接口，不同的业务方（触点）作为 Provider 实现该接口，SDK 通过 SPI 机制动态加载和调用。
   - 改造前：每个触点独立对接省钱卡服务，代码大量重复，新增触点需要重新开发。
   - 改造后：省钱卡核心逻辑下沉到 SDK，各触点只需实现 SPI 接口（如提供触点上下文、处理展示结果），SDK 处理通用逻辑（权益校验、实验命中、个性化推荐等）。
   - 收益：新触点接入从"周级开发"降至"天级配置"，核心逻辑统一维护，bug 修复和功能迭代一处生效。

2. **原子校验能力：**
   - 校验项：
     - 实验校验：用户是否命中 AB 实验（是否展示省钱卡入口）。
     - 人群校验：用户是否属于目标人群（新客/老客/流失用户等）。
     - 会员状态校验：用户是否已是会员（避免对已开卡用户展示购卡入口）。
     - 商品校验：当前商品是否支持省钱卡权益。
     - 库存校验：卡券库存是否充足。
     - 风控校验：用户是否命中风控策略（如黑名单）。
   - 执行顺序：按"过滤率从高到低"排列（先执行最可能过滤的校验），提前终止减少不必要的调用。例如：实验校验（过滤 50%）→ 人群校验（过滤 30%）→ 会员状态 → 商品 → 库存 → 风控。
   - 短路策略：任一校验不通过则直接返回不展示，后续校验不执行。

3. **通用性与定制化平衡：**
   - SDK 核心层：处理所有触点共有的逻辑（校验、数据查询、权益计算）。
   - 触点适配层（SPI 实现）：每个触点提供自己的上下文（如直播间 ID、商品 ID、页面类型），SDK 根据上下文做差异化处理。
   - 配置化差异：不同触点的展示样式、文案、校验规则可通过配置中心控制，无需代码修改。
   - 钩子（Hook）机制：SDK 在关键节点暴露 Hook（如 BeforeCheck、AfterRender），触点可注入自定义逻辑。
   - 原则："核心逻辑通用化，展示和交互定制化"。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| SPI 理解 | 3 分 | 概念清晰，改造前后对比明确 |
| 校验设计 | 4 分 | 校验项完整，执行顺序有优化考量 |
| SDK 架构 | 3 分 | 通用与定制的分层设计合理 |

---

## 四、MySQL 与存储

### 题目 9：MySQL 在高并发交易场景的应用

**问题：**
1. 省钱卡的会籍表你是怎么设计的？核心字段有哪些？索引如何设计？
2. 在退卡场景中，需要同时更新会籍状态 + 创建退款记录 + 回收权益，这三步如何保证在 MySQL 层面的一致性？
3. 如果会籍表数据量达到千万级，查询性能下降，你会怎么优化？

**参考答案：**

1. **会籍表设计：**
   ```sql
   CREATE TABLE membership (
       id BIGINT AUTO_INCREMENT PRIMARY KEY,
       user_id BIGINT NOT NULL,
       card_type TINYINT NOT NULL COMMENT '卡类型：月卡/季卡/年卡',
       status TINYINT NOT NULL COMMENT '状态：0-待开通 1-已开通 2-已过期 3-已退卡',
       order_id BIGINT NOT NULL COMMENT '关联订单',
       start_time DATETIME NOT NULL,
       expire_time DATETIME NOT NULL,
       auto_renew TINYINT DEFAULT 0 COMMENT '是否自动续费',
       created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
       updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
       UNIQUE KEY uk_user_order (user_id, order_id),
       KEY idx_user_status (user_id, status),
       KEY idx_expire_time (expire_time, status)
   ) ENGINE=InnoDB;
   ```
   - `uk_user_order`：保证同一用户同一订单不会重复开卡。
   - `idx_user_status`：根据用户 ID 和状态查询当前有效会籍。
   - `idx_expire_time`：定时任务扫描即将过期的会员（发续费提醒）。

2. **退卡一致性：**
   - 同一数据库内的操作用本地事务：
     ```go
     tx := db.Begin()
     // 1. 更新会籍状态
     tx.Model(&Membership{}).Where("id = ? AND status = 1", id).
         Update("status", 3)
     // 2. 创建退款记录
     tx.Create(&RefundRecord{...})
     // 3. 提交事务
     tx.Commit()
     ```
   - 权益回收（跨系统调用）：在事务提交成功后，异步发送 MQ 消息通知券系统回收权益。如果回收失败，通过重试 + 对账补偿。
   - 乐观锁防并发：`WHERE status = 1` 保证只有状态为"已开通"的记录才能被退卡，防止并发退卡。

3. **千万级优化：**
   - 分库分表：按 `user_id` 做分片（如 user_id % 64），分散到多个库/表。
   - 冷热分离：将已过期/已退卡的历史数据迁移到归档表，主表只保留有效会籍。
   - 读写分离：写操作走主库，读操作走从库（会员状态查询允许短暂延迟的场景）。
   - 缓存加速：用户当前会员状态缓存到 Redis（`user:{uid}:membership`），大多数查询不走 DB。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 表设计 | 3 分 | 字段合理，索引设计有针对性 |
| 事务处理 | 4 分 | 本地事务 + 异步补偿方案正确，有乐观锁等防护 |
| 大表优化 | 3 分 | 分库分表、冷热分离等方案具体可行 |

---

## 五、消息队列与中间件

### 题目 10：MQ 在复杂业务场景中的应用

**问题：**
你的项目中大量使用了 MQ（Kafka/RocketMQ），请结合实际场景回答：
1. 在省钱卡系统中，MQ 被用在哪些场景？每个场景选择 Kafka 还是 RocketMQ？为什么？
2. 延时 MQ 的底层实现原理你了解吗？RocketMQ 的延时消息是如何实现的？
3. 消费者消费速度跟不上生产速度（消息积压），你在直塞系统中遇到过吗？如何处理？
4. 消息的顺序性在你的业务中是否有要求？如果有，如何保证？

**参考答案：**

1. **MQ 使用场景与选型：**
   - 开卡/退卡事件通知（权益发放/回收、标签更新等）→ **RocketMQ**：需要事务消息（开卡成功才发消息）和延时消息（延时一致性校验），RocketMQ 原生支持。
   - 用户行为事件（页面访问、消费事件触发直塞）→ **Kafka**：高吞吐、低延迟，适合海量事件流处理。
   - Diff 任务分发 → **Kafka 或 RocketMQ** 均可：消息量不大但需要可靠投递。
   - 选型原则：需要事务消息/延时消息 → RocketMQ；纯高吞吐事件流 → Kafka。

2. **RocketMQ 延时消息原理：**
   - RocketMQ 支持 18 个延时级别（1s/5s/10s/30s/1m/2m/.../2h），不支持任意延时。
   - 实现：延时消息先投递到内部的 `SCHEDULE_TOPIC_XXXX` 主题，由 ScheduleMessageService 定时扫描，到期后转投到目标主题。
   - 如果需要任意延时：(a) 基于时间轮（Timing Wheel）自行实现；(b) 利用 Redis 的 Sorted Set（score 为到期时间戳）+ 定时扫描。
   - RocketMQ 5.x 已支持任意延时时间。

3. **消息积压处理：**
   - 发生过：大规模直塞活动时，百万用户的权益发放消息在短时间内涌入，消费者处理速度跟不上。
   - 处理方案：
     - 短期：增加消费者实例数（水平扩容），提高消费并发度。
     - 中期：优化单条消息处理速度（批量操作替代逐条操作、减少不必要的远程调用）。
     - 长期：生产侧限速（令牌桶控制消息发送速率），避免瞬时涌入超过消费能力。
     - 兜底：设置消费 Lag 告警，积压超阈值自动触发扩容。

4. **顺序性要求：**
   - 有要求的场景：会籍状态变更（开通→有效→过期→退卡），必须保证状态变更的顺序，否则可能出现"先过期后开通"的逻辑错误。
   - 保证方式：
     - Kafka：将同一用户的消息发到同一 Partition（按 user_id 做 key），Partition 内天然有序。
     - RocketMQ：使用 MessageQueueSelector 将同一用户的消息路由到同一 Queue。
   - 注意：消费失败重试时可能打破顺序，需要阻塞重试（失败时阻塞该 Partition 的消费直到重试成功），但这会影响吞吐，需权衡。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 场景选型 | 3 分 | 场景与 MQ 选型匹配，理由充分 |
| 延时原理 | 2 分 | 了解 RocketMQ 延时消息的实现机制 |
| 积压处理 | 3 分 | 短中长期方案完整，有实际经验 |
| 顺序性 | 2 分 | 理解 Partition 有序性和消费失败对顺序的影响 |

---

## 六、系统设计

### 题目 11：设计一个通用权益发放平台

**问题：**
基于你在权益直塞系统的经验，请设计一个通用的权益发放平台，要求：
- 支持多种权益类型（优惠券、红包、会员天数、积分等）
- 支持多种触发方式（手动发放、定时批量、事件驱动、算法决策）
- 百万级用户并发发放
- 不超发、不漏发、可追溯
- 支持多业务线接入

请从数据模型、核心流程、幂等与一致性、监控告警等方面展开。

**参考答案要点：**

1. **数据模型：**
   - 权益模板表（benefit_template）：权益类型、面额/数量、有效期规则、库存上限。
   - 发放任务表（push_task）：任务 ID、触发方式、目标人群规则、权益模板 ID、状态、进度。
   - 发放记录表（push_record）：用户 ID、任务 ID、权益 ID、发放时间、状态、幂等 key。
   - 库存表：全局库存 + 日库存 + 用户级频次限制。

2. **核心流程：**
   - 任务创建 → 人群圈选 → 库存预检 → 分片投递（MQ）→ Worker 消费 → 逐批发放 → 结果回写 → 对账校验。
   - 每一步都有状态流转和异常处理。

3. **幂等与一致性：**
   - 全局幂等 key：`{task_id}:{user_id}:{benefit_template_id}`。
   - 库存扣减：Redis Lua 原子操作，扣减成功后才调用下游发放接口。
   - 发放失败回补：Redis 库存 +1，记录回补日志。
   - 最终一致性：延时对账 FaaS 校验已发放数量和 Redis 库存的一致性。

4. **监控告警：**
   - 任务维度：进度（已处理/总量）、成功率、失败明细。
   - 系统维度：发放 QPS、延迟、下游成功率。
   - 资损维度：库存偏差、超发检测、重复发放检测。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 数据模型 | 3 分 | 实体设计完整，库存模型合理 |
| 流程设计 | 3 分 | 全流程覆盖，异常处理完善 |
| 一致性保障 | 2 分 | 幂等 + 库存原子扣减 + 对账方案 |
| 多业务接入 | 2 分 | 可扩展设计（权益类型插件化、触发方式配置化） |

---

## 七、故障排查与稳定性

### 题目 12：线上故障排查

**问题：**
场景一：某天大促期间，你的对账系统检测到省钱卡的某类优惠券发放数量超出了配置的库存上限 5%。此时活动仍在进行中，你如何处理？

场景二：用户反馈开了省钱卡但某项权益没到账（比如免运费券），应该怎么排查？

**参考答案：**

**场景一：券超发处理**

1. **立即止血（前 3 分钟）：**
   - 确认超发规模（5% 是多少张？金额多少？）。
   - 暂停该券的发放（Redis 库存设为 0 或关闭发放开关）。
   - 通知运营和产品，评估影响（已超发的券用户是否已使用）。

2. **排查根因（3-30 分钟）：**
   - 检查 Redis 库存扣减逻辑：是否有并发 bug（如非原子操作）？
   - 检查是否有多条发放链路绕过了库存校验（如补偿逻辑、手动发放等）？
   - 检查 MQ 消息是否有重复投递导致重复发放？
   - 检查幂等逻辑是否失效。

3. **处理方案：**
   - 已超发且未使用的券：通过券系统批量冻结。
   - 已使用的券：评估资损金额，走资损申报流程。
   - 修复 bug 后恢复发放，调整库存。

4. **事后：** 加强库存扣减的原子性、新增实时超发检测（而非依赖定时对账）。

**场景二：权益未到账排查**

1. 获取用户 ID + 订单号 → 查询会籍表确认开卡状态是否正常。
2. 查询权益发放记录表：该权益是否有发放记录？
   - 有记录但状态异常 → 查看失败原因（券系统报错？库存不足？）。
   - 无记录 → 履约服务是否收到了开卡事件？MQ 消息是否丢失？
3. 查看链路追踪（traceID）：定位具体在哪一步失败。
4. 确认后手动补发 + 根据根因修复代码/配置。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 止血速度 | 3 分 | 第一时间暂停发放，控制影响范围 |
| 排查深度 | 3 分 | 多角度排查，能定位到代码/配置级别 |
| 补偿方案 | 2 分 | 对已超发的处理方案合理 |
| 权益排查 | 2 分 | 逐层排查思路清晰（会籍→发放记录→MQ→链路） |

---

### 题目 13：压力面 — 系统稳定性设计

**问题：**
假设明天抖音有一个超大型促销活动，预计省钱卡的购买量是平日的 10 倍（从 1 万/小时到 10 万/小时），你作为省钱卡的核心开发，需要提前做哪些准备？请给出你的大促保障方案。

**参考答案：**

1. **容量评估与扩容：**
   - 梳理核心链路的各环节容量（Hertz 网关、Kitex 服务、Redis、MySQL 连接池）。
   - 按 10 倍峰值预估，各服务提前扩容（Pod 数 × 10 或根据压测结果调整）。
   - Redis 集群扩容：增加节点和内存，特别是库存相关的热 key 所在分片。
   - MySQL 主库评估写入 QPS 是否足够，必要时扩容读副本。

2. **全链路压测：**
   - 在预发布环境做全链路压测（下单→支付→开卡→权益发放）。
   - 关注 P99 延迟、错误率、DB 慢查询、Redis 热 key。
   - 压测暴露的问题逐一修复。

3. **预案准备：**
   - 降级预案：非核心功能（如盲盒、推荐）可一键关闭。
   - 限流预案：入口限流阈值预设，超过则排队或返回"稍后再试"。
   - 熔断预案：下游服务异常时自动熔断，返回降级结果。
   - 回滚预案：大促期间冻结代码变更，如有紧急 bug 可秒级回滚。

4. **监控加强：**
   - 核心指标大盘：QPS、延迟、成功率、库存水位、资损指标。
   - 告警阈值临时调整（大促量级下正常的波动不应触发告警）。
   - 值班安排：核心开发全程 On-Call，分工明确。

5. **缓存预热：**
   - 省钱卡商品信息、权益配置、实验配置等提前加载到 Redis。
   - 热点用户数据预加载（如会员状态）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 容量评估 | 3 分 | 链路梳理完整，扩容有量化依据 |
| 压测 | 2 分 | 有全链路压测意识和方法 |
| 预案完善度 | 3 分 | 降级/限流/熔断/回滚预案齐全 |
| 监控与值班 | 2 分 | 大盘、告警、值班安排到位 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | 交易链路与一致性校验 FaaS | 10 | ⭐⭐⭐⭐ |
| 2 | 项目深挖 | 盲盒系统与续费卡设计 | 10 | ⭐⭐⭐ |
| 3 | 项目深挖 | 权益直塞系统 | 10 | ⭐⭐⭐⭐ |
| 4 | 项目深挖 | 资损防控与对账系统 | 10 | ⭐⭐⭐ |
| 5 | 一致性 | 分布式数据一致性（2PC/TCC/Saga） | 10 | ⭐⭐⭐⭐ |
| 6 | 一致性 | Redis 并发自旋锁与任务去重 | 10 | ⭐⭐⭐ |
| 7 | Go 语言 | 并发模型、Map 安全、Context | 10 | ⭐⭐⭐ |
| 8 | 架构设计 | SPI 化改造与 SDK 设计 | 10 | ⭐⭐⭐⭐ |
| 9 | MySQL | 会籍表设计与高并发事务 | 10 | ⭐⭐⭐ |
| 10 | MQ | 消息队列深入应用 | 10 | ⭐⭐⭐⭐ |
| 11 | 系统设计 | 通用权益发放平台 | 10 | ⭐⭐⭐⭐ |
| 12 | 故障排查 | 线上问题排查与处理 | 10 | ⭐⭐⭐ |
| 13 | 稳定性 | 大促保障方案 | 10 | ⭐⭐⭐⭐ |

---

## 建议面试流程（60 分钟）

| 阶段 | 时间 | 内容 |
|------|------|------|
| 项目深挖 | 25 分钟 | 题目 1（必问，核心交易链路）+ 题目 3 或 4 选一 |
| 技术深度 | 15 分钟 | 题目 5（分布式一致性，必问）+ 题目 7（Go 并发，快速过） |
| 系统设计 | 15 分钟 | 题目 11（权益发放平台）或题目 8（SDK 设计） |
| 稳定性 | 5 分钟 | 题目 12 或 13 选一 |

---

## 综合评价维度

| 维度 | 权重 | 说明 |
|------|------|------|
| **数据一致性理解** | 25% | 核心能力——高并发交易场景下的一致性保障方案设计 |
| **工程落地能力** | 25% | 从 0 到 1 搭建系统、SDK 设计、框架抽象能力 |
| **系统稳定性意识** | 20% | 资损防控、对账、监控告警、大促保障等生产级思维 |
| **Go 语言基础** | 15% | 并发模型、内存安全、标准库使用 |
| **沟通与表达** | 10% | 技术问题阐述清晰度 |
| **业务理解** | 5% | 对电商会员、权益运营等业务的理解程度 |

---

## 面试官特别关注点

此候选人的简历有以下亮点值得深挖：

1. **"一致性"是高频词** —— 领券落表一致性、会籍 binlog 合法性、延时 MQ + FaaS 一致性保障、跨系统数据一致性。面试官应重点考察候选人对分布式一致性的**真实理解深度**，区分"背八股"和"真实践"。

2. **从 0 到 1 建设经验多** —— 直塞系统、对账系统、Diff SDK。考察候选人在设计初期如何做技术选型和取舍，而不仅仅是"怎么实现"。

3. **资损防控意识强** —— 主动召回风险 10+ 次。这是区分普通开发和优秀开发的重要维度，应肯定并深入了解。

4. **跨域重构经验** —— 从营销中心迁出，涉及架构迁移。考察候选人在大型重构中的角色、方法论和踩坑经验。
