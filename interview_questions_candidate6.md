# 面试题目集（候选人六）— 基于简历定制

> **候选人核心画像：** C++ 广告引擎工程师，快手约 2 年经验（23 届校招），核心负责搜索广告引擎（触发/召回/粗排/精排全链路）和品牌广告引擎（索引架构升级）。技术栈以 C++ 为主，兼顾 Go/Java/Python。有字节搜索和 Disney+ LTR 的实习经历。推动搜索广告日消耗从 800 万增长到 1800 万，品牌峰值收入 8000 万/天。

---

## 一、项目深挖类

### 题目 1：精排预估架构改造——模型预估图化

**问题：**
你主导了精排预估架构改造，这是简历中获奖的项目：
1. 精排模块改造前的架构是什么样的？存在什么问题？为什么需要"图化"？
2. "将特征处理、模型注册、模型请求与预估分解析拆分为算子"——算子的粒度怎么定？如何避免拆得太细导致调度开销过大？
3. "模型直连改造"具体改了什么？之前模型交互走了什么中间链路？为什么精简后能降 5-10ms？
4. "用户特征从本地缓存改为实时获取"——这不是增加了延迟吗？你怎么在实时性和性能之间做平衡的？

**参考答案：**

1. **改造前的问题：**
   - 精排模块代码高度耦合：特征拼装、模型请求、结果解析混在一个大函数中，修改一个模型的特征逻辑可能影响其他模型。
   - 模型数量多（搜索广告精排通常有 CTR、CVR、eCPM 等多个模型），每新增一个模型需要在代码中硬编码整个预估流程，开发周期长。
   - 不同业务线（搜索/品牌/开屏）各自维护一套精排逻辑，代码重复、质量参差不齐。
   - "图化"的目标：将预估流程拆解为标准化的算子（Operator），通过 DAG 编排组合，实现模块化、可复用、可配置。

2. **算子粒度：**
   - 粒度原则：**按功能语义划分，而非按代码行划分**。一个算子应该完成一个完整的功能单元。
   - 典型算子：`FeatureExtractor`（特征抽取）、`FeatureTransformer`（特征变换/交叉）、`ModelRequest`（模型请求构造）、`ModelPredict`（模型推理调用）、`ResultParser`（预估结果解析）、`BidCalculator`（出价计算）。
   - 避免调度开销：(a) 不把过细的操作拆成独立算子（如每个特征字段一个算子是不合理的）；(b) DAG 引擎（dragon 框架）在编译期做算子融合优化，减少运行时的调度次数；(c) 同进程内的算子间数据传递用零拷贝或引用传递，避免序列化开销。

3. **模型直连改造：**
   - 之前：广告引擎 → 模型代理服务（Model Proxy）→ 模型推理服务。中间的 Proxy 层做了路由、限流、日志等功能，但增加了一跳网络延迟（1-3ms）和序列化开销（2-5ms）。
   - 之后：广告引擎直接调用模型推理服务（gRPC 直连），把路由和限流逻辑下沉到 SDK。
   - 降低 5-10ms 的来源：(a) 减少一跳网络 RT（约 2-5ms）；(b) 减少一次序列化/反序列化（约 2-5ms）；(c) Proxy 层本身的处理延迟消除。

4. **本地缓存 → 实时获取的平衡：**
   - 问题：本地缓存的用户特征可能是几分钟前的旧数据，导致在线特征与离线训练特征不一致（特征穿越/漂移），影响模型精度 → 影响收入。
   - 实时获取确实增加了延迟（需要一次 RPC），但通过以下方式控制：
     - **异步预取：** 在召回/粗排阶段就异步请求用户特征，精排使用时已经返回。
     - **数据裁剪：** 只获取精排必需的特征子集（而非全量用户画像），减少数据量和传输耗时。
     - **超时降级：** 设置极短超时（如 5ms），超时则降级使用本地缓存旧数据。
   - 收益 > 成本：特征一致性提升带来的模型精度提升（收入+）远大于额外 RT 的损失。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 改造动机 | 2 分 | 能说清改造前的具体痛点（耦合、重复、效率低） |
| 算子设计 | 3 分 | 粒度划分合理，了解调度开销的控制 |
| 直连改造 | 3 分 | 能量化拆解 5-10ms 的来源 |
| 特征一致性 | 2 分 | 异步预取+裁剪+降级的平衡方案合理 |

---

### 题目 2：粗排架构重构——明暗投统一

**问题：**
你重构了搜索广告粗排，将明投暗投统一：
1. "明投"和"暗投"分别是什么？为什么搜索广告要区分明暗投？它们在召回和粗排阶段有什么本质差异？
2. "Python DSL + C++ 算子"的架构具体怎么工作？Python 和 C++ 各自负责什么？为什么不全用 C++？
3. "算子漂移"将粗排服务上移至 Matcher 层——什么是算子漂移？为什么要"上移"而不是"下沉"？
4. "先合并后统一排序"相比"分别排序后合并"，为什么能提升消耗 1.19%？这个收益的来源是什么？

**参考答案：**

1. **明投 vs 暗投：**
   - **明投（精准匹配/关键词广告）：** 广告主主动出价投放某些关键词，用户搜索这些词时触发广告。明确的 Query→Ad 映射关系。
   - **暗投（智能匹配/扩展匹配）：** 系统自动将广告匹配到语义相关但广告主未显式出价的 Query 上，扩大广告的覆盖面。
   - 区分的原因：明投是广告主明确意图，匹配精度高但覆盖面有限；暗投是系统智能扩展，覆盖面广但精度较低。两者的召回策略、匹配逻辑和出价机制不同。
   - 粗排差异：明投粗排更依赖关键词匹配质量和出价；暗投粗排更依赖语义相关性和预估 eCPM。改造前，两者的粗排策略各自独立实现和迭代，导致策略割裂。

2. **Python DSL + C++ 算子：**
   - C++ 算子：承载计算密集型逻辑（特征计算、模型推理、打分排序等），性能敏感，必须用 C++。
   - Python DSL：描述算子的编排流程（哪些算子串行/并行、条件分支、参数配置等），作为粗排流程的"剧本"。
   - 工作方式：Python DSL 在离线被编译为执行计划（DAG 描述文件）→ 加载到 C++ 引擎运行时 → 引擎按 DAG 调度 C++ 算子执行。
   - 为什么不全用 C++：C++ 编写编排逻辑效率低（编译慢、表达繁琐），Python DSL 让算法/策略同学可以快速修改编排流程而不需要重新编译 C++ 服务，将粗排迭代周期从"天级"缩短到"小时级"。

3. **算子漂移：**
   - 概念：不改变算子代码本身，只将算子从一个服务层（粗排服务）移动到另一个服务层（Matcher 层），改变执行位置。
   - 为什么上移：原本粗排在召回之后独立运行，明暗投的候选集在粗排前已经分开处理。将粗排算子上移到 Matcher 层（召回与粗排的衔接层），可以在明暗投候选集**合并之后**再做统一排序，而不是各自排序后再合并。
   - 上移而非下沉：下沉是将逻辑推向更后端（远离用户），上移是将逻辑推向更前端（更接近合并点），这里需要在合并点之后排序，所以是"上移粗排到合并点所在的 Matcher 层"。

4. **统一排序的收益来源：**
   - 问题：分别排序后合并时，明投和暗投各自有独立的排序标准和截断逻辑。可能出现：明投中排名靠后的低 eCPM 广告进入了最终结果，而暗投中高 eCPM 的广告因为暗投内部截断被淘汰了。
   - 统一排序后：所有候选广告在同一个 eCPM 标尺下竞争，全局最优的广告更容易胜出 → 整体 eCPM 更高 → 消耗（收入）提升。
   - 本质：**从局部最优变为全局最优**。类似于将两个独立排序的数组合并后重新排序，选出的 TopK 一定不比分别选 TopK/2 再合并更差。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 明暗投理解 | 2 分 | 概念准确，能说清两者的业务差异和召回差异 |
| DSL + 算子架构 | 3 分 | Python/C++ 分工合理，理解"策略同学可快速迭代"的价值 |
| 算子漂移 | 3 分 | 概念清晰，上移的目的（在合并点后统一排序）解释正确 |
| 收益分析 | 2 分 | "局部最优→全局最优"的逻辑链正确 |

---

### 题目 3：OneModel——端到端生成式模型落地

**问题：**
你从 0 到 1 搭建了搜索广告端到端生成式模型：
1. OneModel 的核心思想是什么？与传统的"Query→关键词匹配→广告"的检索范式有什么本质区别？
2. Query Tokenization 具体做了什么？为什么不直接用原始 Query 作为模型输入？
3. 生成式模型推理耗时高，你设计的"异步请求 + 后链路 Wait + 结果缓存"方案具体如何工作？如何保证缓存命中率？
4. Sid（语义 ID）到 Photo 的倒排用的是 Roaring Bitmap，为什么选 Roaring Bitmap 而不是普通 Bitmap 或 HashSet？

**参考答案：**

1. **OneModel 核心思想：**
   - 传统范式：Query → 分词/改写 → 关键词匹配倒排索引 → 候选广告。依赖手工构建的关键词体系，覆盖面受限于关键词库。
   - OneModel 范式：Query → 生成式模型直接推理出语义 ID（Sid）→ Sid 检索倒排索引 → 候选广告。模型端到端学习 Query 到广告的映射关系，突破关键词的限制。
   - 本质区别：从**基于规则的离散匹配**（关键词）转变为**基于模型的语义匹配**（端到端学习）。能发现关键词体系无法覆盖的 Query-Ad 关联，挖掘更多变现机会。

2. **Query Tokenization：**
   - 将自然语言 Query 转化为模型可以理解的 token 序列。
   - 不直接用原始 Query 的原因：(a) 原始 Query 是字符串，模型需要数值化的输入；(b) 需要处理同义词、错别字、大小写等归一化；(c) Token 粒度影响模型效果（字级别 vs 词级别 vs subword）。
   - 可能使用 BPE/SentencePiece 等 subword tokenization，平衡词汇量和语义粒度。

3. **异步请求 + Wait + 缓存：**
   ```
   触发阶段（早期）            后链路（使用结果时）
   ┌─────────────────┐     ┌─────────────────┐
   │ 异步发起模型推理  │     │ Wait 模型结果     │
   │ （不阻塞主流程）  │     │ （有超时控制）     │
   │        ↓         │     │        ↓         │
   │ 模型结果写入缓存  │     │ 读缓存获取 Sid    │
   └─────────────────┘     └─────────────────┘
          ↑                        ↓
          └───── 同一请求内共享 ─────┘
   ```
   - 触发阶段（链路早期）异步发起模型推理请求，主流程继续执行其他召回策略。
   - 模型推理完成后，结果写入请求级缓存（如内存中的 ConcurrentMap，key 为 Query hash）。
   - 后链路需要 Sid 时，从缓存读取。如果模型尚未返回，则 Wait（带超时）。
   - 缓存命中率保障：(a) 触发阶段尽早发起异步请求，给模型足够的推理时间；(b) "针对 OneModel 链路调整触发阶段耗时"——可能是将模型请求前置到更早的阶段；(c) 相同 Query 的请求可以复用缓存（跨请求级别的 Query 维度缓存）。

4. **Roaring Bitmap 选型：**
   - 普通 Bitmap：如果物料 ID 范围很大（如 0~10 亿）但实际物料数较少（如百万级），普通 Bitmap 需要 10 亿 bit ≈ 120MB 内存，绝大部分是 0，浪费严重。
   - HashSet：内存效率低（每个元素需要对象头+hash+指针），且不支持高效的集合运算（交集/并集）。
   - Roaring Bitmap：将 ID 空间按高 16 位分桶，每个桶根据密度选择不同存储方式（稀疏用有序数组，密集用位图，连续用 Run-Length 编码）。内存极优，且支持高效的集合运算（AND/OR），非常适合倒排索引的交集/并集操作。
   - 在广告场景中：一个 Sid 对应的广告集合可能几十到几万不等，Roaring Bitmap 在各种密度下都有良好表现。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| OneModel 思想 | 3 分 | 从关键词匹配到端到端语义匹配的范式转变清晰 |
| Tokenization | 2 分 | 理解 tokenization 的必要性和选型考量 |
| 异步缓存方案 | 3 分 | 方案完整，缓存命中率的保障措施合理 |
| Roaring Bitmap | 2 分 | 与普通 Bitmap/HashSet 的对比准确 |

---

### 题目 4：品牌广告索引架构升级

**问题：**
你参与了品牌广告索引架构升级到 AdTable：
1. 品牌广告的索引与效果广告的索引有什么区别？为什么需要独立的索引架构？
2. "Schemafree 化"是什么意思？为什么能将索引更新从分钟级提升到秒级？
3. "Bitmap Affinity（位图亲和）"技术是什么？它如何优化查询性能？CPU 利用率优化 7pp 是怎么做到的？
4. 多条业务线（信息流/开屏/搜索品专/PLC）的平滑迁移，你是怎么保证"业务效果完全打平"的？

**参考答案：**

1. **品牌 vs 效果广告索引：**
   - 效果广告：物料量级大（百万~亿级），以 eCPM 竞价为核心，索引更新频率高（实时出价变动）。
   - 品牌广告：物料量级小（万~百万级），以合约排期为核心（如"某品牌包断明天开屏位"），但定向条件复杂（人群包/地域/时段/设备等多维组合），且对投放准确性要求极高（合约承诺的曝光量必须完成）。
   - 独立索引的原因：品牌广告的定向查询模式与效果广告不同（效果广告以关键词/向量为核心，品牌广告以多维定向交叉为核心），通用索引框架无法高效支撑。

2. **Schemafree 化：**
   - 之前：索引的字段结构是固定的（Schema），新增一个字段需要修改索引定义 → 重建索引 → 全量分发，周期长。
   - Schemafree 后：索引以 KV 形式存储（如 Protobuf Any 或 JSON），新增字段只需在数据源加字段并在应用层解析，无需修改索引 Schema 或重建。
   - 秒级更新的原因：(a) Schemafree 避免了 Schema 变更引发的全量重建；(b) 基于 Datalink 统一框架实现增量数据实时推送（binlog 监听 → 增量更新索引），取代了之前的定时全量构建。

3. **Bitmap Affinity（位图亲和）：**
   - 品牌广告的定向查询本质是多维条件的交集：`地域=北京 AND 年龄=25-35 AND 设备=iOS AND 时段=20:00-22:00`。
   - Bitmap Affinity：将每个定向条件的匹配结果表示为 Bitmap，多个条件的交集 = 多个 Bitmap 的 AND 运算。"亲和"指的是在内存布局上让相关的 Bitmap 尽量在同一个 Cache Line 中，减少 CPU Cache Miss。
   - CPU 优化 7pp 的来源：(a) Bitmap 位运算替代逐条遍历的条件判断，CPU 指令数大幅减少；(b) SIMD 指令加速 Bitmap AND 运算；(c) 内存布局优化减少 Cache Miss。

4. **平滑迁移验证：**
   - **双读对比：** 迁移期间同时从旧索引和新索引（AdTable）读取数据，对比结果是否一致（字段值 Diff）。
   - **灰度切流：** 按比例（1% → 10% → 50% → 100%）将流量从旧索引切到新索引，每阶段观察业务指标（消耗、CTR、填充率）。
   - **AB 实验：** 实验组用新索引，对照组用旧索引，统计分析业务效果是否有显著差异。
   - **回滚能力：** 切流通过配置中心控制，发现异常可秒级切回旧索引。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 品牌 vs 效果 | 2 分 | 索引需求差异理解正确 |
| Schemafree | 3 分 | 概念清晰，与秒级更新的因果关系正确 |
| Bitmap Affinity | 3 分 | 原理正确，CPU 优化的来源能具体分析 |
| 迁移验证 | 2 分 | 双读/灰度/AB/回滚方案完整 |

---

### 题目 5：触发白盒系统

**问题：**
你从 0 到 1 搭建了触发 Trace 链路和白盒系统：
1. "触发阶段 Trace 盲区"具体指什么？之前排查触发问题时遇到什么困难？
2. 基于 Kafka + Flink 的 Trace 链路架构是怎样的？如何保证 Trace 信息不影响主链路性能？
3. 触发漏斗的可视化具体展示了什么？你是怎么设计这个视图的，让产研运营都能看懂？

**参考答案：**

1. **Trace 盲区：**
   - 之前排查"为什么某个广告没有被触发"时，只能看到最终结果（触发了/没触发），但不知道在触发过程的哪个环节被过滤掉了——是关键词没匹配上？是相关性过滤掉了？是多样性截断了？是去重合并时被淘汰了？
   - 困难：排查一个 case 需要翻多个模块的日志、对照多处配置，耗时长且容易遗漏。

2. **Trace 链路架构：**
   ```
   广告引擎（触发模块）
      ↓ 每个阶段输出 Trace 事件（异步、非阻塞）
   Kafka（Trace Topic）
      ↓
   Flink（实时聚合、加工、关联请求上下文）
      ↓
   存储层（ES/ClickHouse，支持查询）
      ↓
   白盒前端（可视化漏斗）
   ```
   - 不影响主链路：(a) Trace 信息异步写入 Kafka（fire-and-forget），不等待确认；(b) Trace 数据量大时可采样（如 1% 采样率）；(c) Trace 写入失败不影响广告返回。

3. **漏斗可视化设计：**
   - 输入：一个具体的 Query + 一条具体的广告（或一个广告主）。
   - 展示：该广告在触发链路各阶段的状态（通过/被过滤/被截断），以及具体的过滤原因。
   - 漏斗视图：`总候选 → 关键词匹配 → 相关性过滤 → 多样性过滤 → 去重合并 → 粗排截断 → 最终触发`，每一层显示通过数量和被淘汰数量。
   - 让非技术人员看懂：使用业务语言（如"因为相关性评分低于阈值被过滤"而非"score < 0.3 filter"），提供下钻能力（点击某层可以看具体的过滤明细）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 痛点理解 | 3 分 | Trace 盲区的具体场景描述清楚 |
| 架构设计 | 4 分 | Kafka+Flink 链路完整，异步/采样保证不影响主链路 |
| 可视化设计 | 3 分 | 漏斗视图直观，考虑了非技术用户的使用体验 |

---

## 二、广告系统核心概念

### 题目 6：广告检索引擎核心概念

**问题：**
作为广告引擎工程师，请回答以下核心概念：
1. eCPM 是什么？如何计算？为什么广告系统以 eCPM 作为排序标准而不是出价？
2. 广告的"一价"和"二价"计费模型分别是什么？你在品牌广告中做的"一价升级二价"解决了什么问题？
3. 什么是 Ad Load？"N 出 M 多样性策略"如何平衡 Ad Load 和用户体验？
4. 搜索广告的"内循环"和"外循环"分别是什么？

**参考答案：**

1. **eCPM（effective Cost Per Mille）：**
   - 定义：每千次展示的期望收入。
   - 计算：`eCPM = pCTR × pCVR × Bid × 1000`（简化版，不同场景公式略有不同）。
   - 为什么不用出价排序：纯出价排序会让高出价但低质量（低点击率）的广告胜出，用户体验差 → 用户流失 → 平台长期收入下降。eCPM 综合了出价和广告质量（点击率/转化率），平衡了短期收入和长期生态健康。

2. **一价 vs 二价：**
   - 一价（First Price）：广告主支付自己的出价。问题：广告主有动机不断降低出价试探底线，导致不稳定。
   - 二价（Second Price/Vickrey）：胜出者支付第二名的出价 + 最小增量。鼓励广告主报出真实意愿价格（真值策略均衡）。
   - 品牌广告"一价升级二价"：之前品牌广告用一价，导致广告主博弈行为严重（频繁调价），平台收入不稳定。升级二价后，广告主的最优策略是报真实价格，竞价更稳定，胜出率和曝光量都提升了。

3. **Ad Load 与 N 出 M：**
   - Ad Load：广告在内容中的占比（如每 10 条内容中有 2 条广告，Ad Load = 20%）。
   - N 出 M 策略：用户每 M 次请求中最多展示 N 次广告（如 5 次请求最多 3 次有广告），通过动态控制展示频率，降低对敏感用户（高频使用用户）的广告干扰。
   - 平衡方式：对高敏感人群降低 N/M 比例，对低敏感人群保持或提高。同时设置 CPM 门槛——只有 eCPM 足够高的广告才展示，低质量广告不展示，保证展示的广告有足够价值。

4. **内循环 vs 外循环：**
   - 内循环：广告主在快手站内生态中的投放和消费闭环（如快手小店的商品广告，用户在快手内完成浏览→点击→购买→复购）。
   - 外循环：广告主将流量导向外部（如跳转到外部网页、下载 App），用户行为离开快手生态。
   - 区别：内循环数据可追踪（转化数据可回流），模型优化更精准；外循环数据难追踪，需要依赖归因技术。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| eCPM | 3 分 | 公式正确，理解"质量×出价"的平衡逻辑 |
| 计费模型 | 3 分 | 一价/二价区别清晰，理解二价的博弈论基础 |
| Ad Load | 2 分 | N 出 M 策略原理正确 |
| 内外循环 | 2 分 | 概念准确，知道数据可追踪性的差异 |

---

## 三、C++ 与性能优化

### 题目 7：C++ 在广告引擎中的应用

**问题：**
你负责百万级 QPS 的 C++ 广告引擎：
1. 在广告引擎中，哪些数据结构是性能敏感的？你是怎么选型的？
2. 引擎中如何管理内存？有没有用到内存池或 Arena 分配器？为什么？
3. 如何做到服务的热更新（如索引更新、配置更新）而不重启进程？
4. 你在做性能优化时，常用的 profiling 工具和方法有哪些？

**参考答案：**

1. **性能敏感的数据结构：**
   - **倒排索引：** 核心数据结构。用 Roaring Bitmap 或压缩的有序数组存储 posting list，支持高效的集合交/并运算。
   - **正排索引：** 广告物料的属性数据（标题、出价、定向条件等）。用列式存储（Column-Oriented）或 FlatBuffers 实现零拷贝访问，减少反序列化开销。
   - **特征存储：** 用户/广告特征。用紧凑的 KV 结构（如 Robin Hood HashMap 或 Swiss Table），减少内存占用和查找延迟。
   - **排序：** 粗排/精排的候选集排序。小规模用 `std::partial_sort`（部分排序），大规模用 TopK 堆。

2. **内存管理：**
   - **Arena 分配器：** 广告引擎中每个请求的生命周期明确（请求开始→处理→返回），请求内产生的临时对象可以在 Arena 上分配，请求结束后一次性释放。避免频繁 `new/delete` 的碎片和锁开销。
   - **对象池：** 频繁创建的固定大小对象（如候选广告结构体）使用对象池复用。
   - **jemalloc/tcmalloc：** 替代 glibc malloc，减少多线程下的锁竞争。
   - 原因：广告引擎 QPS 高、请求处理链路中对象分配频繁，默认分配器的开销在 profiling 中会占到 5-10%。

3. **热更新：**
   - **索引热更新：** 双 Buffer 机制——维护两份索引（active + standby），新索引构建完成后写入 standby，原子切换指针让 standby 变为 active。正在处理中的请求仍使用旧索引（引用计数），请求完成后旧索引释放。
   - **配置热更新：** 监听配置中心变更事件，收到后在内存中更新配置。使用 RCU（Read-Copy-Update）模式——读不加锁，写时复制新版本并原子替换指针。
   - **模型热更新：** 类似索引的双 Buffer，模型文件加载到内存后原子切换。

4. **Profiling 工具：**
   - **perf：** Linux 性能分析工具，采样 CPU 热点函数，生成火焰图。
   - **gperftools（pprof）：** Google 的 CPU 和内存 profiler，集成到 tcmalloc 中。
   - **Valgrind/AddressSanitizer：** 内存泄漏和越界检测。
   - **火焰图（Flame Graph）：** 直观展示 CPU 时间分布，快速定位热点。
   - **自定义埋点：** 在引擎关键路径埋入计时器（高精度时钟 `std::chrono::high_resolution_clock`），统计各阶段耗时分布。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 数据结构 | 3 分 | 选型有针对性，理解广告场景的特殊需求 |
| 内存管理 | 3 分 | Arena/对象池/tcmalloc 的使用场景合理 |
| 热更新 | 2 分 | 双 Buffer + RCU 等机制清晰 |
| Profiling | 2 分 | 工具链丰富，有实际使用经验 |

---

### 题目 8：低代码化与 AST 表达式

**问题：**
你提到"基于 AST 表达式增强 dragonfly 的 select_item 表达能力，推动动态 pipeline 热更新"：
1. AST（抽象语法树）在这里是怎么用的？表达式引擎的工作原理是什么？
2. "动态 pipeline 热更新"——pipeline 的哪些部分可以动态更新？热更新的边界在哪里（什么能热更新什么不能）？
3. "模型配置化接入从天级缩短至分钟级"——之前接入一个新模型需要做哪些事情？现在简化为什么？

**参考答案：**

1. **AST 表达式引擎：**
   - 用户通过 DSL 编写规则表达式（如 `if model.ctr > 0.05 then select(ad) else drop(ad)`）。
   - 系统将表达式解析为 AST → AST 编译为可执行代码（或解释执行）→ 运行时对每条广告执行表达式求值。
   - 增强 select_item 表达能力：原来只能用简单的条件（如字段 > 阈值），增强后支持复杂表达式（如多字段组合运算、函数调用、嵌套条件）。

2. **热更新边界：**
   - 可热更新：(a) 模型接入配置（接哪个模型、特征如何组装、结果如何解析）；(b) 排序表达式（select_item 的逻辑）；(c) 过滤条件和截断阈值。
   - 不可热更新：(a) 算子本身的 C++ 代码（需要重新编译部署）；(b) 底层数据结构变更；(c) 涉及内存布局变化的修改。
   - 本质：**数据和配置**可以热更新，**代码逻辑**需要重新部署。低代码化的目标是把尽可能多的"代码逻辑"转变为"配置/表达式"。

3. **模型接入简化：**
   - 之前（天级）：写 C++ 代码定义特征拼装逻辑 → 写模型请求构造逻辑 → 写结果解析逻辑 → 代码 Review → 编译 → 部署 → 灰度验证。
   - 现在（分钟级）：在配置平台填写模型地址、特征映射规则（用表达式描述）、结果解析规则 → 提交 → 热更新生效 → 灰度验证。
   - 核心变化：特征拼装和结果解析从 C++ 硬编码变为表达式配置，无需编译部署。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| AST 原理 | 3 分 | 解析→编译→执行的流程清晰 |
| 热更新边界 | 4 分 | 能区分数据/配置（可热更）和代码（需部署）的边界 |
| 接入简化 | 3 分 | 前后对比清晰，简化的核心变化准确 |

---

## 四、系统设计

### 题目 9：设计一个搜索广告检索引擎

**问题：**
如果让你从零设计一个搜索广告检索引擎，要求：
- 支持百万级广告物料
- QPS 10 万，P99 < 100ms
- 支持关键词匹配 + 语义匹配
- 支持实时广告上下线
- 支持多种排序模型

请给出整体架构设计。

**参考答案要点：**

1. **整体链路：** Query 理解 → 召回（多路：关键词倒排 + 向量 ANN + 人群定向）→ 粗排（轻量模型截断到千级）→ 精排（复杂模型打分到百级）→ 机制层（频控/去重/多样性/竞价）→ 返回。

2. **索引设计：** 正排（FlatBuffers/列存，广告属性）+ 倒排（Roaring Bitmap，关键词→广告集合）+ 向量索引（HNSW/IVF，语义召回）。双 Buffer 热更新。

3. **排序框架：** DAG 编排引擎，算子化设计，支持模型配置化接入和 AB 实验。

4. **实时更新：** 增量索引（binlog → Kafka → 在线服务消费并更新索引），支持秒级广告上下线。

5. **性能保障：** Arena 内存分配、Bitmap 并行过滤、模型推理批量化、超时降级。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 链路完整性 | 3 分 | 召回→粗排→精排→机制层完整 |
| 索引设计 | 3 分 | 正排+倒排+向量索引，热更新方案 |
| 排序框架 | 2 分 | 可配置、可扩展、支持 AB |
| 性能设计 | 2 分 | 有针对百万 QPS 的具体优化手段 |

---

## 五、稳定性与故障排查

### 题目 10：广告引擎稳定性

**问题：**
1. 广告引擎与普通后端服务相比，在稳定性上有什么特殊要求？
2. 搜索广告请求如果超时了，对业务的直接影响是什么？你怎么控制超时？
3. 品牌广告投放了一个 8000 万/天的开屏合约，如果引擎故障导致曝光不足，后果是什么？你怎么保障？

**参考答案：**

1. **广告引擎特殊要求：**
   - **资损直接：** 广告引擎故障 = 直接收入损失（非广告服务故障可能只影响体验但不影响收入）。
   - **时延极敏感：** 广告需要在内容加载时同步返回（如搜索结果页），100ms 的超时意味着这次展示机会直接丢失。
   - **准确性要求高：** 计费相关（曝光/点击计数）必须准确，多计或少计都是资损。
   - **全链路依赖多：** 一个广告请求涉及特征服务、模型服务、预算服务、频控服务等多个下游，任何一个慢都会影响整体。

2. **超时控制：**
   - 全链路超时预算分配：总预算 100ms → 召回 30ms + 粗排 15ms + 精排 40ms + 机制 15ms。
   - 各阶段独立超时控制，超时后立即返回当前已有的候选集（降级而非失败）。
   - 下游调用有独立超时（如模型推理 20ms，超时则用缓存的旧预估值）。
   - 超时率监控：各阶段超时率作为核心指标，超过阈值告警。

3. **品牌合约保障：**
   - 后果：合约曝光不足 → 违约赔偿（可能赔付广告费的数倍）→ 品牌客户流失 → 商誉损失。
   - 保障措施：
     - 冗余部署：品牌广告独立集群，不与效果广告共享资源，避免互相影响。
     - 流量保障：品牌广告优先级高于效果广告，库存分配时优先保证品牌的曝光量。
     - 离线预演：提前模拟合约期间的流量分配，确认库存是否充足。
     - 实时监控：合约消耗进度实时监控，消耗落后时自动调整 CPM 门槛（降低门槛增加曝光）。
     - 应急补量：极端情况下人工介入调整投放策略，确保合约完成。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 特殊要求 | 3 分 | 资损/时延/准确性/依赖链的特殊性理解到位 |
| 超时控制 | 3 分 | 预算分配+逐级降级+监控方案完整 |
| 合约保障 | 4 分 | 冗余/优先级/预演/监控/补量多重保障 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | 精排图化改造（算子设计+直连+特征一致性） | 10 | ⭐⭐⭐⭐ |
| 2 | 项目深挖 | 粗排重构（明暗投统一+DSL+算子漂移） | 10 | ⭐⭐⭐⭐⭐ |
| 3 | 项目深挖 | OneModel 生成式模型（异步缓存+Roaring Bitmap） | 10 | ⭐⭐⭐⭐ |
| 4 | 项目深挖 | 品牌索引升级（Schemafree+Bitmap Affinity） | 10 | ⭐⭐⭐⭐ |
| 5 | 项目深挖 | 触发白盒系统（Kafka+Flink Trace） | 10 | ⭐⭐⭐ |
| 6 | 广告概念 | eCPM/一二价/Ad Load/内外循环 | 10 | ⭐⭐⭐ |
| 7 | C++ | 数据结构选型/内存管理/热更新/Profiling | 10 | ⭐⭐⭐⭐ |
| 8 | 架构 | AST 表达式/热更新边界/低代码化 | 10 | ⭐⭐⭐⭐ |
| 9 | 系统设计 | 搜索广告检索引擎设计 | 10 | ⭐⭐⭐⭐⭐ |
| 10 | 稳定性 | 广告引擎稳定性/超时/品牌合约保障 | 10 | ⭐⭐⭐ |

---

## 建议面试流程（60 分钟）

| 阶段 | 时间 | 内容 |
|------|------|------|
| 项目深挖 | 25 分钟 | 题目 2（粗排重构，**必问**，技术含量最高）+ 题目 1 或 3 选一 |
| 广告基础 | 10 分钟 | 题目 6（快速过，验证广告领域理解） |
| 技术深度 | 15 分钟 | 题目 7（C++ 性能）或 题目 8（低代码/AST） |
| 设计/稳定 | 10 分钟 | 题目 9 或 10 选一 |

---

## 综合评价维度

| 维度 | 权重 | 说明 |
|------|------|------|
| **广告引擎核心能力** | 30% | 召回/粗排/精排全链路理解，倒排/Bitmap/向量索引等核心技术 |
| **架构设计能力** | 25% | DAG 编排/图化改造/索引架构升级等系统级设计 |
| **C++ 工程能力** | 20% | 内存管理、热更新、性能 profiling 等系统编程功底 |
| **业务理解** | 15% | eCPM、竞价机制、漏斗优化等广告商业逻辑 |
| **成长潜力** | 10% | 23 届校招到现在 2 年，成长速度和学习能力 |

---

## 面试官特别关注点

1. **粗排重构（题目 2）是区分度最高的项目** —— "明暗投统一""算子漂移""Python DSL + C++ 算子""全局最优 vs 局部最优"等概念串联在一起，能完整说清楚的人一定对广告引擎有深入理解。

2. **OneModel（题目 3）体现了前沿技术落地能力** —— 生成式模型在广告中的应用是行业前沿，候选人从 0 到 1 搭建并取得了实际收益（消耗+1.79%），值得深挖异步缓存方案和耗时优化的工程细节。

3. **候选人只有 2 年经验但项目密度极高** —— 精排改造 + 粗排重构 + OneModel + 触发白盒 + 品牌索引升级 + 多项业务迭代，需要验证每个项目的参与深度。建议用"随机深挖一个项目的一个技术细节"来验证真实性。

4. **需要关注"做了什么"vs"理解了什么"** —— 23 届工程师在大厂广告团队 2 年，可能很多工作是在 leader 指导下完成的。追问"为什么这么做""还有没有其他方案""你的 leader 给了什么建议"可以区分独立思考能力和执行能力。
