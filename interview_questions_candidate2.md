# 面试题目集（候选人二）— 基于简历定制

> **候选人核心画像：** Go 后端开发工程师，3 年经验（字节跳动 + 华为），擅长微服务架构、缓存体系设计、并发编程，有 MCN 机构平台和活动线上化等业务系统从 0 到 1 的落地经验。

---

## 一、项目深挖类（考察对自身项目的理解深度与技术决策能力）

### 题目 1：MCN 机构平台高可用架构

**问题：**
你提到 MCN 机构平台支撑 7K+ QPS、服务可用性 99.999%，请详细回答：
1. 99.999% 可用性意味着全年停机时间约 5 分钟，你们是如何度量和保障这个指标的？具体做了哪些高可用措施？
2. Hertz（HTTP）和 Kitex（RPC）各自承担什么职责？为什么不统一用一种协议？请画出请求从用户到数据库的完整链路。
3. 7K+ QPS 在你们的场景下瓶颈在哪里？如果要扩展到 70K QPS，你的架构需要做哪些改动？

**参考答案：**

1. **可用性保障：**
   - 度量方式：通常基于成功请求比例计算（成功请求数 / 总请求数），排除计划内维护窗口。也可能基于分钟级可用性统计（每分钟成功率 > 阈值则该分钟可用）。
   - 高可用措施：
     - **多实例部署 + 负载均衡：** 服务多副本部署，通过 Service Mesh 或内部负载均衡组件分发流量。
     - **熔断降级：** Kitex 内置或集成 Sentinel 等熔断器，在下游异常时快速失败，避免级联故障。
     - **超时控制：** 全链路设置合理的超时时间，避免慢请求拖垮服务。
     - **限流：** 入口层限流保护，防止突发流量打垮服务。
     - **缓存兜底：** 核心接口有缓存兜底策略，DB 故障时可降级返回缓存数据。
     - **灰度发布 + 快速回滚：** 变更上线采用灰度策略，异常可秒级回滚。
     - **告警 + On-Call 机制：** 多维度监控告警，保证及时响应。

2. **Hertz 与 Kitex 的分工：**
   - **Hertz（HTTP）：** 面向外部客户端（Web/App），作为 BFF（Backend For Frontend）层，处理鉴权、参数校验、协议转换等。
   - **Kitex（RPC/Thrift）：** 内部微服务间通信，采用 Thrift 二进制协议，性能优于 HTTP JSON，适合高频内部调用。
   - 不统一的原因：外部需要 HTTP REST 兼容性，内部追求高性能低延迟；Kitex 的 Thrift 编解码比 JSON 快数倍，且支持连接多路复用。
   - 完整链路：Client → Hertz API Gateway → Kitex Service A → Redis Cache → MySQL / Kitex Service B → ...

3. **扩展到 70K QPS：**
   - 水平扩容计算节点（无状态服务直接加 Pod）。
   - 缓存命中率优化：提升 Redis 缓存命中率到 95%+ 以上，减少穿透到 DB 的请求。
   - MySQL 分库分表：单库可能成为瓶颈，需按机构 ID 分片。
   - 热点数据本地缓存（进程内 Cache）：减少 Redis 网络调用。
   - 读写分离升级：引入更多只读副本。
   - 异步化：非核心链路进一步异步化（MQ 削峰）。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 可用性理解 | 3 分 | 能准确定义可用性指标，措施全面且有层次 |
| 架构分层 | 3 分 | Hertz/Kitex 职责清晰，链路完整，协议选型有理有据 |
| 扩展性思考 | 4 分 | 能识别瓶颈并给出针对性的扩展方案 |

---

### 题目 2：机构 Pack 接口多 Loader 并发优化

**问题：**
你提到机构 pack 接口采用多 loader 并发架构，接口耗时从数百 ms 优化到 30ms：
1. 请详细描述"多 loader 并发架构"的设计。每个 loader 是什么？如何编排？如何收集结果？
2. 如果某个 loader 超时或失败，如何处理？是否会导致整个请求失败？
3. 并发 loader 之间如果存在数据依赖（如 loader B 需要 loader A 的结果），你怎么处理？
4. 优化前后的性能瓶颈分别是什么？30ms 的耗时是如何拆解的？

**参考答案：**

1. **多 Loader 并发架构：**
   - 每个 Loader 负责加载一类数据源（如机构基本信息、达人列表、业绩指标、资质信息等），是一个独立的数据获取单元。
   - 使用 `sync.WaitGroup` 或 `errgroup.Group` 编排多个 goroutine 并行执行所有 Loader。
   - 每个 Loader 将结果写入共享的 response 结构的不同字段（无数据竞争），或通过 channel 汇总。
   - 整体耗时 = max(各 Loader 耗时)，而非串行的 sum。

   ```go
   g, ctx := errgroup.WithContext(ctx)
   g.Go(func() error { return loadBasicInfo(ctx, &resp) })
   g.Go(func() error { return loadCreatorList(ctx, &resp) })
   g.Go(func() error { return loadMetrics(ctx, &resp) })
   if err := g.Wait(); err != nil { ... }
   ```

2. **超时/失败处理：**
   - 非核心 Loader（如运营标签、扩展信息）：失败则降级返回空值或默认值，不影响主流程。
   - 核心 Loader（如基本信息）：失败则整个请求失败，返回错误。
   - 通过 `context.WithTimeout` 控制每个 Loader 的超时时间。
   - `errgroup` 的 `WithContext` 模式可以在任一 goroutine 出错时取消其他 goroutine。

3. **数据依赖处理：**
   - DAG 编排：将 Loader 按依赖关系组织为有向无环图，先执行无依赖的 Loader，完成后再执行有依赖的 Loader。
   - 分阶段执行：第一阶段并发执行独立 Loader，第二阶段用第一阶段的结果并发执行依赖 Loader。
   - 也可以使用 channel 做同步：被依赖的 Loader 完成后通过 channel 通知依赖方。

4. **性能拆解：**
   - 优化前：串行调用 5-8 个数据源，每个 50-100ms，累计 400-800ms。
   - 优化后：并发执行，耗时 ≈ 最慢的 Loader（约 20-25ms）+ 结果组装（约 5ms）= ~30ms。
   - 瓶颈转移：从"串行调用延迟累加"变为"最慢的单个数据源延迟"。进一步优化需要针对最慢的 Loader 做缓存或预计算。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构设计 | 3 分 | Loader 抽象清晰，并发编排方式合理 |
| 容错设计 | 3 分 | 有核心/非核心区分，降级策略完善 |
| 依赖处理 | 2 分 | 能处理 Loader 间的依赖关系 |
| 性能分析 | 2 分 | 能做延迟拆解，识别优化前后的瓶颈 |

---

### 题目 3：MQ 异步导出方案设计

**问题：**
你设计了"MQ 异步分发 + 专属离线消费服务"来处理大规模数据导出：
1. 请描述该方案的完整架构，从用户发起导出请求到最终下载文件的全流程。
2. 如果导出任务执行到一半失败了（如服务重启），如何保证数据不丢失、不重复？
3. 如何处理超大文件导出（如百万行 CSV）的内存问题？
4. 导出任务的优先级如何设计？如果大量导出任务堆积，如何保证系统稳定？

**参考答案：**

1. **全流程架构：**
   - 用户发起导出请求 → 主服务校验参数，生成导出任务记录（写入 MySQL，状态为 pending）→ 发送消息到 MQ。
   - 专属离线消费服务消费 MQ 消息 → 更新任务状态为 processing → 分批查询数据库 → 拼装 CSV/Excel → 上传至对象存储（如 TOS/S3）→ 更新任务状态为 completed，记录下载 URL。
   - 用户在前端轮询或推送通知获取导出结果，点击下载链接。
   - 隔离优势：导出服务独立部署，CPU/内存密集的导出任务不影响主服务的 QPS 和延迟。

2. **可靠性保障：**
   - 消息确认（ACK）机制：消费者在任务完全完成后才 ACK，失败则消息重新投递。
   - 任务幂等性：通过任务 ID 做幂等，重复消费不会产生重复文件。
   - 断点续传：在任务表中记录已处理的 offset/页码，服务重启后可从断点恢复。
   - 死信队列（DLQ）：多次失败的消息进入死信队列，人工介入处理。

3. **大文件内存优化：**
   - 流式写入：不在内存中拼装完整文件，而是分批查询 + 流式写入临时文件或直接流式上传到对象存储。
   - 分批查询：每次只查 1000-5000 条记录，处理完写入后释放内存，再查下一批。
   - 使用 `csv.Writer` 或 Excel 流式写入库（如 excelize 的 StreamWriter）。

4. **任务优先级与稳定性：**
   - 多队列/多 Topic：高优先级（管理员导出）和低优先级（普通用户导出）使用不同队列，高优先级队列有更多消费者。
   - 并发控制：限制同一用户的并发导出任务数（如最多 3 个），避免单用户占用过多资源。
   - 消费者限速：消费服务设置最大并发任务数，防止资源耗尽。
   - 队列积压告警：监控消费 Lag，积压超阈值触发告警和自动扩容。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 架构完整性 | 3 分 | 全流程描述完整，隔离设计清晰 |
| 可靠性设计 | 3 分 | ACK、幂等、断点续传等机制考虑周全 |
| 内存优化 | 2 分 | 流式处理思路正确 |
| 运维稳定性 | 2 分 | 优先级、限流、监控方案合理 |

---

### 题目 4：达人活动配置化平台设计

**问题：**
你将达人活动从 20+ 人日降到 1 人日，核心是配置化能力的建设：
1. 请描述你是如何抽象"活动"模型的？一个活动有哪些核心实体和配置项？
2. 活动的奖励结算流程是怎样的？如何保证结算的准确性和幂等性？
3. 百万级达人圈选 20 分钟完成，具体的技术方案是什么？瓶颈在哪里？
4. 配置化系统如何保证灵活性的同时不引入过高的复杂度？遇到过哪些配置化的坑？

**参考答案：**

1. **活动模型抽象：**
   - 核心实体：活动（Activity）、规则（Rule）、任务（Task）、奖励（Reward）、参与者（Participant）。
   - 活动配置项：时间范围、目标人群（圈选规则）、参与条件、任务类型与目标（如完成 N 单、GMV 达到 X 元）、奖励类型与金额（现金/券/流量）、结算周期等。
   - 公共模块：人群圈选引擎、进度计算引擎、奖励结算引擎、消息通知模块。
   - 可配置属性：将差异化的部分（如不同活动的计算公式、判定条件）抽象为规则表达式或策略模式。

2. **奖励结算流程与保障：**
   - 流程：定时任务触发 → 查询活动参与者列表 → 逐一计算任务完成进度 → 判定是否达标 → 生成结算单 → 调用发放接口 → 更新状态。
   - 准确性：(a) 进度数据来源于 Hive 离线计算（T+1）或实时指标，双向校验；(b) 结算前做数据一致性检查。
   - 幂等性：(a) 每次结算生成唯一结算单 ID，发放接口基于该 ID 做幂等；(b) 状态机控制：待结算 → 结算中 → 已结算，不允许重复流转；(c) 分布式锁防止并发结算。

3. **百万级达人圈选：**
   - 方案：基于 Redis 的实时标签 + Hive/CK 的离线指标做条件过滤。
   - 流程：先用离线数据在 CK 中做初步过滤（如近 30 天活跃、品类匹配），缩小范围到十万级 → 再用实时指标做精细筛选。
   - 并发处理：将达人列表分片，多个 goroutine 并行执行圈选逻辑。
   - 瓶颈：(a) CK 查询的 IO 和计算延迟；(b) 圈选条件复杂时的 CPU 消耗；(c) Redis 批量查询的网络开销（pipeline 优化）。

4. **配置化的平衡与坑：**
   - 灵活性控制：不追求万能配置，而是覆盖 80% 常见场景，剩余 20% 允许代码扩展。
   - 踩过的坑：(a) 配置项过多导致运营理解成本高，需要配合文档和校验；(b) 配置变更缺乏版本管理，难以回溯；(c) 规则冲突检测不完善导致线上异常；(d) 过度抽象导致代码可读性下降。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 模型抽象 | 3 分 | 实体关系清晰，配置化设计合理 |
| 结算可靠性 | 3 分 | 幂等、状态机、一致性保障完善 |
| 圈选方案 | 2 分 | 技术方案清晰，能识别瓶颈 |
| 配置化反思 | 2 分 | 对配置化的边界有清醒认知，踩过坑能说出教训 |

---

## 二、Go 语言深入

### 题目 5：Goroutine 与 Channel 并发模型

**问题：**
1. Goroutine 的调度模型（GMP）是什么？当一个 goroutine 发生系统调用阻塞时，调度器如何处理？
2. 你提到"基于 goroutine 与 channel 构建健壮的并发任务执行框架"，请描述该框架的设计。如何控制 goroutine 的生命周期？如何防止 goroutine 泄漏？
3. 以下代码有什么问题？如何修复？

```go
func process(tasks []Task) []Result {
    results := make([]Result, len(tasks))
    for i, task := range tasks {
        go func() {
            results[i] = execute(task)
        }()
    }
    return results
}
```

**参考答案：**

1. **GMP 模型：**
   - **G（Goroutine）：** 用户态协程，栈大小初始 2KB（可动态扩缩），极轻量。
   - **M（Machine/Thread）：** 操作系统线程，真正执行代码的实体。
   - **P（Processor）：** 逻辑处理器，数量默认等于 CPU 核数（GOMAXPROCS），持有本地运行队列。
   - 调度流程：P 从本地队列取 G 绑定到 M 上执行。本地队列空时从全局队列或其他 P 的队列窃取（work stealing）。
   - 系统调用阻塞时：M 被阻塞，P 会与该 M 解绑，寻找或创建新的 M 来运行队列中的其他 G。阻塞的 M 在系统调用返回后，尝试重新获取 P，获取不到则将 G 放入全局队列，M 进入休眠。

2. **并发任务执行框架设计：**
   - Worker Pool 模式：固定数量的 worker goroutine 从任务 channel 中消费任务。
   - 生命周期控制：通过 `context.Context` 传递取消信号，worker 监听 `ctx.Done()` 优雅退出。
   - 防止泄漏：(a) 所有 goroutine 都有退出条件（context 取消或 channel 关闭）；(b) 使用 `sync.WaitGroup` 等待所有 worker 退出；(c) 有界 channel 防止无限制提交任务。
   - 错误收集：通过 error channel 或 `errgroup` 收集 worker 的错误。

   ```go
   func RunWorkerPool(ctx context.Context, workers int, tasks <-chan Task) {
       var wg sync.WaitGroup
       for i := 0; i < workers; i++ {
           wg.Add(1)
           go func() {
               defer wg.Done()
               for {
                   select {
                   case <-ctx.Done():
                       return
                   case task, ok := <-tasks:
                       if !ok { return }
                       task.Execute()
                   }
               }
           }()
       }
       wg.Wait()
   }
   ```

3. **代码问题：**
   - **闭包变量捕获问题：** `i` 和 `task` 是循环变量，goroutine 闭包捕获的是变量的引用而非值，所有 goroutine 执行时 `i` 和 `task` 可能已经是最后一个值。
   - **无同步机制：** 没有等待所有 goroutine 完成就直接 return，返回的 results 可能是空的。
   - **数据竞争：** 多个 goroutine 并发写 results 切片（虽然写不同索引，但因为 `i` 的捕获问题可能写同一个索引）。

   修复：
   ```go
   func process(tasks []Task) []Result {
       results := make([]Result, len(tasks))
       var wg sync.WaitGroup
       for i, task := range tasks {
           wg.Add(1)
           go func(idx int, t Task) {
               defer wg.Done()
               results[idx] = execute(t)
           }(i, task)
       }
       wg.Wait()
       return results
   }
   ```
   注：Go 1.22+ 中 `for` 循环变量语义已变更（每次迭代创建新变量），但面试中应展示对该问题的理解。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| GMP 模型 | 3 分 | 三个组件角色清晰，阻塞处理流程正确 |
| 框架设计 | 3 分 | Worker Pool 设计合理，生命周期管理和泄漏防护到位 |
| 代码分析 | 4 分 | 能指出闭包捕获、缺少同步、数据竞争三个问题并正确修复 |

---

### 题目 6：Go 内存与性能

**问题：**
1. Go 的 GC 机制是怎样的？三色标记法的过程？STW（Stop The World）在什么阶段发生？
2. 在高 QPS 场景下，Go GC 可能成为性能瓶颈。你在项目中是否遇到过 GC 相关的问题？有哪些优化手段？
3. `sync.Pool` 的原理和适用场景是什么？它和对象池有什么区别？在什么情况下使用它会适得其反？

**参考答案：**

1. **Go GC 三色标记法：**
   - 三色：白色（未访问）、灰色（已发现但子节点未访问）、黑色（已访问且子节点已处理）。
   - 过程：(a) 初始所有对象为白色；(b) 将根对象（栈、全局变量）标为灰色；(c) 取一个灰色对象，将其标为黑色，将其引用的白色对象标为灰色；(d) 重复直到无灰色对象；(e) 清除所有白色对象。
   - 写屏障（Write Barrier）：GC 过程中并发执行的 mutator 可能修改引用关系，写屏障保证不会丢失存活对象。Go 使用混合写屏障（Hybrid Write Barrier）。
   - STW 发生在：(a) GC 开始时的标记准备阶段（开启写屏障，极短）；(b) 标记终止阶段（关闭写屏障，极短）。大部分标记和清除工作与用户代码并发执行。

2. **GC 优化手段：**
   - 减少堆分配：(a) 尽量使用栈分配（小对象、不逃逸的变量）；(b) 使用 `sync.Pool` 复用临时对象；(c) 预分配切片/Map 容量，避免频繁扩容。
   - 调整 GOGC：增大 GOGC 值（默认 100）可以减少 GC 频率，但会增加内存占用。
   - 使用 `go tool pprof` 分析堆分配热点（`alloc_objects`/`alloc_space`）。
   - 结构体字段对齐优化，减少内存浪费。
   - 字节切片复用：对于频繁的序列化/反序列化，复用 buffer。
   - Go 1.19+ 的 `runtime/debug.SetMemoryLimit` 可设定软内存上限。

3. **sync.Pool：**
   - 原理：每个 P 有一个私有对象和共享链表，Get 时先从私有获取，再从共享链表，最后从其他 P 偷取，都没有则 New。Put 时放回。GC 时 Pool 中的对象会被全部清除。
   - 适用场景：频繁创建和销毁的临时对象（如 buffer、临时结构体），生命周期短。
   - 与对象池的区别：对象池是持久化复用，Pool 中的对象可能被 GC 回收，不保证持久性。
   - 适得其反的场景：(a) 对象创建成本很低时，Pool 的 Get/Put 开销可能超过直接 new；(b) 对象很大且 Pool 中堆积，可能占用大量内存直到下次 GC；(c) 对象需要复杂的重置逻辑时，容易引入 bug。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| GC 机制 | 4 分 | 三色标记过程正确，STW 阶段清晰，了解写屏障 |
| 优化手段 | 3 分 | 手段丰富且实用，最好有实际项目中的案例 |
| sync.Pool | 3 分 | 原理正确，适用边界清晰 |

---

## 三、缓存与 Redis

### 题目 7：缓存最佳实践框架

**问题：**
你沉淀了组内缓存使用的最佳实践框架并推广使用：
1. 这个框架解决了哪些痛点？核心提供了哪些能力？
2. 缓存穿透、缓存击穿、缓存雪崩分别是什么？你的框架是如何应对这三种问题的？
3. 缓存与数据库的一致性问题你是怎么处理的？Cache Aside、Read/Write Through、Write Behind 这几种模式的区别和适用场景？
4. 分布式锁在你的业务中是如何使用的？Redis 分布式锁有哪些需要注意的点？

**参考答案：**

1. **框架核心能力：**
   - 痛点：开发者直接操作 Redis 容易出现常见问题（如缓存穿透、热 key 未处理、过期时间不合理、缓存与 DB 不一致等）。
   - 核心能力：
     - 统一的缓存读写入口（封装 Get/Set/Del）。
     - 内置空值缓存（防穿透）和布隆过滤器（可选）。
     - Singleflight 防击穿（同一 key 的并发请求只有一个穿透到 DB）。
     - 随机过期时间（防雪崩）。
     - 自动降级（Redis 异常时降级到 DB 直查）。
     - 监控指标埋点（命中率、延迟、异常等）。

2. **三大缓存问题：**
   - **缓存穿透：** 查询不存在的数据，缓存无法命中，请求直达 DB。应对：缓存空值（短 TTL）+ 布隆过滤器前置拦截。
   - **缓存击穿：** 热点 key 过期瞬间，大量请求同时穿透到 DB。应对：Singleflight 或互斥锁（只允许一个请求回源）+ 热 key 永不过期（后台异步刷新）。
   - **缓存雪崩：** 大量 key 同时过期，请求集中打到 DB。应对：过期时间加随机偏移 + 多级缓存 + 限流降级。

3. **一致性处理：**
   - **Cache Aside（旁路缓存）：** 读：先读缓存 → 未命中读 DB → 写缓存。写：先更新 DB → 删除缓存。最常用，适合读多写少。
   - **Read/Write Through：** 应用只与缓存交互，缓存层负责与 DB 同步。实现复杂但一致性更好。
   - **Write Behind（异步回写）：** 写入只更新缓存，异步批量写回 DB。性能最好但有数据丢失风险。
   - 实际业务中多用 Cache Aside + 延迟双删（先删缓存 → 更新 DB → 延迟再删一次缓存），降低不一致窗口。

4. **Redis 分布式锁：**
   - 使用场景：结算、库存扣减等需要串行化的操作。
   - 注意点：
     - `SET key value NX PX timeout` 原子获取锁，value 为唯一标识（UUID），释放时用 Lua 脚本 CAS 删除（防止误删他人的锁）。
     - 合理设置锁超时时间，太短可能任务未完成锁就过期，太长可能影响其他请求等待。
     - 看门狗（Watchdog）机制：持有锁时后台线程自动续期，防止业务执行时间超过锁超时。
     - 红锁（RedLock）：多节点部署下需要多数节点加锁成功才算获取锁（但业界有争议）。
     - 可重入性：如果业务需要，需额外实现。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 框架设计 | 3 分 | 核心能力完整，解决的痛点明确 |
| 三大问题 | 3 分 | 定义准确，应对方案具体可行 |
| 一致性理解 | 2 分 | 多种模式对比清晰，能结合业务选型 |
| 分布式锁 | 2 分 | 关键注意点覆盖（唯一值、Lua 脚本、超时、续期） |

---

## 四、MySQL 与数据层

### 题目 8：MySQL 索引设计与慢查询优化

**问题：**
1. 请解释 MySQL InnoDB 的聚簇索引和二级索引的区别。什么是"回表"？什么是"覆盖索引"？如何利用覆盖索引优化查询？
2. 联合索引 `(a, b, c)` 在以下查询中哪些能命中索引？
   - `WHERE a = 1 AND b = 2 AND c = 3`
   - `WHERE a = 1 AND c = 3`
   - `WHERE b = 2 AND c = 3`
   - `WHERE a = 1 AND b > 2 AND c = 3`
   - `WHERE a = 1 ORDER BY b`
3. 线上发现一个慢查询（执行时间 > 2s），你的排查和优化流程是什么？
4. 你提到了读写分离，主从延迟在你的业务中如何处理？

**参考答案：**

1. **聚簇索引 vs 二级索引：**
   - 聚簇索引：InnoDB 的主键索引，B+ 树叶子节点存储完整行数据。每张表只有一个。
   - 二级索引：非主键索引，叶子节点存储主键值。查询时先在二级索引中找到主键，再回到聚簇索引查找完整数据（回表）。
   - 覆盖索引：查询所需的所有字段都在索引中（包含在联合索引的列中），无需回表。优化方式：将常用查询字段加入联合索引。
   - 例：索引 `(user_id, name, age)`，查询 `SELECT name, age FROM t WHERE user_id = 1` 可走覆盖索引。

2. **联合索引命中分析（最左前缀原则）：**
   - `WHERE a=1 AND b=2 AND c=3` → 完全命中 (a, b, c) ✅
   - `WHERE a=1 AND c=3` → 命中 a，c 无法利用索引（b 被跳过），MySQL 8.0+ 有索引跳跃扫描（Index Skip Scan）可能优化 ✅（部分）
   - `WHERE b=2 AND c=3` → 不命中（缺少最左列 a）❌
   - `WHERE a=1 AND b>2 AND c=3` → 命中 a 和 b（范围条件），c 无法利用索引（范围查询后的列失效） ✅（部分，a + b 范围）
   - `WHERE a=1 ORDER BY b` → 命中 a，且 b 可用于排序优化（避免 filesort）✅

3. **慢查询排查流程：**
   - `EXPLAIN` 分析执行计划：看 type（ALL/index/range/ref/const）、rows、Extra（Using filesort/Using temporary）。
   - 检查索引：是否缺少索引、索引失效（如函数包裹列、隐式类型转换、like '%xx'）。
   - 检查数据量：表是否过大需要分区或分表。
   - 优化手段：(a) 添加/调整索引；(b) 改写 SQL（避免子查询，使用 JOIN）；(c) 减少返回字段（避免 SELECT *）；(d) 分页优化（深分页用延迟关联）；(e) 数据归档（历史数据迁移）。

4. **主从延迟处理：**
   - 场景：写主库后立即读从库可能读到旧数据。
   - 方案：
     - **强制走主库：** 关键业务（如写后读）强制路由到主库。GORM 可通过中间件或标记实现。
     - **延迟感知：** 监控 `Seconds_Behind_Master`，延迟超阈值时自动切主库。
     - **业务层补偿：** 写入后在缓存中写入最新值，短时间内优先读缓存。
     - **半同步复制：** 确保至少一个从库收到 binlog 后才返回写成功，减少延迟窗口。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 索引原理 | 3 分 | 聚簇/二级索引、回表、覆盖索引概念准确 |
| 索引命中 | 3 分 | 最左前缀分析正确，范围查询后失效等细节到位 |
| 慢查询优化 | 2 分 | 排查流程系统化，优化手段丰富 |
| 主从延迟 | 2 分 | 有多种处理方案，能结合业务选择 |

---

## 五、Elasticsearch

### 题目 9：ES 索引设计与性能优化

**问题：**
你基于 ES 设计了机构信息存储索引以支撑多维度检索：
1. 你的 ES 索引 Mapping 是如何设计的？有哪些字段？不同字段类型（keyword vs text）的选择依据是什么？
2. 分词器是如何选择和定制的？中文分词有哪些方案？
3. ES 的写入和查询性能如何优化？你遇到过哪些性能问题？
4. ES 与 MySQL 的数据同步是如何实现的？如何保证一致性？

**参考答案：**

1. **Mapping 设计：**
   - 典型字段：机构名称（text，需全文检索）、机构 ID（keyword，精确匹配）、行业类目（keyword 数组）、注册时间（date）、状态（keyword/integer）、达人数量（integer，范围查询）、评分（float）。
   - keyword vs text：
     - keyword：不分词，用于精确匹配、聚合、排序（如 ID、状态、枚举值）。
     - text：分词后建立倒排索引，用于全文搜索（如名称、描述）。
   - 多字段（multi-field）：同一字段同时定义 text 和 keyword 子字段，满足全文搜索和精确匹配的双重需求。

2. **分词器：**
   - 英文：标准分词器 + lowercase filter。
   - 中文：IK 分词器（ik_max_word 最细粒度分词用于索引，ik_smart 智能分词用于搜索）。
   - 定制：添加业务专有词汇到自定义词典（如品牌名、行业术语），避免被错误切分。
   - 同义词：可配置同义词过滤器。

3. **性能优化：**
   - 写入优化：(a) 批量写入（Bulk API）；(b) 调大 refresh_interval（如 30s），减少 segment 刷新频率；(c) 写入时关闭副本，完成后再开启。
   - 查询优化：(a) 合理使用 filter 而非 query（filter 有缓存且不计算评分）；(b) 避免深分页（用 search_after 替代 from+size）；(c) routing 将相关数据路由到同一分片；(d) 合理设置分片数和副本数。
   - 遇到的问题示例：字段过多导致 mapping 膨胀、nested 类型查询性能差、热点分片不均等。

4. **数据同步：**
   - 方案一：双写 —— 业务代码同时写 MySQL 和 ES。简单但一致性难保证。
   - 方案二：CDC（Change Data Capture）—— 通过 Canal/Debezium 监听 MySQL binlog，实时同步到 ES。推荐方案，解耦且可靠。
   - 方案三：MQ 中转 —— 业务写 MySQL 后发消息到 MQ，消费者写 ES。有延迟但可重试。
   - 一致性保障：(a) 定期全量对账（比对 MySQL 和 ES 数据差异）；(b) 消费端幂等处理；(c) 兜底的全量重建机制。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| Mapping 设计 | 3 分 | 字段类型选择合理，理解 keyword/text 的区别 |
| 分词方案 | 2 分 | 了解 IK 分词器，有定制经验 |
| 性能优化 | 3 分 | 读写优化手段丰富，有实际案例 |
| 数据同步 | 2 分 | 同步方案合理，有一致性保障措施 |

---

## 六、消息队列

### 题目 10：MQ 原理与实践

**问题：**
1. 你在项目中使用 MQ 的场景有哪些？每个场景为什么要用 MQ 而非直接同步调用？
2. 如何保证消息不丢失？从生产者、Broker、消费者三个环节分别说明。
3. 如何保证消息的幂等消费？请结合你的业务场景举例。
4. 死信队列和延迟队列各自的原理和使用场景是什么？

**参考答案：**

1. **MQ 使用场景：**
   - **异步解耦（数据导出）：** 主服务不直接执行耗时的导出操作，通过 MQ 交给离线服务处理，解耦主链路。
   - **流量削峰（活动圈选/结算）：** 批量处理任务通过 MQ 平滑消费，避免瞬时流量打垮下游。
   - **异步通知（状态变更）：** 机构状态变更后，通过 MQ 通知多个下游系统（ES 更新、缓存失效、消息推送等），避免同步调用的耦合和延迟。
   - 为什么不同步：同步调用导致主链路延迟增加、耦合度高、失败时需要复杂的重试/回滚逻辑。

2. **消息不丢失保障：**
   - **生产者端：** (a) 同步发送 + 确认机制（等待 Broker 返回 ACK）；(b) 发送失败重试（指数退避）；(c) 本地消息表：先写本地 DB 再发 MQ，定时任务扫描未发送成功的记录重试。
   - **Broker 端：** (a) 持久化存储（消息落盘）；(b) 多副本（主从复制），至少 ISR 中有一个副本确认。
   - **消费者端：** (a) 手动 ACK（消费成功后才确认）；(b) 消费失败重试（退避策略）；(c) 超过重试次数进入死信队列人工处理。

3. **幂等消费：**
   - 方案一：唯一消息 ID + 去重表。每条消息携带唯一 ID，消费前查去重表，已处理则跳过。
   - 方案二：业务状态判断。如结算单状态为"已结算"则跳过重复的结算消息。
   - 方案三：数据库唯一约束。利用 `INSERT ... ON DUPLICATE KEY` 或 `UPSERT` 语义。
   - 业务示例：达人奖励发放 —— 结算单 ID 为唯一键，重复消费只会执行一次发放。

4. **死信队列与延迟队列：**
   - **死信队列（DLQ）：** 消费多次失败后的消息进入死信队列，供人工排查和处理。使用场景：消费逻辑 bug、下游服务长时间不可用等导致的消费失败。
   - **延迟队列：** 消息在指定延迟时间后才可被消费。使用场景：(a) 延迟双删缓存（更新 DB 后延迟 500ms 再删缓存）；(b) 超时取消（订单 30 分钟未支付自动取消）；(c) 定时通知。
   - 实现方式：RocketMQ 原生支持延迟级别；Kafka 需要借助额外机制（如时间轮）；RabbitMQ 通过 TTL + 死信交换器实现。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 场景理解 | 2 分 | 使用场景合理，与业务紧密结合 |
| 消息可靠性 | 3 分 | 三个环节的保障措施完整 |
| 幂等消费 | 3 分 | 方案可行且有具体业务案例 |
| DLQ/延迟队列 | 2 分 | 原理正确，使用场景合理 |

---

## 七、系统设计

### 题目 11：设计一个活动配置化平台

**问题：**
请设计一个通用的活动配置化平台，支持以下能力：
- 运营可通过后台配置创建新活动（无需开发介入）
- 支持多种活动类型（拉新、促活、GMV 目标等）
- 支持实时/离线指标的进度计算
- 支持自动化奖励结算
- 支持百万级用户参与

请从数据模型、核心流程、技术选型、可扩展性等方面展开。

**参考答案要点：**

1. **数据模型：**
   - 活动表（activity）：ID、名称、类型、时间范围、状态、配置 JSON 等。
   - 规则表（rule）：活动 ID、规则类型（圈选/任务/奖励）、规则表达式（如 JSON DSL）。
   - 任务表（task）：活动 ID、任务描述、指标类型、目标值、计算方式。
   - 参与记录表（participation）：用户 ID、活动 ID、任务 ID、进度值、状态。
   - 结算单表（settlement）：用户 ID、活动 ID、金额、状态、幂等 key。

2. **核心流程：**
   - 活动创建 → 人群圈选 → 活动推送 → 用户参与 → 进度计算（实时 + 离线）→ 达标判定 → 奖励结算 → 发放通知。
   - 实时进度：基于事件流（Kafka）+ 实时计算（Flink/Go 消费）更新 Redis 中的进度计数。
   - 离线进度：Hive/Spark 批量计算 T+1 指标，结果写入 CK。

3. **技术选型：**
   - 配置存储：MySQL + 配置 JSON（或独立的规则引擎）。
   - 进度存储：Redis（实时计数器） + CK/MySQL（离线指标）。
   - 任务调度：分布式定时任务框架（如 XXL-JOB）。
   - 消息通信：MQ（Kafka/RocketMQ）。
   - 规则引擎：简单场景用 JSON DSL + 代码解析；复杂场景可引入 Drools 或自研 DSL。

4. **可扩展性：**
   - 新活动类型：通过策略模式 + 配置化，新增活动类型只需注册新的规则解析器。
   - 新指标类型：指标计算插件化，新增指标只需实现计算接口。
   - 水平扩展：参与记录按用户 ID 分片，结算任务按活动分片并行处理。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 数据模型 | 3 分 | 实体关系清晰，字段设计合理 |
| 流程完整性 | 3 分 | 从创建到结算的全流程覆盖 |
| 技术选型 | 2 分 | 各组件选型合理，能说明原因 |
| 可扩展性 | 2 分 | 有良好的扩展设计，不硬编码 |

---

### 题目 12：高并发秒杀系统设计

**问题：**
设计一个秒杀系统，要求：
- 商品库存 1000 件，预计 QPS 峰值 50 万
- 不能超卖，不能少卖
- 用户体验良好（快速响应，排队有反馈）
请给出你的架构设计方案。

**参考答案：**

1. **分层设计：**
   - **前端层：** 静态页面 CDN 化，按钮防抖，倒计时本地计算，减少无效请求。
   - **网关层：** 用户维度限流（同一用户 N 秒内只能请求 1 次）、黑名单过滤、请求签名校验防刷。
   - **服务层：** 先用 Redis 原子操作（`DECR`）快速判断库存，库存为 0 直接返回"已售罄"。扣减成功后发消息到 MQ。
   - **订单层：** MQ 消费者创建订单、扣减数据库库存（最终一致性）。

2. **库存方案：**
   - 预热：秒杀开始前将库存加载到 Redis。
   - Redis 扣减：`DECR stock_key`，原子操作无需加锁。返回值 >= 0 表示抢购成功，< 0 表示已售罄（需回补：`INCR`）。
   - DB 兜底：消费者在 MySQL 中用乐观锁（`UPDATE stock SET count = count - 1 WHERE count > 0`）做最终扣减。
   - 不超卖保障：Redis 原子操作 + DB 乐观锁双重保障。

3. **异步下单：**
   - 扣减 Redis 库存成功 → 发送 MQ 消息 → 返回用户"排队中"。
   - 消费者异步创建订单 → 通过轮询/WebSocket 通知用户下单结果。
   - 好处：秒杀接口只需处理 Redis 操作（微秒级），QPS 可达数十万。

4. **兜底方案：**
   - 超时取消：用户未在规定时间内支付，释放库存。
   - 降级方案：Redis 异常时直接返回"系统繁忙"，不穿透到 DB。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 分层架构 | 3 分 | 前端→网关→服务→订单分层清晰 |
| 库存方案 | 3 分 | Redis 原子操作 + DB 乐观锁，不超卖 |
| 异步处理 | 2 分 | MQ 异步下单，接口快速响应 |
| 兜底降级 | 2 分 | 异常场景处理完善 |

---

## 八、故障排查与线上运维

### 题目 13：线上问题排查

**问题：**
场景一：MCN 平台某个接口在某天下午突然出现大量超时（P99 从 30ms 飙升到 5s），请描述你的排查流程。

场景二：消费者服务消费 MQ 消息的速度突然下降，消息积压持续增长，如何排查和处理？

**参考答案：**

**场景一：接口超时排查：**

1. **确认范围：** 是所有接口还是特定接口？是所有实例还是部分实例？看监控面板确认影响面。
2. **检查近期变更：** 是否有代码发布、配置变更、数据变更？有则优先回滚。
3. **查看链路追踪：** 通过 Trace 系统（如 Jaeger/字节内部的 trace 系统）看慢请求的各阶段耗时，定位具体是哪个环节变慢（Redis、MySQL、RPC 调用、本地计算）。
4. **下游排查：**
   - Redis：检查 Redis 慢日志（`SLOWLOG`）、连接数、内存使用、是否有大 Key 操作或 AOF 重写。
   - MySQL：检查慢查询日志、锁等待（`SHOW PROCESSLIST`）、连接池是否打满。
   - 下游 RPC 服务：检查其健康状态和延迟。
5. **本机排查：** CPU 使用率、GC 频率（`GODEBUG=gctrace=1`）、goroutine 数量（pprof）、是否有死锁。
6. **网络排查：** 丢包率、带宽、DNS 解析等。

**场景二：MQ 消息积压排查：**

1. **消费者健康检查：** 消费者是否正常运行？实例数是否减少（如 Pod 被驱逐）？
2. **消费速度分析：** 单条消息处理时间是否变长？通过日志或监控确认。
3. **下游依赖检查：** 消费者依赖的服务（DB、Redis、外部 API）是否异常？如 DB 慢查询导致消费变慢。
4. **处理方案：**
   - 短期：增加消费者实例数（水平扩容）。
   - 如果是某类消息处理异常导致卡住：临时跳过或转入死信队列。
   - 如果是突发流量：增加消费者并发度（调大 consumer group 的 partition 消费者数）。
5. **预防：** 设置消费 Lag 告警、消费者处理时间告警、消费者实例数告警。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 排查思路（场景一） | 4 分 | 流程系统化，从表象到根因层层深入 |
| 排查思路（场景二） | 3 分 | 能快速定位积压原因，处理方案实际可行 |
| 工具使用 | 2 分 | 能提到具体的排查工具和命令 |
| 预防意识 | 1 分 | 有事后改进和预防措施 |

---

### 题目 14：压力面 — 缓存框架设计挑战

**问题：**
你推广的缓存最佳实践框架在某天出现了严重问题：由于 Redis 集群发生故障切换（主从切换），导致大量缓存 key 在短时间内失效。此时恰好是业务高峰期，大量请求穿透到 MySQL，MySQL 连接池打满，核心接口全线超时。作为框架 Owner，你如何应对？事后如何改进框架？

**参考答案：**

1. **紧急应对（前 5 分钟）：**
   - 启动限流：在网关层或服务层对 DB 查询做限流，只放行部分请求，其余返回降级数据。
   - 启动本地缓存：切换到进程内缓存（如 Go 的 `groupcache` 或内存 map + TTL），虽然命中率低，但能缓解一部分压力。
   - 手动确认 Redis 集群状态：是否已恢复？是否需要人工介入？

2. **恢复阶段（5-30 分钟）：**
   - Redis 恢复后，启动缓存预热：从 DB 批量加载热点数据到 Redis（预热脚本或异步任务）。
   - 逐步放开限流，观察 Redis 和 DB 的负载情况。
   - 预热完成后切回正常模式。

3. **事后改进框架：**
   - **多级缓存：** 增加进程内 L1 Cache（如 go-cache、freecache），即使 Redis 故障也有兜底。
   - **熔断降级：** 检测到 Redis 不可用时，自动切换到降级模式（直连 DB + 限流），而非让所有请求穿透。
   - **缓存预热机制：** 框架内置 Redis 故障恢复后的自动预热能力。
   - **Singleflight 增强：** 确保缓存穿透时，同一 key 只有一个请求到 DB，其他请求等待结果。
   - **连接池保护：** MySQL 连接池设置合理的上限和等待超时，避免被打满。
   - **告警增强：** Redis 集群故障切换时第一时间告警，提前准备。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 应急能力 | 3 分 | 限流、降级、本地缓存等止血手段快速有效 |
| 恢复能力 | 3 分 | 缓存预热和逐步放量的恢复流程清晰 |
| 框架改进 | 4 分 | 改进措施全面且可落地（多级缓存、熔断、Singleflight 等） |

---

## 九、华为经历相关

### 题目 15：可观测性平台过载检测

**问题：**
你在华为负责了可观测性平台的过载专项，将过载溯源时间从 30 分钟缩短到 1 分钟内：
1. "过载检测"的技术方案是怎样的？基于什么指标判定服务过载？
2. "溯源"是如何实现的？如何在复杂的微服务调用链中快速定位过载根因？
3. 从 30 分钟到 1 分钟，具体优化了哪些环节？

**参考答案：**

1. **过载检测方案：**
   - 指标采集：CPU 使用率、内存使用率、请求延迟（P99/P999）、错误率、队列积压深度、线程池饱和度等。
   - 判定规则：多指标联合判定，如"CPU > 80% AND P99 > 阈值"持续 N 秒。
   - 检测方式：(a) 规则引擎（静态阈值）；(b) 动态基线（基于历史数据的异常检测，如 3σ 原则或 EWMA）。

2. **溯源实现：**
   - 基于服务依赖拓扑图：构建微服务调用关系的 DAG。
   - 从告警节点出发，沿调用链向上/向下追溯，结合时间窗口内各节点的指标异常情况，定位根因服务。
   - 可能使用的算法：因果推断、异常传播分析、相关性分析。

3. **优化环节：**
   - 数据接入实时化：从分钟级批量采集优化为秒级实时流处理。
   - 检测自动化：从人工看图判断优化为自动检测 + 告警。
   - 溯源自动化：从人工逐个排查优化为自动拓扑分析 + 根因推荐。

**评分标准（满分 10 分）：**
| 维度 | 分值 | 标准 |
|------|------|------|
| 检测方案 | 3 分 | 指标选择合理，判定逻辑清晰 |
| 溯源能力 | 4 分 | 能描述基于拓扑的根因分析方法 |
| 优化分析 | 3 分 | 各环节优化点具体清晰 |

---

## 总评表

| 题号 | 类别 | 考察重点 | 满分 | 难度 |
|------|------|----------|------|------|
| 1 | 项目深挖 | MCN 平台高可用架构 | 10 | ⭐⭐⭐ |
| 2 | 项目深挖 | 多 Loader 并发优化 | 10 | ⭐⭐⭐ |
| 3 | 项目深挖 | MQ 异步导出方案 | 10 | ⭐⭐⭐ |
| 4 | 项目深挖 | 活动配置化平台 | 10 | ⭐⭐⭐⭐ |
| 5 | Go 语言 | Goroutine/Channel 并发模型 | 10 | ⭐⭐⭐ |
| 6 | Go 语言 | GC 与内存优化 | 10 | ⭐⭐⭐⭐ |
| 7 | 缓存 | 缓存最佳实践框架 | 10 | ⭐⭐⭐⭐ |
| 8 | MySQL | 索引设计与慢查询优化 | 10 | ⭐⭐⭐ |
| 9 | ES | 索引设计与性能优化 | 10 | ⭐⭐⭐ |
| 10 | MQ | 消息队列原理与实践 | 10 | ⭐⭐⭐ |
| 11 | 系统设计 | 活动配置化平台设计 | 10 | ⭐⭐⭐⭐ |
| 12 | 系统设计 | 高并发秒杀系统 | 10 | ⭐⭐⭐⭐ |
| 13 | 故障排查 | 线上问题排查 | 10 | ⭐⭐⭐ |
| 14 | 综合压力 | 缓存故障应急与改进 | 10 | ⭐⭐⭐⭐ |
| 15 | 华为经历 | 可观测性平台过载检测 | 10 | ⭐⭐⭐ |

---

## 建议面试流程（60 分钟）

| 阶段 | 时间 | 内容 |
|------|------|------|
| 项目深挖 | 25 分钟 | 从题目 1-4 中选 2 题深入追问，重点考察架构理解和技术决策能力 |
| 技术基础 | 15 分钟 | 题目 5（Go 并发，必问）+ 题目 7 或 8 选 1 题 |
| 系统设计 | 15 分钟 | 题目 11 或 12 选 1 题 |
| 故障排查 | 5 分钟 | 题目 13 或 14 选 1 题 |

---

## 综合评价维度

| 维度 | 权重 | 说明 |
|------|------|------|
| **技术深度** | 25% | Go 语言、缓存、数据库等核心技术的理解深度 |
| **工程能力** | 30% | 实际项目中的设计、实施、优化能力，是否有生产环境的实战经验 |
| **系统思维** | 20% | 能否从全局角度思考架构，权衡取舍 |
| **业务理解** | 10% | 是否理解技术方案背后的业务需求和约束 |
| **沟通表达** | 10% | 技术问题的阐述是否清晰、有条理、有重点 |
| **学习成长** | 5% | 对新技术的关注度，从华为到字节的成长轨迹 |
